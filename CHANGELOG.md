# 变更日志

形制按Keep a Changelog；版本语义见decisions/0010与0012（0.x期：
minor可破坏兼容，patch只含兼容修复，**不回port**——修复只进新版本）。
发版脚本强制：版本必须先在本页有条目才准发。

## Unreleased

### 张力驱动链：磁粉离合器＋理想PID（2026-08-17，决策0062轨道乙）

- **新增`physics_engine.drives`**（力学域，实验档）：`MagneticParticleClutch`
  （电流→扭矩，线性段＋**截断**饱和＋**零阶保持的精确**一阶磁滞——命令在一步内恒定时
  那一步没有离散误差，前向Euler在`dt > 2τ`时会发散而那个发散是纯数值的）、
  `SpoolTension`（`T = M/R(n)`，半径随匝数生长：同扭矩下500匝张力掉45%，
  真机ATC600的锥度张力功能就是为它存在的）、`PidController`（积分双边限幅、
  首步微分为零、状态不可变）、`TensionLoop`（把四者串成闭环，时延经
  `actuators.ActuationDelayLine`进环——**不自己实现时延**）；
- **判据取纯积分闭环，因为它精确是二阶系统**（PI闭环带零点`−Ki/Kp`，标准超调式
  对它不成立）：`ω_n = sqrt(K·Ki/τ)`、`ζ = 1/(2·sqrt(τ·K·Ki))`。三档阻尼比下
  超调、峰值时刻、零静差全中；**超调与峰值时刻必须并判**——只判超调时把`τ`与`Ki`
  同乘一个倍数不会被发现。整环收敛阶**实测二阶**（比值4.025/3.845/4.527），
  而不是从"前向Euler是一阶"推出来的一阶——**阶是量出来的**；
- **时延从规范里的一句话变成一个数**：spec/10写着"没有时延训练出的策略上真机会翻车"，
  实测（`ζ=0.5`、`dt=1ms`）时延0/5/10/20/40 ms给出超调
  0.16305/0.20633/0.25747/0.38234/0.70026——**20 ms把超调翻了2.35倍**，40 ms翻4.3倍
  且稳态开始变坏。没有这条门，`actuators`那整套时延机械只是个更严谨的摆设；
- **一条被实测纠正的判断**：写模块时判定超调式与`contact.restitution_from_damping_ratio`
  "逐位相同"，实测当场否掉（`ζ=0.5`处差1.83倍）。两式确实共享
  `exp(−ζΦ/√(1−ζ²))`，**只差`Φ`**：阶跃峰值是`π`，而0052第一节裁的截断约定
  给出`2·acos(ζ)`。**只在`ζ=0`处重合。** 有一条门守着这个"不同"；
- **执行器物理没有长在`actuators.py`里**（0062第9.1节）：那样会让基座反向依赖物理域，
  正是0035抓过的"方向反了"。`actuators`那套门**一条没动、逐字节不变**。
  代价如实写：spec/10的`Actuator.apply`这个方法名今天仍不存在；
- **`identity.BASE_UNIT_SUFFIXES`加`amp`**（0062第9.2节，纯扩张）：spec/14早已登记
  "基础集一个电学单位都没有"，0048挂账的触发条件写的是Ic，**来的却是磁粉离合器的
  线圈电流**。用`amp`不用SI符号`a`的理由是实测——仓里已有15个`_a`结尾的名字，
  `body_a`/`name_a`/`node_a`一类是成对命名、根本没有单位，加`a`等于把轴2规则3废掉；
  有一条门守着它们仍被拒；
- 能力位**S6.3由todo进partial**。**主分母仍是0/6**：`drives`产出的是一个力的数值，
  尚无案例让它经`PointLoad`真的加载在带材上——那是端到端的活；
- 源码字节台账重记`697618`（涨7.5%），上限由`720896`抬到`1 MiB`（约1.503倍）并在
  `source_bytes_ceiling_history`留一行——旧余量只剩23278字节，与0019那次
  （1224字节）是同一种没有意义。

### 绞盘案例与一道门本身的修正（2026-08-17，决策0062轨道甲收口）

- **新增案例`cases/capstan_tension_ratio`**（B档、local_batch）：带材绕**真机导轮**
  （R=50mm、轴向半宽8.5mm，2026-07-07现场实测尺寸）90°，8个接触经
  `advance_contacts_quasistatic`同时滑移。**一条装置穿四层**——圆柱罚法向、
  库仑return-map、多槽位步进器、`AxialStretch`的张力传递；
- 四条判据分成**恒等式两条**（全滑移下`|T_i|/(μN_i)`实测**2.22e-16**落在摩擦锥上；
  法向力＝两侧张力的法向合量，实测**2.44e-07**＝残差容差÷法向力）与**收敛结果两条**
  （逐节点比对精确离散式`(1+μtan(Δφ/2))/(1−μtan(Δφ/2))`，载荷步**一阶**收敛，
  实测比**2.10/2.06**）。**不判端到端`T_last/T_first`**——两端是半节点，
  实测随载荷步非单调（120步1.6030、240步1.5562、480步1.5341）；
- **新增`energies.AxialStretch.axial_force_n`**：逐边轴向力，绞盘判据要判的量。
  与`normal_force_n`同一条理由做成公开面——判据不该去读私有闭包；
- 案例计数26→27，走完`state`→`energies`→`solve`整条路的从6条到**7条**
  （带接触的从三条到**四条**）。能力位**S6.5由todo进partial**：
  带材在接触里仍是中心线上的点、没有宽度，碰不到法兰；轮固定不自旋；
  接触对是声明的不是检测的。**主分母仍是0/6**；
- **修了`tests/perf/test_budgets.py`的陈旧度门**：它把墙钟放进了功能路径，
  而同一个文件的开头正写着"墙钟不进门……不受宿主负载影响，因此可以放心进功能路径"。
  实测同一份代码在空闲机上比值2.71（绿）、邻居项目开跑后3.20（红）——
  **负载改变了功能结论**，0014法则2禁止的正是这件事；而运行器在同一份回执里
  已写明`resource=UNQUALIFIED`、`performance=NOT_EVALUATED`，
  **一道门拿另一道门明文作废的数判红，判出来的红不带信息**。
  判据抽成纯函数`accept_timing_staleness`并补三个分支的必红；
  "在安静机器上重测`accept_full`中位数"按触发条件登记进缺口清册。

### 绕线机端到端主线开工：圆柱侧面接触与多槽位步进器（2026-08-17，决策0062轨道甲）

- **新增`contact.PenaltyCylinderContact`**（实验档）：杆节点对**有限长圆柱侧面**的罚法向
  接触。活动条件是**两条**——穿透且轴向仍在筒宽内；Hessian为
  `k·(n⊗n) + (k·g/ρ)·(t⊗t)`，`t = a × n`，**几何刚度只出现在周向**
  （沿轴移动不改变到轴的距离，故`H·a`恰为0）。另给
  `axial_clearance_mm`/`radial_distance_mm`/`outward_normal`三个观测量，
  分别让案例为端沿硬切、近轴失精与摩擦接线设门；
- **实测记下一条会咬人的界**：圆柱上的间隙`g = ρ − R`是两个`O(R)`量相减，
  于是**半空间那条"跨六个数量级刚度法向力一个ulp都不动"在这里不成立**。
  可达残差地板是`0.5·k·ulp(R)`（两半径×五档刚度共10组实测，比值全在
  `[0.15, 0.48]`），法向力精度因此是`k·ulp(R)`的**绝对**量。导轮`R=50mm`、
  `k=1e4`时地板`3.6e-11 N`，而链路张力是10—30N——够用，但容差必须按它定；
- **新增`contact.advance_contacts_quasistatic`与`ContactPoint`/`MultiContactStep`**：
  **多个槽位同时迭代**，兑现0050第一节登记的欠账（也是S3.6`missing`那一条）。
  所有粘着弹簧进**同一个**`TangentialStickSpring`并按声明次序排列，
  收敛判据取**全体**。老函数`advance_contact_quasistatic`**一个字未动**；
  忠实性由两条门守着：单接触时12组（三档横载×四档趟数）**逐字节相同**，
  以及横载3.0N的发散构型上两者**同样地失败**；
- 多槽位实测的第二条界：判别从滑移翻成粘着的那一趟走**9311次回溯**、
  残差涨到`3.19e-9`（平时`3.6e-11`），病根是0050第四节的`C¹`非`C²`。
  已登记进缺口清册，触发条件是它成为绕线机主线的主要成本时；
- 必红实测：圆柱项9条注错全被抓到（最低1条）；多槽位7条全被抓到，
  **其中"试探力错位"第一轮红0条活了下来**——补门后才抓住，
  且补门的第一版判据（判趟数）照样抓不住，判对量（接地点自己的切向力）才行；
- 决策记录[0062](docs/decisions/0062_绕线机端到端物理主线开工裁决_20260817.md)：
  用户裁定主战场在引擎、目标为机制级、张力控制建到"磁粉离合器+理想PID"层；
  同时**重开0034第四节**（触发条件"带法兰导轮的书面需求"已到达）并裁定
  **仍不改spec/11词汇**——蹭边要的是法兰内环面，而`modelgen`按0032已把带法兰带盘
  精确分解为三件独立`FiniteCylinder`，内环面用既有原语表达得了；
  `geometry.mass_properties`对带法兰圆柱**维持失败关闭**，它的触发条件没有到来。

### WDS轴向—弯曲N/M耦合单站正式进入main记录（2026-08-16，消费侧）

- `winding-deviation-sim`以新增`spool_wrap_case 1.3`、`engine_section_history 1.1`和独立
  plugin，在R25 ep10n v003固定名义材料顶点64；两相邻边的对偶单元轴向应变与
  easy-axis曲率同时交给0.6.0公开截面API，消费侧装配N、M与完整2×2一致切线；
- WDS显式减去目标顶点拥有的左右edge-stretch各一半，避免与原生StretchEnergy双计数；
  trial保持只读，只有accepted continuation点提交同一份64纤维history。真实案例完成
  80节点、39个feed步和64次提交；accepted路径最大绝对轴力为`1.6001820726530358N`，
  最大绝对轴向—曲率耦合切线超过`1477N·mm`，证明不是只在单元测试中构造耦合项；
- 旧1.2案例的resolved/dependencies闭包保持逐字节不变。最终WDS提交
  `bef42882de776b231e67d3d09c45f8e6b6981008`已快进进入其`main`并由远端反向核验；
  V2为1369 passed/10 skipped、timing 4 passed/1个master无npm明示skip、Mac Node
  101/101，VA为1369 passed/10 skipped、timing 4/4；两份master性能正本也已重签；
- 本条只记录消费方利用既有0.6.0公开API完成的单站机制。physics-engine没有源码、API、
  wheel或tag变化，不触发发版；整杆多站、引擎原生轴向全局项、hard-axis、扭转、snap
  history、材料实测标定和动态仍未完成，S5.1保持partial。

### WDS R25塑性history正式进入main记录（2026-08-16，消费侧）

- `winding-deviation-sim`验收树`4b7b42a328515a832bde4feacaec3eaff2c14225`新增
  1.2独立plugin，在R25 ep10n v002
  固定名义材料顶点64，使用正式0.6.0的64纤维理想弹塑性；history只在accepted
  continuation点与state/reference/friction事务提交，被拒候选回滚并跨growth携带；
- 新案例80节点、39个feed步全部收敛，最终64次提交、58/64个纤维history非零，没有启用
  snap；严格`engine_section_history.json`与既有结果原子发布，旧15案解析闭包与plugin身份
  保持。双跑物理结果指纹一致，性能正本、master精确0.6.0环境、最终V2与隔离VA均通过；
- 用户随后授权正式合入；验收树无冲突快进到WDS `main`，状态收口提交为
  `013ffbef90b905e49817a5e95e6f87522101a192`。新案例仍为`hypothesis_only`固定单站
  机制证据，不代表整杆塑性、snap历史、轴向耦合或动态完成。本仓没有源码改动，不重建
  wheel、不改`v0.6.0`tag，也不触发发版。

### WDS正式接缝交付记录（2026-08-13，文档收口）

- `winding-deviation-sim`已在提交`f88986f7...`把正式0.6.0 wheel、单站接缝、独立R250
  v002案例、两份重签性能正本与交付文档合入main；wheel SHA-256为
  `53114e430a62c2023fb02c512b40f3cfbb558b4d0ac32819ec81a17922ea71be`；
- 既有14案完成双版本门：14/14解析字节一致，10个收敛运行物理指纹一致，3个既有
  `feed-nonconvergence`诊断一致，余下1个保持input-only；master隔离V1/V2/VA和开发Mac
  Node 101/101均通过；
- 本条只更新发布后的消费方事实，不改变`v0.6.0`tag或已发布wheel，也不把默认关闭的
  R250线弹性单站外推为R25塑性、多站、轴向耦合或动态已经迁移。

## 0.6.0 — 2026-08-13

### 0.6.0与WDS单站opt-in采纳接缝（2026-08-13）

**决策0061。0.6.0按不可变wheel、tag与双远程链路正式发布。** 0.5.0已经作为
不可变wheel发布，0059/0060不能继续伪装成同一版本，故本版升为0.6.0。新增显式
`LinearElastic1D`，让WDS第一片线弹性采纳不再依赖任意大的屈服应力哨兵；消费方独立
分支以默认关闭、单站easy-axis、准静态的适配器调用本仓，动态与塑性历史仍失败关闭。
引擎发版、消费方合入和master验收仍是三道独立门；本条在0.6.0发版时只解除第一道，
后两道的完成事实记入上方Unreleased发布后收口，不反写`v0.6.0`制品边界。
- Linux master验收揭示`os.utime(path)`在同一时钟tick内可能不改变mtime；溯源观察窗的
  必红用例改为显式推进2秒并先断言注错确实改变文件签名，避免测试没有真正制造漂移却
  把生产校验误判为漏检。生产四点签名实现未放宽。

### 阶段4全局截面站点：WDS运动学、增量势与收敛后历史提交（2026-08-13）

**决策0060，随0.6.0发布。** 把0059的矩形弹塑性纤维截面接入一个真实WDS式
三节点/两边扭角站点、`EnergyRegistry`与全局Newton；WDS消费仓仅只读核验，尚未迁移。

- 新`section_beam`按WDS/Bergou公式从9个节点位置+2个边扭角生成easy-axis曲率；
  截面M与一致切线经解析二阶jet装入全局残差/Hessian，含不能漏的几何刚度项；
- 64点布局共有139个状态标量，但只有11个运动学坐标，128个点历史槽由受保护求解
  入口自动固定；失败Newton返回最后trial构型供诊断，却逐字节保留输入提交态；
- 为既有理想弹塑性return-map补同一应力曲线的闭式增量势。它含本步塑性耗散，显式
  标为`quasistatic-only`；通用动力学加速度桥遇到它失败关闭，不冒充可恢复势能账；
- 新B档案例`kirchhoff_section_vertex_springback`用独立`Fraction`闭式验加载曲率、
  弯矩、全局结点力、完整一致切线与全局回弹；六格错法必须红；
- WDS提交`c1b8fe6`的`state.py`/`energies.py`按源SHA只读生成非平面、非零扭角夹具，
  曲率、弹性能与11维梯度对住。它只证明源级兼容，不能代替WDS更新wheel、重签回执或
  既有产物逐字节不变；WDS当时的未提交groove-fit工作未被触碰；
- profile后把能量、残差、Hessian拆成零/一/二阶按需求值；同一高负载进程交替对照
  旧全二阶路径，64点全局回弹七组中位数`0.046551 s→0.019071 s`（2.44×），仍为
  4次Newton、0回溯；零/一/二阶公开结果由逐位门守一致；
- S5.1仍为partial，S1.2仍todo；机械计数仍`12/42`、同行C档仍`6/13`、端到端仍
  `0/6`。尚无轴向N联立、整杆多站、hard-axis、扭转、压扁/翘曲或真实WDS采纳；
- 收口树收集1210条测试；`accept full`两次样本为69.0/49.1秒（均低于120秒），
  非batch 1162 passed、2 skipped，batch 46 passed，三轴PASS且仓库稳定。源码递归实测646850字节，
  `720896/646850=1.115<1.6`，不调整上限；运行时依赖仍为0。

### 阶段4最小截面切片：逐点弹塑性历史、局部回弹与同行语义复核（2026-08-13）

**决策0059，随0.6.0发布。** 新增矩形中点纤维截面、一维理想弹塑性return-map、
逐点显式历史、N/M与一致切线，以及固定轴向应变下的受保护Newton曲率平衡。

- `epsilon_0/kappa`是两个广义截面运动学坐标；当前回弹只解`kappa`。积分点应力由
  广义变形导出，逐点历史会增加状态宽度与内存，但不增加全局运动学未知量；
- 参考身份绑定截面ID、宽厚、点数和求积规则，同点数不同几何不能误装旧历史；
  trial候选不修改已提交状态，外层接受后才提交返回历史；
- 新B档案例`rectangular_section_springback`用独立`Fraction`解析金标验全点分布、
  8→64点二阶收敛、自由回弹、同曲率不同历史与逐字节重放；六格典型错法必须红；
- profile后把64点回弹由40次纯二分装配改为2次区间保护Newton装配，本机中位数
  `0.011474 s→0.000852 s`，约快13.5倍；零切线或越界候选仍回退二分；
- S5.1只从todo升partial，S1.2保持todo；机械计数仍`12/42`、同行C档仍`6/13`、
  端到端仍`0/6`。没有全局梁装配、独立宽厚运动学或电磁`K_i`场求解。
- 收口树收集1197条测试；`accept full`本批两次样本51.954/54.370秒，均在120秒内
  三轴PASS，不拿单次墙钟冒充新中位数。
  源码递归实测612727字节，`720896/612727=1.177<1.6`，不调整上限；运行时依赖仍为0。

### 阶段2/3最小闭环、动态接触接线、能力位复核与性能测量链纠偏（2026-08-13）

**决策0055、0057、0058，随0.6.0发布。** 本批把“阻尼”“十球漏斗”、动态检测—响应
和四项待裁从计划变成可证伪实现，但没有把partial冒充端到端完成：机械计数由
`10/42`变为**`12/42`**，穿过物理机械的done位由3变5；同行C档仍**6/13**，
六场景端到端仍**0/6**。

- **性能报告先修身份再报数**：被计时子进程默认只认返回码0，碰撞CLI的1在调用点
  显式登记为成功语义；源码字节改为递归口径；`dist/`里的wheel只有在文件集合与每个
  `.py`字节都等同当前源码时才报告，否则标`stale_or_invalid`。各失败分支都有必红；
- **42能力位独立逐位复核并冻结分母**：未发现原10个done中的虚假或重复计数；
  每场景位数冻结为`7/5/10/7/6/7`，删并位必须先走新决策与红门。四项待裁全部收口：
  `0<e≤1`、过阻尼快根、真实接触时长分辨界、自由刚体只归主分母S3.1；
- **耗散仍走唯一装配表**：每个注册项显式标`potential|dissipation`；旧势能求和次序和
  保守加速度表达式不变；耗散项同表提供力与非负功率，独立入口累计物理耗散；
- **新增二阶阻尼积分器而不替换旧项**：`velocity_verlet_damped`在线性阻尼振子上
  实测二阶；`velocity_verlet`注册名与无阻尼路径保持不变；
- **线性法向dashpot覆盖欠阻尼到过阻尼**：平面与球-球统一用合力归零截断；
  `e=0.8`与`e=0.05`跨`ζ=1`对拍恢复系数、接触时长、稳定快根、解析耗散与能量残差；
  `e=0`只在无限阻尼极限到达并失败关闭；
- **动态检测结果开始驱动响应**：`FinalizedScene.contact_pairs`成为稳定候选池，
  `collision.py`按声明次序返回候选/broad/narrow确定性计数；新`contact_pipeline.py`
  只让球体窄相确认事件进入罚势与dashpot，活动集变化不重排0050历史槽；
- **`ten_ball_funnel`迁移到新流水线**：10球、三个解析平面、45个球-球候选同跑重力、
  两类罚响应与耗散账；末帧有门守`活动≤窄相≤broad<45`。它只支撑S3.10的partial：
  候选仍全量声明，平面未进流水线，且没有网格窄相、转动、摩擦或实验标定；
- **动态查询的10/20/64球规模门**：候选数45/190/2016、活动数9/19/63逐位确定；
  等价快路径后本机强制未缓存单帧中位数0.1481/0.4614/3.6419ms，p95为
  0.2021/0.7673/4.2928ms，同帧缓存命中约0.0008—0.0021ms；
- **profile后做结果不变的接触快路径**：单位姿态AABB不再旋转8个角点，球心直接取平移，
  世界系窄相几何每体每帧只准备一次；pipeline已经验证的活动响应不再逐请求重验。
  六条新门均先红后绿；十球0.3物理秒三次setup为3.41/3.36/3.42秒，中位数3.41秒，
  比优化前6.51秒下降47.6%。旧直连与新流水线12个共有非计时字段逐值相同；
- 源码递归实测**591651字节**，对720896上限占82.1%，`ceiling/measured=1.219<1.6`，
  按0053触发线不调上限；运行时依赖仍为0；`accept full`性能样本三次
  57.2/58.3/58.3秒、中位数58.3秒/120秒；最终树另验49.5秒，
  1181 passed、2 skipped，功能/计时/资源三轴PASS。

### 并行开工前置：十一个冲突面、开工自检、三个可选位升必需（2026-08-12）

**决策0053**。它是0041的续篇与扩容——**一份并行前置的价值，先看它承认自己上一版漏了什么**：
0041为2026-08-05那一波清了三个共享面，**那一波仍然冲突了五次**。

- **冲突面重新测绘：3个 → 11个**（按近40个提交的改动频次）。0041覆盖5个，
  **6个裸着，其中2个是0041之后才长出来的门**（公开面清册与它的门）；
- **新增`tools/check_worktree_env.py`**：四条断言证明"**这棵树验的是它自己的代码**"。
  四条的红分支各自在真worktree上实跑红过一次（包身份/空跑/收集为0/起点落后8个提交），
  正确配置的树4/4全过。必红矩阵12条常驻`tests/governance/test_worktree_env_check.py`，
  **含一条元测试，它把自己写错的第一版当场打红了**；
- **`accept.py`三个可选位升为必需**，并加入开工自检共四个，全部无条件上膛。
  旧行为"文件在才上膛，不在就记一条`absent`然后照样PASS"在单人开发下是诚实的，
  **在多代理并行下是致命的**——一个代理漏带工具文件，全队的门变虚而验收仍报PASS。
  回执字段`absent_optional_commands`保留（登记过的面，字节形制不动），语义改为
  "哪几个必需工具不见了"。**立本条前这个行为一条治理测试都没有**，已补5条含一条必红；
- **一条操作性硬结论**：工作树**不许放在`.claude/`下**。Claude Code默认落在
  `.claude/worktrees/`，而`rtime-project-check`把`.claude`当跳过目录，
  实测在那里扫**0**个文件、主仓扫292个——**放默认位置的树永远过不了断言2**。
  本仓并行工作树一律放仓外同级`../physics-engine-wt/<轨道名>/`；
- **性能台账本轮不预抬**（当前用了74.1%，余量187145字节），并登记了
  0041那条**至今没兑现过**的自我约束的触发条件。

`accept full`: PASS 32.5s/120s，测试1009条。

### 欠账逐条实测复核：九条里八条已关，其中一条三天前就关了没人划掉（2026-08-12）

**方法是实测每一条，不是读文档判断。** 结果两条与文档记的不一样：

- **`source_bytes`门没有下限——已经被关掉了，而清单没更新。** 把路径两处都写错，
  `test_the_recorded_source_bytes_is_not_stale`**当场红**。那道陈旧度门是08-06同一批加的，
  顺手把这个洞堵上了，**而没人回来划掉那一行**——这本身就是教训二的又一个实例；
- **冲突标记只认恰好7连——是真的。** 判据要求`stripped == marker`或
  `startswith(marker + " ")`，对10连的``<<<<<<<<<<``**两条都不成立**（第8个字符是`<`不是空格）。
  而`conflict-marker-size`可配，**把它调大正是为了避开内容里恰好有7连的情况**——
  也就是说，越是内容复杂的仓越可能用加长标记，**而那正是这道门最需要管用的时候**。

**修的三处**：

1. **冲突标记改成≥7连**，补16条必红，含**4条反向必红**守着"判据放宽后不许开始对
   合法markdown误红"（setext式一级标题的下划线就是一串`=`，本仓文档里真的有）。
   实证：10连的三行标记端到端抓到3处，全仓306个文件仍0误红；
2. **预测-修正趟数用尽的失败关闭**——但**做成选择进入**（`require_pass_convergence=False`）。
   无条件抛会打断所有既有调用方：`yield_excess_n`量的是**修正前**的试探力，
   而理想塑性的修正让屈服条件在**修正后**成立，`max_passes=1`加滑移时
   "用尽且残差>0"是**正常且正确**的。依据是本函数docstring自己写过的原则——
   **默认值不该替既有调用方改变行为**。
   顺带实测出一条：横载1.5 N时外层16趟压到1.4e-3，**而内层牛顿在32趟左右先死**，
   **该构型下不存在能到1e-9的趟数预算**——"迭代发散没有兜底"那笔账在龇牙，已钉成用例；
3. **步进器的slot/node落位校验**——**只做了能证明的一半**。
   `ContactSlot`与`ContactDeclaration`**都不带node**，完整对应要改0050的布局承重设计。
   补的四条挡的是会静默写坏状态的混淆：节点越界（写进锚点块=改别人的历史）、
   负节点（0050落地时`PenaltySphereContact`真吃过这个亏，``vector[-3:]``读的正是锚点槽）、
   槽指进节点块（=悄悄改位形）、槽越过向量末尾。

**台账两处记账**：`source_bytes` 533751→**547714**（用了76.0%）；
`accept_full`计时21.3s→**31.8s**（3倍容差内但确实陈旧）。

**0041那条自我约束第一次被真的执行。** 它在2026-08-05预支上限时写下"收口时必须复测"，
0053给了可判的触发线（`ceiling/measured > 1.6`才调回）。本轮复测：
`720896/547714 = 1.316 < 1.6`，**不调**，并在`source_bytes_ceiling_history`记了一行。
记它的理由是——**一条永远没人回来执行的自我约束比没有更坏，它让人以为有人会回来看**。

### 并行波次收口：四条轨道，主分母第一次有了算出来的数（2026-08-12）

#### 轨道A 积分

见上一条（步长顾问、`oscillatory_step_coefficient`、`bouncing_ball_restitution`）。

#### 轨道C 计数

**主分母第一次不从散文里来。** `docs/capability_ledger.json`把六场景拆成**42个能力位**、
同行C档做成13条，每一位带状态、理由、缺在哪、以及`case:`/`test:`形制的证据；
`tools/check_capability_ledger.py`**算**出分子并进`accept full`。第一次的数：

```
主分母 10/42位、C档 6/13条    端到端做完的场景：0/6
  其中穿过物理机械的done位：3/10（其余是闭式计算器与装配基座）
  卡在同一条结构性缺口上的位：接触5位、体积与厚度3位、历史1位、耦合1位
```

**清单选JSON不选Python模块**，理由是"**一份能执行代码的清单是一份能撒谎的清单**"——
Python模块可以写出`DONE = len([...])`这种看着像算出来、实际算别的东西的东西。

**C档算出来的分子是6，散文里那个数是对的**；但**它的构成在两处已经陈旧**，
清单是第一个记下这件事的地方：`cases/README.md`说"第7、8条各做了一半"，
**而今天是三条**——轨道A的`bouncing_ball_restitution`中途落地，
它自己的案例页写着"不验恢复系数小于1的任何情形"，故C9是`partial`不是`todo`。
**清单合并main重算后分子没动**——那正是它该做的事。

**31条必红，一个判据分支一条，全部在命令行实跑红过。** 三条元测试让分支覆盖变成机械的
而不是口号：**两个分支不许共用一个错误码**（共用会让一条红"盖住"一条从没执行过的分支，
正是教训三的形态）、每个码都必须被必红走到、每个`assert_*`都必须接进`check()`。
四次"注错回旧bug"实验证明测试不是空的。

顺带清掉两笔旧账：`MAY_BE_EMPTY`的batch豁免（**判据形态也变了**——
从"名单必须等于某常数"改成"名单里的档位必须实测收集到0条"，
**理由消失豁免就自动红**）；`check_case_pages`三个洞（**这道门此前一条测试都没有**，
新建17条）。

#### 轨道D 推导

**过阻尼分支ζ≥1推出来了**（0052登记的阻塞项）。三段闭式形状完全平行：

| 分支 | ω·t_c | e |
|---|---|---|
| ζ<1 | 2·arccos ζ | exp[−2ζ·arccos ζ/√(1−ζ²)] |
| ζ=1 | 2 | exp(−2) |
| ζ>1 | 2·arcosh ζ | **exp[−2ζ·arcosh ζ/√(ζ²−1)]** |

独立数值对拍（RK4积ODE+二分定事件，**不用任何被验闭式**）：19档ζ最大绝对偏差**5.27e-15**。

**两条改变结论的**：

1. **恢复系数没有绝对下界。** `exp(−2)=0.1353`只是**ζ<1分支**的下界。
   全ζ轴值域是开区间(0,1)，ζ→∞时`e ~ 1/(4ζ²)`。反解跨两支照样二分：
   e=0.05→ζ=1.945、e=1e−6→ζ≈500。**0052第一节登记的那条"低于0.135要先推过阻尼分支"
   的前半已兑现**；
2. **约定A（重叠归零）在ζ≥1上根本没有有限根**——sinh>0让重叠再也回不到零，
   物体被拉力粘住。**用户选的力归零约定是过阻尼区唯一能用的那个**，
   这条在裁决时并不知道。

**research/13第五节那张表三行全错**（初稿只登记了一行），真值/原值分别是
**2.398 / 1.104 / 1.010倍**。该节从"闭式代闭式"升到三条腿：闭式、独立RK4、**引擎实测**。

**三球金字塔`μc = 2−√3`精确成立，差0.0e+00**——Q(√3)上精确有理算术装配9方程/8未知的
完整刚体平衡，**秩恰为8、解唯一**，故"静定"这句话本身也复核了。
**顺手撞出一条原推导没提的前提**：两底球恰好相切是真实存在的第五个接触，
它不传力的理由不是"略去"，是顶球把它们往外推。**这条不成立则问题立刻超静定，
那个闭式就没了。**

#### 收口时改掉的一条自己写的规矩

0053第四节"轨道不自己往plans/07插行"**与本页第二节自相矛盾**：
`check_gap_register`无条件上膛后，任何写了带触发条件的决策记录的轨道，
它自己的`accept full`必然红。轨道C撞上了，**用列头写"重开条件"绕过并明写在页上**。

**让路的是那条规矩，不是门**，两条证据：轨道A（主代理自己）**根本没遵守它**——
0054落地时直接插了两行于是一路绿，**是靠违反自己刚写的规矩保持绿的**；
更要紧的是它**与plans/07存在的理由相反**——那一页立起来就是因为
"散着的欠账合起来没人看得见"，把登记推迟到收口正是在制造那个病。

### 阶段1—3的七条裁决，与并行开工前的摸底（2026-08-12）

用户定下总原则：**最真实永远是首要目标，其次是性能**。它当场改掉了一处选型。

**新增两份决策记录**：

- **0051 多点接触的前置是转动自由度**：兑现plans/09第六节登记的欠账。
  `max_points_per_pair > 1`的前置**不是**网格窄相，是接触体有转动自由度——
  接触体今天是3自由度质点，**质点没有力矩**，多给几个锚点槽只是把切向刚度乘N，
  不多算任何物理。**0050第一节那句挂在形状族上的话是错的，本页起以0051为准。**
- **0052 阶段1—3开工前的七条裁决**：plans/08三个"先裁"清单一次性收口。

**七条裁决里最要紧的一条**：LSD分离瞬间的拉力**截断**（接触结束判据从"重叠归零"
换成"力归零"）。理由是不粘的两个固体之间不可能有吸引力。
**代价两条，都是这次算出来才看见的**：

- LAMMPS `pair_granular`、Chrono `ChContactSMC.cpp`、EDEM三家逐字相同的那条反解式
  （给定`e`求阻尼系数）是**从不截断式反解出来的**，照抄会让声明的`e`与实际系统性不符。
  截断式对ζ超越，**反解要数值求根**；
- **可声明的恢复系数有下界`exp(−2) = 0.1353`**（本仓10万点复核：截断式在ζ∈(0,1)上
  单调下降，值域(0.135335, 1)）。低于它必须走过阻尼分支ζ≥1，
  **而那条闭式两个约定都没被推过**——已登记进缺口清册，且接口对`e ≤ 0.135`失败关闭。

**本仓独立复核了两条闭式**：ζ=0.9处相差**101.91倍**，与plans/08记的"100倍"一致；
极限值`exp(−2)`与理论逐位相符。

**其余六条**：主分母改成能力位（顺带兑现"没有门看着分子"与"C档13条做成清单"两笔账）；
耗散记账给`EnergyRegistry`加类型标签（不破"注册表里全是势能"，也不开第二条装配路径）；
保二阶的积分步新增一个、老的一个字节不动；步长顾问放`integrate.py`且只吃纯数字
（实测确认该模块今天零包内import，这条独立性因此保住）；`production_ready`先写死
四条翻转条件、这轮不翻；阶段3先小规模跑通并如实报级别。

**并行开工前的摸底（尚未落地为工具）**：

- **清空17个陈旧worktree + 18个分支**，释放55MB。删前逐个确认独有提交为0；
- **测穿了一条比已知更狠的坑**：共享`.venv`里editable安装的`.pth`是主仓`src`的
  **绝对路径**，所以站在worktree里`import physics_engine`**加载的是主仓的包**
  （实跑验证）。17个副本里14个带着这条软链——
  **代理可以在副本里跑完整套门、全绿、交差，而它验的是别人的代码**；
- **冲突面从3个长到11个**：0041为2026-08-05那一波清了3个，那一波仍冲突5次。
  按近40个提交的改动频次重新测绘，今天要写一行的地方有11个，
  **其中6个0041没覆盖，3个是0041之后才长出来的门**。

**修的两处文档不一致**：

- plans/07第1.1节写"四片已落／两个案例／仍缺多体接触、曲面法向"，
  而**同一页第1.3节已写三个接触案例、第六节已把曲面法向标✅**——一页三处互斥，
  **而`check_gap_register.py`全绿**（它守"决策记录有没有被引用"，看不见本页自相矛盾）。
  这是plans/09教训二的**第四个实例**，已订正并登记触发条件；
- 性能台账`source_bytes`的`measured`529440 → **533751**（实测），漂0.81%。
  5%阈值下本来是绿的，但plans/09第一节那张"实测不是印象"的表报的正是那个台账值。

### 四路对抗审核的收口（2026-08-06）

派了四个对抗审核（物理／门／文档对账／推导），**任务是证伪不是复核**。结果：

**两条静默错值（物理审核，都在"分离"上）**：

- **分离后粘着弹簧照样出力**。`TangentialStickSpring`没有间隙判据，
  而步进器此前**无条件**把它接进注册表。实测：节点抬到面上方500mm，
  法向力正确归零、regime正确报SEPARATED、报告切向力也是0，
  **而平衡位置仍被一根不存在的摩擦弹簧顶住**。更难看的是**正确的活动集会失败关闭**
  （切向没刚度→奇异），**正确的做法炸、错误的做法静默给答案**
- **分离不清切向历史**。"贴地拖2mm→抬起→空中横移到50mm→放下"那一步报出
  `slip_increment_mm = 48`，**凭空记了282.5 N·mm摩擦功，而那48mm是在空中走的**。
  同行（Chrono DEM、LAMMPS granular）失去接触时一律清切向历史

**三条失败开放（同门其它函数都是失败关闭）**：`PenaltySphereContact`不校验节点索引
（**`node = -1`被接受、`node_index_bound()`返回1所以装配门放行，
而`vector[-3:]`读的正是接触锚点槽**——算出316681 N·mm能量，全部由历史值来）；
`TangentialStickSpring`的法向校验弱于同门（**nan能过单位矢量门**，
因为`abs(nan−1.0) > tol`是`False`）；`coulomb_return_map`不校验试探力
（nan会经锚点写进状态向量，而状态是复现契约）

**门的缺口（门审核）**：

- **域隔离门对相对import一概失明**——`node.level`非零时`node.module`被整个丢掉，
  `from .optics import x`扫出的边是`physics_engine`而不是`physics_engine.optics`，
  **五条门同时失效**。门当时全绿**不是因为它挡得住，是因为九条必红全部用绝对import、
  这条分支从没被走过**。已修并补三条相对import的必红
- **四条注错能让全部977条测试保持全绿**：分离时报法向力/接触力、
  `N=0`报粘着而非分离、摩擦锥`<=`改`<`。**行为本来是对的，但没有任何门看着**。
  已补四条门，逐条注错验红
- **公开面清册门**（新）：`src/`下每个模块必须出现在`__init__.py`的API清单里。
  建门当场抓到四个未登记：`contact`、`cli`（0017登记过的历史前科，从未修）、
  `actuators`、`sensors`
- **台账的`measured`此前没有生产者也没有门**：`source_bytes`台账475269、实测529440
  （差11.4%），`accept_full`台账0.5s、实测21.3s（**差43倍**），
  而README/plans/01/spec/13三处以现在时引"验收full 3.4s"——**三个数互不相同**。
  已补两条陈旧度门并记账

**文档对账**：README与`__init__.py`两处能力边界**都还写着"没有接触与摩擦"**，
而接触是当天最大的一块代码，README自己那句"两段必须同批改"就在同一节里。
连同六场景表、案例计数（19→22）、决策编号（0049→0050）、plans/04/06/07的接触状态
一并改正

**推导审核（`research/13`）**：七组闭式**没有一条算错**，但抓到两处硬伤——
①**金字塔那节漏了力矩平衡**，`M_y = −(√3/6)·W·r ≠ 0`，
即该构型**对可转动的刚体球不是平衡态**，`μc = 1/(3√3)`依赖"转动被钉住"，
而"边界（必须写明）"那节只写了"球-球无摩擦"；
②"加摩擦后**静不定**"**没被验证过且很可能是错的**（含力矩的刚体解疑似静定、
闭式`μc = 2−√3`）。**已删旧断言、不收新结论**（本仓未独立复核，登记为待验）。
另修两处：瞬态穿透那节的倍数因子配错、一处交叉引用指错；
并登记该节"核验是闭式代闭式、没有独立积分"与"表里没有重力
（`k=1e2`那行真值是表中值的2.40倍）"


### 接触第一片与第二片：锚点布局 + 法向罚函数项（决策0050）

- **`physics_engine.contact`（实验档，力学域）**：0050的形制第一次成为代码。
  `build_contact_layout`按**声明的接触对**分槽造**定长**布局——
  正向的门钉住那条承重性质：**活动集变化时布局指纹一个字节都不动**
  （0043列的三条路都会让它变，而变了跨步守恒断言当场全废）
- 槽位形制：活动标志 + 锚点(3) + 粘/滑判别，**全部`is_history=True`**（0033保守方向）。
  出生态全零且`REGIME_SEPARATED`恰好是0.0——**没碰过的接触没有历史**，
  不需要额外初始化步骤（需要初始化的历史字段迟早有人忘了初始化）
- **上一片登记的那条洞关掉了**：`ContactLayout.assert_matches_context`比对质量表与
  节点块。门里先钉住"`resolve_node_count`那里确实判不出来"再验这里判得出来——
  否则将来那条门会在验一件已经不成立的事
- **`PenaltyNormalContact`**：半空间罚函数法向接触`U = ½kg²`（仅`g<0`），
  满足四方法协议 + `node_index_bound`。**实测跨k六个数量级（1e2—1e8）
  法向力相对偏差恒为0.00e+00、穿透逐档等于N/k、牛顿1步收敛**——
  这就是模型该有的样子：**位置有O(1/k)误差，力没有误差**，判据据此只判力与阈值
- **两条已知性质钉成正向的门**：分离态切线刚度**奇异**（`g>0`时Hessian零贡献，
  悬空自由度没有刚度，牛顿迈不出第一步）。**这条要被钉住，否则将来有人把它当bug
  "顺手"正则化掉**——那等于给分离的接触也加刚度，即隔空吸引的假物理
- **四条注错实测**：去掉`g<0`红2、力符号反红8、能量漏½红8、
  **Hessian当轴对齐只红1**。最后一条是这批门的盲区：本文件除斜法向那条外
  法向全是`+z`，而轴对齐时`n⊗n`退化成单位阵——**一个把法向硬编码成轴对齐的实现
  能从几乎全部门底下走过去**。盲区已写进测试文件
- `contact`登记进力学域环（完备性门按设计红过：新模块不登记即红）

### 节点块的权威改成布局——**那条洞的两半，第三次才修全**

- **`StateLayout.node_dof_count`**：布局显式声明节点块占前多少个标量。
  声明了就必须与上下文的质量表一致，不一致即失败关闭。
  `build_contact_layout`一律声明；老形制（不声明）保持"上下文说了算"，
  它对纯节点布局永远正确
- **为什么要修第三次**：同一条洞（"节点数有两个来源而没有一个是权威的"）
  修过两次都只修了一半。第一次修能量项，第二次修桥"不崩"，
  **而实测确认桥仍会把重力−9810放到锚点槽的前三格上**——
  因为两处都还在信上下文的质量表。**病根不是某处实现错了，
  是节点数有两个来源**
- **`node_dof_count`不进`to_document()`**，与`is_dimensionless`逐字同理：
  打包契约的内容地址描述的是"次序与宽度"，边界不改变哪个数落在哪一格。
  **实测既有布局的指纹逐字节不变**（0001三前提第三条）
- **一条门按设计红了并被删掉**：`test_context_that_disagrees...`此前断言"那里确实
  判不出来"，并在注释里写明"洞不在了就该删——门不该验一件已经不成立的事"。
  洞关上后它红了，于是照它自己说的删了。**这就是那句话该兑现的样子**

### 能量→加速度的桥补上节点块边界——**瞬态接触当场就通了**

- **同一个假定的第二处实现**：`EnergyRegistry.acceleration`按`node_masses_kg[index // 3]`
  取质量，遇到含接触锚点槽的布局抛裸`IndexError`（实测）。
  能量项那一侧本轮早些时候修过，**但桥没跟上——修了一处不等于修了那件事**
- 修法不是"给锚点编个质量"，是**节点块之外一律返回0.0**：
  锚点是历史、由return-map推进，**把它当成有质量的点去积分等于给摩擦锚点编造惯性**
- **两条纪律各守一半**：能量项不许碰节点块之外（`node_index_bound`），
  桥自己也挡一道。门里专门构造了一个"往锚点写梯度"的违规项来验桥那一半——
  **别把正确性寄托在"梯度恰好是零"上**
- **由此实测确认一件事，并更正上一轮的判断**：此前说"恢复系数要隐式积分族"，
  **不对**。DEM就是罚接触+显式积分，而本仓三个显式积分器与这条桥都在。
  修完桥直接跑通自由落体反弹：无阻尼罚接触+velocity Verlet（辛），
  **三档刚度/步长（1e3/5e-6、1e4/2e-6、1e5/5e-7）反弹峰值对理论e=1
  偏差1.9e-06—5.3e-06**。C档第9条缺的只是一个dashpot，不是一个子系统

### 接触第六片：预测-修正的迭代（曲面法向）

- **`advance_contact_quasistatic`收`max_passes`与可调用的`normal`**：法向可以是
  固定元组，也可以是"给定当前状态返回当前法向"的可调用对象
- **加迭代的依据是实测出来的，不是推的**：法向固定时一趟精确（理想塑性的
  return-map修正后屈服条件恰好成立）；**法向随位形转时一趟差得远**——
  两个固定球夹一个受横载的球，一趟之后屈服残差2.32 N，
  **每趟压缩因子恒为1.427**，跨四次倍增（1→2→4→8→16趟）逐格吻合
- **两处设计缺陷由"用起来"暴露，都改了**：
  ① `ContactStep`此前没有屈服残差出口，而`tangential_force_n`是**投影后**的力、
  滑移时按构造恒等于μN——**拿它判收敛永远得0**，起草时正是这样量了个寂寞。
  **观测不到的收敛判据不是判据**，故加公开字段`yield_excess_n`；
  ② 步进器第一版**自己造了一个过原点的半空间法向项**，球-球接触下球心离原点27mm
  被判成分离、法向力为0、锚点一动不动——症状是"迭代不收敛"，
  病根是**步进器越权拥有了一个它不该拥有的东西**。改成由调用方给`normal_force_of`
- **`slip_increment_mm`改成整步总滑移**（此前取最后一趟的修正量，多趟下会把前几趟
  滑掉的距离丢掉，而那正是这一步的不可逆位移）
- **默认值刻意保守（1趟、0容差）**：老调用点全是固定法向、一趟本来就精确，
  **默认值不该替既有调用方改变行为**
- **一处边界约定登记**：滑移修正后状态**恰好落在锥面上**，而`|T| ≤ μN`把边界判成粘，
  所以`regime`标签在滑移步后**依赖趟数预算**（1趟报滑、2趟报粘）。
  锚点与滑移量**逐位相同**，力差8个ulp。**是约定不是缺陷**，但改默认趟数会改语义

### 接触第五片：多体接触与三球金字塔

- **`PenaltySphereContact`**：球-球罚函数法向接触。此前接触都发生在节点与**固定**
  半空间之间，而固定面不参与自由度；本项**两端都是自由度**，
  于是Hessian第一次有**跨节点耦合块**——**那才是多体接触与单体接触的真正分界**，
  且配了一条正向门（耦合块必须等于对角块的相反数，即牛顿第三定律在Hessian上的形状）
- 法向随位形转，故Hessian多一块**几何刚度**`(k·g/L)·(I−d⊗d)`，接触时为负（横向softening）。
  与`AxialStretch`压缩时那块同源。**漏掉它梯度照样对、平衡点照样对**，
  只有收敛与稳定性判据会变
- **`PenaltyNormalContact`的半径改成显式必填参数（破坏性，实验档）**。
  数学上把面抬高`r`等价，但那条路**当场把我绊了一次**：球心在高度`R`、面在`z=0`，
  间隙算出来是`+R`，**底球被判成悬空**，切线刚度里冒出零模态、牛顿走不动。
  症状是"求解器坏了"，病根是"调用方以为传的是球、这里当它是质点"。
  **让半径出现在调用点，那个误会就没有发生的余地**；质点写`0.0`
- **案例`three_sphere_pyramid`（C档，交互级）**：`μc = 1/(3√3) = √3/9`，
  **纯几何、与质量重力刚度全无关**。Chrono `utest_DEM_pyramid.cpp`形制的两侧判据：
  `μ/μc=1.00001`撑住、`0.99999`塌，零容差
- **这个案例与斜面案例有一处关键差别，值得单独记**：斜面上法向固定，
  穿透不改变力的分解，力精确到1 ulp。**这里穿透改变接触几何本身**
  （球心距变了、连心线角度跟着变），`T/N`因此带`O(1/k)`偏差。
  **于是多一条收敛阶门**：刚度涨10倍偏差降**10.00/10.50**倍，一阶。
  **这条比"偏差很小"强得多——它证明那个偏差是模型的柔度而不是实现的错误**
- **浮点地板的位置是算出来的不是玄学**：`k=2e9`时穿透约4e-9mm而心距20mm，
  相对量2e-10，双精度只剩约6位——实测偏差比在2e8/2e9两档退化到2.28与0.09，
  故收敛阶只取一阶区间的三档，并把地板写进案例页
- **残差容差按地板估计给（`k·2r·ε`）而不是拍一个数**：地板来自`length−2r`的相消，
  **与质量无关**。起草时固定容差在`m=0.05kg`下永远不收敛（残差1.557e-09），
  而同一容差在`m=1.5kg`下轻松过——**不是求解器变差了，是力小了30倍而地板没动**
- **四条注错实测**：耦合块符号反红5、丢几何刚度红4、间隙忘减半径红4、球间法向反红4
- **C档第7条按半条记，不进位**：Chrono原版所有接触都有摩擦（静不定），
  本仓做的是球-球无摩擦的**静定变体**（才有闭式`μc`）。**定性判据复现了，闭式是本变体的**

### 接触第四片：准静态步进器与**引擎第一次记住历史**

- **`advance_contact_quasistatic`**：弹性预测→求解→return-map修正→**锚点写回状态**。
  前三片里锚点是**输入**（整个过程不动），本片是第一个**改写**它的东西——
  **而改写它就是"历史发生了"**。0033裁"锚点是真历史"之后，这是兑现
- **案例`friction_hysteresis_loop`（C档，本机批级）**：位移控制拖拽的弹塑性滑块。
  力饱和`|T|=μN`**零容差**（构造保证的等式不是数值逼近）；
  整循环耗散对闭式`4μN(u_max−u_y)`三个幅值**实测偏差0.00e+00**
- **那个"偏差为0"带了解释，否则它是个可疑的数**：回线分段线性、梯形积分在线性段上
  精确，且步数取5的倍数让拐点**恰落在步长边界**。用不整除的步数复验：
  K=333给1.35e-05、K=337给1.32e-05、K=999给1.00e-06——**机械是真的，零不是运气**
- **路径相关是本案例最要紧的一条，它验形制不验公式**：同一位置两条路径，
  力之比**恰为1/2**、判别相反（A粘B滑）、锚点各差`0.5u_y`。
  **锚点若能从当前位形算出来，`incline_slide_threshold`的判据照样全绿**——
  它们只看当前位形。**只有这一条能把"历史是不是真的历史"验出来**
- **反向门**：纯弹性往返不许挪锚点、不许产生耗散。只验"滑了会挪"不够——
  一个每步都挪锚点的实现同样能过那条，而它会让纯弹性往返产生虚假耗散
- **四条注错实测**：锚点不写回红4、滑移量不减锥面红2、粘着也挪锚点红3、
  锥面放宽0.1%红1
- **起草时积错了两次区间，都写进案例页与测试**：耗散把处女加载段一起积了（差28%）、
  纯弹性那条回路没闭合（多出`½k_t u₀²`=1.17e-08）。
  **两次都不是物理错，是积错了区间**——但都是靠对不上数才发现的

### 接触第三片：粘着弹簧 + 摩擦return-map + **引擎第一个带接触的案例**

- **`TangentialStickSpring`（能量项）与`coulomb_return_map`（不是能量项）**：
  0050第二节"接触是**半个能量项**"第一次有了具体形状。分界来自0033的调研——
  带粘着的库仑摩擦把切向位移**分解成可逆的"粘"与不可逆的"滑"**，与塑性力学同构：
  **可逆那一半是弹性的、有势函数**（进`EnergyRegistry`、进牛顿切线刚度），
  不可逆那一半耗散、非associative，**写不成任何位置的势**
- **案例`incline_slide_threshold`（C档，交互级）**：`θc = arctan(μs)`。
  实测**五档倾角的`N=Wcosθ`与`T=Wsinθ`偏差≤2.2e-16（1 ulp）**；
  阈值扫描θc−1e-7°粘、θc+1e-7°滑。**三个自由度全部放开，走完
  `state`→`energies`→`solve`整条路**——这是那张分层表里**第一条带接触的整条路案例**
- **起草时先撞了一次墙，写进案例页第一节**：最初只放开`z`，实测法向力给出
  `W/cosθ`而不是`W·cosθ`，差`1/cos²θ`。**接触项没错，是装置错了**——
  把节点约束在竖直线上就不再是斜面问题。它同时暴露了当时缺的那一块：
  **切向没有刚度所以不能放开**，而粘着弹簧正是那块本来就该在的刚度
- **判据刻意选与模型参数无关的量**：阈值里没有`k`也没有`m`，
  故"两个罚刚度各改一个数量级"与"质量改100倍"各配一道门——
  **换一组参数判据必须照样过**。判力与阈值，**不判位置**：
  穿透`δ=N/k`是模型自带的，判位置等于把模型参数当成物理
- **四条注错实测**：摩擦锥放宽**2%**就红3条（阈值够锐）、粘着不扣法向红4条、
  摩擦锥漏乘法向力红4条、恒等改写**不红**（门不对无操作误报）
- **C档第8条按半条记**：本案例只做滑动阈值，该条还含无滑滚球（要转动自由度
  参与接触），**分母仍算6/13不算7**——已在案例页与索引写明

### 缺口清册与它的防过期门

- **[plans/07 缺口清册](docs/plans/07_缺口清册_20260805.md)**：全仓缺口第一次有汇总入口。
  实测欠账散在**13份决策记录+5个源码文件**里（全仓53处触发条件、44处待裁、
  16处已知缺口），**而在此之前没有任何一处能回答"我们一共欠了多少"**。
  散着的欠账各自都写得很清楚，**合起来没人看得见**
- **`tools/check_gap_register.py`**（进`accept.py` full）：登记了欠账的决策记录
  必须在清册第六节**作为表格的一行**出现；清册引用的决策必须真的存在
- **这道门的第一版是错的，必红当场打红了它**：第一版认子串，
  把0042那一行整行删掉**照样绿**——因为"0042"在别处以"同0042"出现。
  **这正是0049第六节刚记过的形态**（案例页校验器只认"名字出现在页上"，
  于是`peer_fcl_distance`长期挂在"在建"那句话里）：**门认得"被提到"，
  认不得"被登记"**。判据改成只认表格首个单元格——
  "被提到"进不了第一格。**一道没被注错验过的门，你不知道它在验什么**

### 接触轨道：节点块边界（0050落地前置，**修了一个静默缺陷**）

- **`energies.py`三处用`len(state.vector) // 3`数节点，假定整条状态向量都是节点坐标。**
  0050要把接触锚点挂在节点块之后，这个假定当场不成立。
  而`EnergyContext`第52行的注释从一开始就写着"**节点数由布局定，这里只存值**"——
  **契约写对了，实现从来没照做**
- **实测后果是静默的**（两节点布局挂一个锚点槽、质量表恰好三项时）：
  重力能29.43→98.1（**3.3倍**）；**梯度长度仍等于布局长度，求解器察觉不到**；
  **重力出现在锚点自由度上**——求解器会真的把摩擦锚点当成有质量的点往下拉
- **新增`resolve_node_count(state, context)`**：节点数的唯一口径来自上下文质量表，
  与向量长度交叉校验、失败关闭。节点块是向量的**前缀**，其后是辅助自由度，
  **能量项一律不许碰**
- **`EnergyTerm`协议加`node_index_bound()`（破坏性，实验档）**：本项索引到的最大
  节点号+1，构造期算一次，`EnergyRegistry`装配时比一次。
  **不做逐次索引的边界检查**——0026的画像里装配是唯一的复杂度问题，
  热点上不加逐元素比较。挡的是"边/载荷/模板点名越界节点，于是读写锚点槽，
  长度对得上、求解器不报错、结果是错的"
- `PointLoad._base`的界限从**向量长度**改成**节点数**（同一个洞的另一面）
- 五条必红，**注错实测**：数节点改回`//3`→3条红；拆掉装配期比较→1条红；复原全绿。
  含一条反向门（合法索引不许被误伤）与一条把历史错值98.1钉死的门
- **如实登记一条没关掉的**：质量表本身与布局不一致时仍判不出来
  （本函数看不见"布局里哪一段是节点块"）。**触发条件**：锚点布局构造器落地时
  把边界带上来比第二次

### 接触轨道：形制裁决（决策0050）

- **0033那条挂了很久的状态布局裁了，而且三条路一条都没选**。0043第3.3节列的
  三条（每种活动集一份布局／可变长段／锚点留状态外）**分母都取错了**：
  它们假定锚点数由**活动**接触集决定。而`FinalizedScene.contact_pairs`（0045）
  是**声明期定死、冻结、有序**的——**按声明的对分槽，布局就是定长的**：
  指纹跨步不变、`fingerprint()`语义不用改（0019的承重条款不动）、
  跨步守恒量断言继续成立，活动与否变成**值**而不是一次布局变更
- **0043看不见这条，因为它看不见**：0043是Track R、0045是Track S，
  **同一波里并行**。这是0048那类并行盲区的一个新形态——
  **不是两条轨道改同一个文件，是一条轨道的结论建立在"某样东西不存在"之上，
  而另一条正在把它造出来**。分槽规则挡字节冲突，**挡不住过期的前提**
- **接触模型：罚函数式法向 + return-map摩擦，不走LCP**。法向写成能量项即插进
  既有`EnergyTerm`四方法协议——**于是第一条带接触的案例天然走完
  `state`→`energies`→`solve`整条路**（今天走完整条路的3条全是没接触的梁）。
  不走LCP的理由：它没有能量函数、要全新求解器、
  **等于让接触绕开既有的全部门**；且C档第7条来自Chrono的DEM（本就是罚函数式），
  判据是行为判据不是算法判据
- **代价写在前面**：穿透不为零（是模型不是缺陷，刚度必须是声明的输入）；
  **摩擦不是势能**（耗散、非associative），所以接触是"**半个能量项**"——
  这条分界要进spec/12。`solve_equilibrium`在活动集变化处不可微、线搜索会失效，
  **现在登记不假装它不存在**
- 判据阶梯先于代码定：斜面阈值`θc=arctan(μs)`（阈值两侧行为定性相反）、
  三球金字塔双向、**以及一条验形制的**——同一终态不同加载路径必须给出不同锚点，
  **若锚点被做成"可从位形导出"，前两条照样能过而这条必红**

### 冲突标记门补齐提交时那一层（决策0049第二节续）

- **判据搬进`tools/check_conflict_markers.py`**（只用标准库、走裸`python`），
  `pre-commit`与`accept.py` full**两处都挂**：钩子可能没装（那次事故里就没装），
  批末这道跑不掉。测试文件import它并补必红——**一个判据两个时机，
  不许有第二份实现**，两份实现迟早分叉而分叉那天绿的那份会被相信
- `.git/hooks/pre-commit`**实际安装了**（此前只有配置没有钩子）；
  `pre-commit>=4`进dev依赖

## 0.5.0 — 2026-08-05

**这一版收的是2026-08-05一整天的产出**：T0收口起、六条并行轨道止，
三个物理域、905条测试、19个案例。

**但分母要如实并排报**：同行C档标准案例6/13（发版后逐条重数纠正，此前报7），
**用户六场景的端到端0/6**（决策0048第二节）。**版本号涨不代表后一个数动了。**

并行开发期的分槽（plans/02第三节规避规则3）：每条轨道只在自己的小节内append。
当初写的是"合并冲突面为零"——**实测不成立**：分槽挡得住互改文本，
**挡不住同点插入**，本页因此曾带着三处嵌套的未解冲突进主干（见T13与决策0049）。

### T0 收口（缺陷清零+面清册+文档对账）

- 面清册一次登记齐四个新面（`physics_scene_collision_events`、
  `engine_perf_baseline`、`engine_oracle_manifest`、`engine_behavior_baseline`），
  其中`physics_scene_collision_events`补登记了自0.3.0起就在落盘却未登记的形制
  （本仓自己违反轴1规则1近两个版本）
- `pyproject.toml`登记与负载三级对应的pytest marker（`batch`/`serverclass`）
- **失败关闭补齐（破坏性：此前被放行的非法场景现在会被拒）**：
  `MeshAsset`的`units`/`usage`/`convexity`与`CollisionShape.direction`
  运行时校验取值；场景文件缺`direction`不再放行；重复`body_id`与
  未知`allowed_pairs`成员在**加载期**失败关闭（此前推迟到查询构造期才炸）
- `pe-scene`退出码修复：查询构造异常纳入捕获，非法场景两个子命令一律退出码2
  （此前`check-collisions`抛栈回溯并以1退出，而1的语义是"有候选"）
- `collision_events.json`补`facet_version`字段（**破坏字节**，实验档minor内声明）
- `examples/collision_preview_cell.scene.json`的网格AABB改为资产真值
  （此前声明的包络在z轴上与资产完全不相交，`direction: "envelope"`不成立）

### T1 性能门

- **spec/13的预算第一次成为会红的门**（决策0018）：`tests/perf/test_budgets.py`
  守三条**确定性量**——源码字节（wheel体积的代理）、eager import模块数
  （冷启动的结构代理）、运行时依赖数（永远为0）。**墙钟不进门**：走
  `tools/bench.py`产报告（照Drake形制明写"本报告不是回退测试"），
  理由是共享runner墙钟CV=2.66%、2%阈值假阳率45%
- `benchmarks/engine_budgets.baseline.json`（`engine_perf_baseline`面）：
  预算与实测口径入册；**wheel 30KB与源码字节上限都改成"抬可以但必须记账"的
  历史账**——现行上限必须等于历史末行且每行带决策记录，悄悄抬过不去
- **`tools/accept.py`四处修**：双档真分家（此前quick与full命令集逐字相同，
  30/120退化成一件事，现按spec/13负载三级用marker分）；轴6规则4资源资格
  （负载不合格→计时`NOT_EVALUATED`，功能结论一个字不改）；轴6规则5执行树哈希
  （金标件与被测输入都在树内）；**超时改杀整棵后代进程树**
  （`subprocess.run(timeout=)`只终止直接子进程，实测确认老写法下孙进程存活）
- **空档位语义**：申报过的档位选不中测试（pytest退出码5）视同通过并在回执与
  stdout如实记名；**交互级绝不许为空**——那说明marker笔误让整档静默不跑
- 未跟踪符号链接不再让验收器崩（指向目录时`IsADirectoryError`）：
  链接的内容按其**目标字符串**入指纹，不跟随读目标字节
- governance元测试 11条 → 21条

### T2 几何

- **`physics_engine.geometry`（实验档）**：四种解析原语的体积、质心、
  绕质心惯量张量（kg·mm²，参考点写进字段名），与spec/11规则2点名却至今
  零实现的rounded-core SDF `φ = φ_core − r_f`（首个实现）。惯量是力学域
  接进来第一个撞上的缺口
- `mass_properties`收密度或质量**恰一个**；带法兰的`FiniteCylinder`与
  `MeshAsset`**失败关闭**——声明里没有法兰轴向尺寸，AABB是包络不是质量分布，
  缺口登记进spec/11第二之二节
- 33条门：解析闭式解 + 圆角盒三条退化极限 + 以SDF为独立oracle的数值求积 +
  平行轴/对称正定/主不变量三条自洽门；**十二条破法逐条实测必须红**，
  其中"圆角盒面板平移项"三条解析退化极限一条都抓不住、只有数值求积抓住
  （决策0022第五节）

### T3 oracle与案例

- **`validation/`落地**：同行库对比实验的独立目录与独立venv，身份边界写死
  （**永不进`dependencies`、永不被`src/physics_engine` import**）；建环境脚本、
  钉死版本的依赖清单、逐库许可证结论（决策0025）。这是0015第二条
  "把别人的库下载下来进行对比实验"的兑现
- **案例`peer_fcl_distance`**：与python-fcl 0.7.0.11逐点对拍**2700组**固定种子
  输入（球/球-胶囊/胶囊-胶囊 × 分离/擦边/穿透），最大绝对偏差**2.27e-13mm**、
  接触判据分歧**0**。产物走run package，manifest带`peer_library`
  （名称/版本/安装方式/许可证/随轮二进制SHA-256）与`runtime_environment`
- **对拍抓到同行两处真实缺陷**，并用`fractions.Fraction`精确算术复核：
  FCL的`distance(enable_signed_distance=True)`在**分离**构型上可错到45.6%相对误差；
  `collide().penetration_depth`在胶囊-胶囊上与**FCL自己的`distance()`**矛盾。
  两处均记录进产物但**不作判据**（spec/08规则1：同行的数不是真值，是另一个证人）
- `tests/cases/test_peer_fcl_distance.py`：conformance门+三条注错必须红例
  （四元数次序/漏减半径/胶囊半长当全长），负载级`batch`；**同行库缺席时skip**，
  accept不因缺库而红。**`batch`档从此不再是空档位**（4条真测试）
- `.gitignore`补`.venv`（无斜杠）：并行开发的worktree里`.venv`是符号链接，
  带斜杠的规则只匹配目录，此前它一直显示为未跟踪
- **`physics_engine.oracles`（实验档）**：轴7规则2的清单面参考实现——生成器身份
  （含**生成器脚本自身的SHA-256**）、逐条expected与逐条tolerances、数组双哈希
  （raw级+语义级）、清单自指哈希；严格加载失败关闭。三条本仓加强：每个量的容差
  必须带非空`reason`（判据表第三列从文档义务变成**加载条件**）；非数值量的容差
  必须为零；`regenerated_by`必须指向**存在的**`docs/decisions/`文件——轴7规则5
  第一次有了执行体
- **首批四条A档案例**（`cases/`，决策0020）：`segment_distance`（球/胶囊解析距离
  与穿透，abs 1e-12mm，五条退化分支+一条一般路径各一条手算用例，**分支覆盖经
  opcode级trace实测**且未改动源码一字节）、`rotated_aabb`（八角点枚举对Arvo
  中心-半边长闭式解，abs 1e-9mm；四类典型错重算得31.0/18.5/40.0/65.3mm偏差）、
  `broadphase_superset`（`separation<0 ⟹ AABB相交`，120对固定种子语料，反例严格为0）、
  `mesh_asset_integrity`（资产SHA+包盒保守性双零容差，**两条必红语料常驻**）
- **`tests/test_narrow_phase.py`的判据搬进清单**（轴7规则3：不得在测试里复述
  oracle公式）；原文件保留可信度分级与诚实降级两条接口诚实性判据
- **仓内自带合成网格资产**（Chrono形制）：284字节二进制STL+生成脚本一并入库。
  0017那条真实缺陷（声明SHA逐字节吻合而声明包盒与真值在z轴完全不相交）此前
  **引擎没有任何门能发现**，现在有了
- **`tools/check_case_pages.py`**：案例页六必填字段缺一即红，`accept.py` full档的
  可选槽位就此上膛；外加案例目录必须被`cases/README.md`索引、`oracle.json`必须过
  严格加载器、清单`case_id`与`load_tier`必须在页里出现三条结构校验。
  六必填按**标题正文**匹配、忽略中文序号——案例可以有第七第八节，
  逼它为凑编号删掉最贵的发现是把形式看得比证据重
- `accept.py`的ruff扫描范围加上`cases/`

### T4 力学地基

- **`docs/spec/12_力学内核与时间推进_v0.md`**：T4的规范面前置就位
  （plans/02第六节第2项结案）。能量项四方法协议与注册表分发、状态三层与
  边界（形状/材料/位姿都不是状态）、准静态与瞬态两路并列、积分器五项出生
  声明、0016的条款化、**有限差分门验不了物理**（附1600倍活标本）与解析
  oracle的两道门、守恒量三种写法及各自的假通过堵法、搬迁五道切换门、
  B档四条判据、已知不成立的五字段登记形制
- **登记三处与既有裁决不一致**（spec/12第11.2节）：①0016设想的"纯Python↔
  NumPy"两实现在WDS并不存在，纯Python那侧要新写——每块内核是"搬一条+
  写一条+建对拍"（0016与plans/02 T5已据此修订）；②"无状态纯函数"只对
  能量层成立，接触摩擦层有真状态，T5搬到接触前要再裁一次；③对拍档位不是
  二选一，能逐字节就必须逐字节（0016已确认此为原意）
- **B档判据两处修正**（实测复核）：显式Euler误差恰为`−a·T·h/2`，与半隐式
  大小相同符号相反——按`|误差|`写的判据分不开两个积分器；`cos(ωT)`收敛
  比值不得写死为4（渐近值，粗档实测3.9985，写死会让正确实现红）
- **`physics_engine.state`与`physics_engine.integrate`（实验档，决策0019）**：
  **引擎第一次能推进物理状态**。状态层是显式数组不是对象图，
  `StateLayout.fingerprint()`把**打包次序做成内容地址**——次序反了而
  `dof_count`与向量长度都不变，只看长度的检查发现不了，指纹发现得了。
  三个积分器（显式Euler／半隐式辛Euler／velocity Verlet）各带spec/12第4.2节的
  **五项出生声明**（适用域／形式阶与实测阶分开／稳定性／耗散记账／失败阶梯），
  缺一构造即拒；三者`production_ready`全为`False`并有门守着
- **0016甲案第一次真正落地**：积分公式**只写一遍**、按`VectorOps`求值，
  `PurePythonOps`与`NumpyOps`执行同一串运算同一个次序——**逐位相同是构造保证的**。
  按spec/12第5.3节"能逐字节就必须逐字节"，对拍档位是**逐字节**而非容差。
  实测600次积分零差异；破法验红：加速档改`float32`→红，
  改共享公式源的求和分组→**不红**（两个后端一起变了，正是"一份公式源"的直接结果，
  边界已写进案例页）
- **NumPy进`[project.optional-dependencies]`的`accel`档**，`dependencies`仍为`[]`；
  eager import模块数**97不变**（NumPy只在构造加速档时import）
- **两条B档案例**：`ballistic_free_flight`（9条oracle）与`harmonic_oscillator`
  （3条oracle）。四条判据与spec/12写规范时预先记下的实测值**逐位吻合**；
  "同幅反号"另立一道独立的门
- **`source_bytes`上限147456→229376并记账**（决策0019）：T4入仓后占用99.2%、
  余量1224字节，门虽未红但余量已无意义。上限的定性再次确认——
  不是"引擎必须保持小"，是"增长必须是一次被记录的决定"

### T5 搬迁（物理搬进来）

- **`physics_engine.energies`（实验档，决策0024）**：搬迁轨第一块——
  能量项**四方法协议**（`energy`/`gradient`/`hessian`/`quantities`，形制采WDS
  `model/energies.py:43`）、`EnergyContext`上下文层、`EnergyRegistry`
  （显式启用、**求和次序即声明次序**——浮点加法不结合，次序变了总能量末位就变）、
  两个能量项（`UniformGravity`、`AxialStretch`）、以及能量梯度→积分器的`acceleration()`接缝
- **`quantities`融合路径的能量值与单独调`energy`逐字节相同**（spec/12第3.1节的
  承重条款）有门守着；零长度边失败关闭（方向未定义、能量在此不可微，报错而不是猜）
- **案例`two_body_spring`**：拉伸能与重力能的闭式值、两体振动角频率
  `ω = sqrt(k/μ·1000)`、质心不动三层判据
- **本块最重要的产出是一个活标本**：`acceleration()`接缝曾漏掉mm与m的换算
  （`N/kg = m/s²`是米制，状态是mm制），加速度小了1000倍，而**有限差分门全绿**——
  换算因子不在能量里，FD看不见它。抓住它的是解析频率门。
  spec/12第6.1节称"有限差分验不了物理"是该页最锋利的一条并附了WDS的1600倍标本，
  **现在引擎自己也有了一个**。验红：拿掉`MM_PER_M`→频率门红、FD门照绿
- **容差是算出来的不是放宽到能过为止**：拉伸能第一版`rel=1e-15`红了，
  最终定`rel=2e-13`——因为内核经`|x_j−x_i|−l0`算伸长，`10.05−10.0`是相消，
  相对误差被放大约`L/δ=200`倍。理由栏写明那个200是相消放大因子

- **`LinearBending`与`physics_engine.solve`（决策0027）**：小挠度Euler-Bernoulli
  弯曲能（位置的二次型，梯度线性、Hessian常量、模板带仿射偏置）+ 准静态求解
  （牛顿+回溯线搜索+稠密LU）。**准静态与瞬态是两条并列的路**，引擎至今只有后者
- **案例`cantilever_self_weight`：引擎第一次把教科书闭式解算对**——
  `δ_tip = qL⁴/(8EI)`，四档加密的收敛比实测**全为4.000**（二阶），
  牛顿一步收敛、零回溯（二次能量该有的行为）
- **两处诊断，症状相同而病根不同**：①一阶收敛的真因是**求积权重**不是边界条件阶次——
  症状与WDS登记的"固支只有一阶"一模一样，但换二阶镜像只从1.949动到1.987，
  给固支端模板半权才变成4.000；②固支模板`(−2,2)`在直链上给出`2(x₁−x₀)`≠0，
  **把轴向也罚了**，悬臂案例侥幸没受影响只因轴向自由度全被钉住——
  这是"案例绿了不代表实现对"的标本，修法是给模板加仿射偏置
- **第二个单位bug，比第一个隐蔽因为它被抵消了**：`UniformGravity`的能量
  `m·g·y`是kg·mm²/s²，与拉伸能不同量纲；而`two_body_spring`的重力能判据当时**是绿的**，
  因为测试把g除以1000传进去、**两个错误互相抵消**。补上的门是自由落体
  （加速度必须恰好等于g且与质量无关）与量纲门，两条都验过红
- 求解器三条申报（spec/12第4.3节）：容差是**绝对**的、**无步长自适应**、
  **线搜索回溯次数进结果**（步长二分是抢救不是收敛证据）；奇异矩阵失败关闭并指出欠约束

- **性能画像入册**（决策0026、research/06）：引擎自身的标度指数、热点排序、
  两后端实测比。**引擎的p≈1.05与WDS的0.59方向相反、两者都对、处方也相反**——
  WDS次线性是固定派发成本主导，引擎线性是**没有可摊薄的固定成本**；
  spec/12第8.1节与plans/02第5.2节已补口径行，两套数不可混算
- **按热点证据做的第一批优化**（决策0028，spec/13义务1的顺序：先画像后动代码）：
  ①`quantities`**真正融合**——画像发现三个能量项都在分别调energy/gradient/hessian，
  边核求值恰为边数的1.0×/2.0×/3.0×，而spec/12第3.1节把融合写成承重条款、
  理由正是"分开调会重做三遍"。**这是补规范义务不是优化**。实测梯度档1.30×；
  ②Hessian装配**稀疏化**——结构非零只占稠密的0.5852%、稠密多做170.9倍工作、
  标度指数被推到2.02；求解器改走稀疏路，实测**16.4×**（75.60ms→4.60ms），
  公开的稠密`hessian()`一字未动
- **逐字节对拍，而且第一版没通过**：优化前指纹`42a8b75f…`，第一版融合后变成
  `9b129860…`。差异定位到把`k*(d_a·d_b)`写成`(k*d_a)*d_b`——**浮点乘法不结合**。
  改回逐字一致后指纹复原。**spec/12第3.1节那条逐字节门在第一次被真考验时就抓住了
  一个末位漂移**，而它在任何"数值上接近"的判据下都会溜过去
- 新增确定性整数门：边核求值次数必须恰等于边数，融合被做回去当场红
- **画像说不做的没做**：不给积分器算术加后端（Amdahl上限1.146×，实测NumPy在小规模
  反而慢25—29%）；不把公开`Matrix`类型改稀疏（轴1面变更，要走决策与消费方评审）；
  不省`State`重建与`isfinite`全扫（只值1.09×，那是拿失败关闭换速度）

### 资产圈（modelgen）

- **新增`physics_engine.modelgen`（决策0032）**：spec/11规则2第二类的首个实现——
  带盘／导轮／骨架三个"参数→形状"纯函数。**除特征长度外全是无量纲比值**
  （case2 `robot_links` §J1形制），产物带生成器身份+版本+整次调用的参数表
- **带法兰带盘按决策0034办**：不动形状词汇，在生成器层用既有原语**组合表达**
  （筒+两片法兰盘）——分解是**精确的**：三件互不重叠、体积可加、惯量按平行轴合并；
  产出的圆柱`flange_outer_radius_mm`恒为`None`
- 新增案例`case/generator_determinism`（A档／interactive，5条oracle）：
  同参数逐字节相同（**含换`PYTHONHASHSEED`起子进程**——进程内比两次抓不到哈希序，
  已用变异实证）、22条一位扰动全改变声明、特征长度乘2时40个长度**逐位**翻倍、
  产出的形对圆柱／胶囊教科书闭式 rel 1e-15（实测最大偏差2.34e-16）
- **15条变异逐条验红，0条漏网**；容差收到2e-16即红——容差是算出来的（解析界3.3e-16的3倍）
- spec/11补**重开材料**：WDS的`CylinderSurface`**一直有**法兰的轴向与径向两个尺寸，
  所以"法兰没有轴向尺寸"是**引擎侧**的缺口不是消费方侧的；0034触发时两条候选都不必从零想
- 预算：`source_bytes` 182570 → 208007，上限229376→**294912并记账**（第4次）

### 欠账清理（决策0030）

- **`cases/axial_stretch_hessian`（新案例，B档）**：闭合0024第六节登记的缺口——
  拉伸项的Hessian此前只有有限差分背书。金标走**两条精确有理路径**
  （`Fraction`二阶前向jet照定义求导 + 手推闭式`k·[δ_ab(1−l0/L) + l0·Δ_aΔ_b/L³]`），
  两条**逐位相等**，生成器不import`energies`
- **容差是算出来的，而且结论与直觉相反**：0024那个`L/δ`相消放大因子**只进梯度**
  （`irrational_length`实测100.7u）**不进Hessian**——横向系数`k·δ/L`的绝对误差里
  δ约掉了，实测≤1.24u·k即使`L/δ=25600`。附带记入Sterbenz引理：`fl(L)`精确时
  δ也精确，相消根本没发生，所以那一项只在`fl(L)`不精确时才该出现
- **一条把spec/12第6.1节做成实验的门**：同一个改错的静止长度，
  **有限差分全绿、解析金标红**
- **`physics_engine.sensors`（新，实验档）**：spec/10第四节那条**原创法条**终于有门——
  声明一个读全场状态的传感器，校验必须拒（三种写法各堵一条）。
  **只做声明与校验，没有`read`**（接口未冻结），并有门断言`SensorDeclaration`上不存在`read`。
  验红带**反证**：把校验换成`if False`，同样两条声明当场变绿
- **`solve_equilibrium`的延迟预算入册**（spec/12第8.2节第1条，0027漏登）：两行进
  `wall_clock_budgets`，**负载级随规模变**——稠密LU让成本按自由自由度约2.6次方长，
  交互级边界约200个自由自由度、本机批级约1536个。墙钟仍不进门
- **`residual_tol_n`取消默认值，改为必填**：绝对残差的可达地板随规模上升
  （10段2.4e-10 N、160段5.6e-8 N），旧默认1e-9在40段以上够不到、会跑满迭代不收敛。
  **一个"看起来能用、一放大就不收敛"的默认值比没有默认值糟糕得多**——
  它把必须由调用方回答的问题伪装成库的实现细节。配一条门守着"不许有默认值"
- **文档对账**：README阶段3"未开始"→"进行中"（核实结论：spec/90对、README没跟上，
  且FTS那半边确实没动所以不是"完成"）；README首屏加能力边界段；
  plans/01审核表B7/B8/B9打钩并注依据；`__init__.py`安装示例0.2.0→0.4.0

### 光学域与域间隔离（决策0031、0035）

- **光学域进引擎**（用户2026-08-05裁决）：`physics_engine.optics`子包——
  圆孔艾里斑闭式解（`J1`纯标准库实现，**分割点是实测出来的**：11/12/13/14
  对应2.60e-12/**8.27e-13**/1.24e-12/4.97e-12，大宗量用Hankel展开的**最优截断**）、
  FTS无切趾仪器线型与半宽`1.2067091288/(2·OPD_max)`、Norton-Beer三组切趾窗与通量代价。
  纯Python零依赖，**本块不做加速档**——标量闭式没有数组可向量化，
  且research/06的p≈1.05说明没有固定成本可摊薄
- **spec/01的两条域边界第一次有执行体**：spec/15 + `tests/governance/test_domain_isolation.py`
  五条门（域间零依赖／域只向基座伸手／域不得import耦合／基座不依赖域／登记完备性）
  + 动态import禁用。**AST静态扫,连函数体内的延迟import也抓**；逐条实注入验红，
  并有两条**反向**测试断言门不过严
- **门上线当天就抓到两处它上线前就存在的错**（决策0035）：
  ①spec/01的环图把资产圈画在物理域圈**之上**，而同页写着"物理域从材料记录取参数"——
  那是一条向上的边。**图错文本对**，资产圈下移；
  ②`sensors`被我登记成基座接口，门当场红——它import`state`（力学），
  "基座依赖物理域"方向反了。**不是门过严，是把愿望当成了事实**，改归力学域
- `state`是否升进基座**已复核并否决**：今天没有非力学代码需要它，
  升格就是为想象中的需求预支通用性。本门的好性质是**升格无法悄悄发生**——
  光学一旦import它当场红，只能显式走决策
- 案例`scalar_diffraction_airy`与`fts_instrument_line_shape`（各2条oracle）；
  单位边界逐条钉死（σ↔k、角↔空间频率、半径↔直径、归一化↔非归一化sinc、
  单侧↔全程OPD、mm↔m），**每处写成有名字的常量**
- AGENTS.md补一条worktree陷阱：跑`accept.py`必须导出`PYTHONPATH`，
  否则测的是主仓代码而你以为测的是自己的

- **`energies.DiscreteElasticBending`（决策0029）**：几何精确弯曲（DER `κ=2·tan(θ/2)`），
  四方法+`hessian_entries`齐备，**梯度与Hessian全解析**。`LinearBending`不变——
  两者是**适用域关系不是新旧关系**。固支顶点的Voronoi长度取`3h/2`：
  这是半格柔度的**静态凝聚推出来的**不是配出来的，三条旁证（与载荷无关、
  1.45h/1.55h/1.65h都退化、自由端权改动不影响）排除了错误归因
- **案例`large_deflection_cantilever`（B档，`batch`）**：大挠度悬臂端点位置对
  Bisshopp-Drucker 1945椭圆积分闭式，二阶收敛实测比**3.9612/4.0512/4.0600**，
  最细档偏差**0.032%**；**小挠度理论在同一载荷下偏差49.1%**——差1519倍。
  金标用Carlson对称形式（零依赖），与WDS的正交求积路线互证到**1.3e-14**
- **本块最重要的发现：物理案例验不了能量表达式**。做验红时只改`curvature_sq`
  没改梯度与Hessian，**整个案例照绿、逐档误差与原来12位有效数字全同**——
  因为求解器只用梯度与Hessian找解，能量值只进线搜索的"降了没有"判断。
  **一个只活在`energy()`里的错误可以从物理案例底下整个溜过去**，
  这是spec/12第6.1节"FD验不了物理"的镜像。补的门是**手算转角**
  （θ=90°给κ²=4、θ=120°给κ²=12），它同时排除弦长近似
- 如实登记的脆点：案例参数是**可行点不是稳健区间**（载荷步数／容差／EA任一改动
  都可能让某档不收敛），根因是`solve.py`无载荷步自适应、无信赖域、
  Hessian非正定时无修正；端部集中力是用1e-15 kg**寄生质量**凑的
  （引擎没有点载荷能量项），已声明并有门

### 调研与证据订正（research/07—09）

- **规范调研充分性审计**（research/07）：十六页逐页审，**充分12页、薄3页、无证据0页**。
  薄的三页：spec/13、spec/15、**spec/10**（第三页不在预期内，是审计自己查出来的——
  它有四处外部锚点但**正本一条都没有**，而它正在往冻结走）。
  一个比"几页薄"更值得先说的结论：**没有一页是纯发明后被当成行业共识的**
- **载重引用抽验：核实属实6条、转述强于来源1条、核实为不成立1条**。
  ①spec/13写MJX"CPU/GPU/TPU无缝切换"，而来源是**重新实现、API有意分歧、
  遇不支持特性抛异常**——"无缝"是我们自己加的，而且它**在拆自己的台**：
  来源的真实形态恰恰更支持该页规则3与规则5。已订正；
  ②spec/15把COMSOL的predefined multiphysics对应到"单体"**不成立**——
  原文里fully coupled与sequential是**求解器层选项**，与interface正交。
  那一格改为**空着并写明无一手锚点**——一个没人该选的型没有实例不损害规则，
  填错的会让人以为那条路被验证过
- **修一个今天就能触发的静默错误**：最长后缀匹配会把`current_density_a_per_m2`
  判成`m2`（**面积**），于是走面积换算`×1e6`，而`A/m² → A/mm²`的正确因子是`×1e-6`——
  **方向反了，差1e12，且不报错**。修法不是补一张更大的换算表（那要为每个复合量纲
  猜因子，正是spec/14第五节禁止的），而是**让最长匹配不许跨过`per_`**。
  于是`converted_to`里那条早已写好的"复合量纲无登记换算即拒"**第一次真正触发**
- **电磁域预研**（research/08）：四类问题的离散手段、门槛清单具体到"缺一个X"
  （缺`StateField`上的`entity_kind`、缺体网格与关联表、缺复数与不定/奇异系统求解路径、
  **缺一个装配面的裁决**——静电静磁有真能量泛函可原样复用能量项协议，
  时谐涡流与全波复数不定、**没有可极小化的实标量能量**）。
  可复用解析基准含矩形波导截止频率（**特征值判据，一条案例同时验物理与验单元选型**）
  与MMS制造解法；TEAM族**是实测型基准，按轴7规则1不得作oracle**，只能当外部对拍源
- **一条方向性判断**：电磁进来撞的**不是**spec/15——那四条门是对依赖图的一般式，
  第三个域只加一行登记。**要改的是spec/12与spec/14。域边界的规则写对了，
  写窄的是内核的形制假设**
- **COMSOL替代性研判**（research/09）：结论见下方"研判结论"

### 求解器提速（决策0039）

- **带状LU落地：链式Hessian的半带宽实测恒为2**（40/160/320段），
  非零只占稠密的1.6%—12.1%。`O(m³)` → `O(m·b²)`，
  **端到端实测 n=320 从16885ms降到359ms，47×**；n=160为11.8×
- **第一版是错的，错法记在决策里**：试图给现有的**高斯-约当**加带宽限制来求
  逐字节相同——高斯-约当上下都消、会把带状结构破坏掉，加限制就得到错答案
  （收敛比从4.0掉到0.01，四条测试当场红）。**带状只对LU+回代成立**
- 因此对拍档位是spec/13义务2的**第二档：数值路径不同→分量声明容差**，
  **如实声明，不硬凑逐字节**。`rel=1e-9`：实测偏差≤9.3e-8mm对3125mm挠度
  是3e-11相对，留约30倍余量
- 新增一条门守**"半带宽不随规模长"**——那是带状值得做的前提，
  前提不成立时收益没了、判据也该重估
- **回答"离散化能不能提速"**：能，但**不是直接提速**——离散化决定每个自由度
  买到多少精度，不是每个自由度花多少时间。高阶单元的价值是同精度下自由度更少，
  **它是间接杠杆且要先有网格与单元库**。直接杠杆在线性求解那一步，
  而`n^2.6`那个稠密LU指纹是0030早就测出来的

### 内核接口（MotionSource／Actuator，决策0038）

- **spec/10冻结前提的最后两个空接口落地**：同`sensors.py`纪律
  **只做声明、校验与纯运动学／时延机械**，`apply`的物理与`command_space`不实现
- **五条插值语义逐条必须由声明者给出，缺一即拒**——WII只给离散样点，
  一个"合理默认"会让两个调用方得到不同的物理而都以为自己对。
  含最易漏的一条：`q`与`−q`是同一旋转但插值走**相反的弧**
- **`delay_s`不是`dt_s`整数倍时默认失败关闭**，唯一放行是显式声明`ceil_to_step`——
  **没有`round`也没有`floor`**：欠时延是spec/10点名的事故方向，两者不对称
- **反证翻出一件本来不知道的事**：关掉整数倍校验并没有变绿——
  另一条"只许多延不许少延"的门独立接住了它。两条门互相独立，现在各有反证
- 一跤记在案：第一版用`u=0.5`验slerp vs nlerp，实测差1.1e-16——
  **半程处两者恰好重合，那条测试永远假绿**。改`u=0.25`才是可见的物理差
- `assert_replayable_for_fingerprint`：轴3规则5的联动门，
  **不可重放来源的运行不得声称复现指纹**；双求值证伪是必要条件不是充分条件，已登记

### 开源多物理栈调研（research/11）

- **十个栈的算法与形制**（DOLFINx／Firedrake／MFEM／deal.II／Elmer／SfePy／
  preCICE／PETSc／Trilinos／scikit-fem），每个给"它最独特的那个设计判断"；
  许可证边界落到"能调用且能复制／能调用不能复制／只能进程外对拍"三列
- **推翻了它自己的假设，这条最值钱**：原以为`p≈1.05`说明我们的装配落后于
  向量化的栈。核实后不成立——**scikit-fem（彻底NumPy向量化）跨三个数量级
  实测也是`p≈1.06`**。`p≈1.05`不是我们独有的病，**是线性装配器该有的样子；
  向量化改常数与可达上限，不改指数**。这直接改掉了"换NumPy装配"那条建议的
  **理由**——不是为了改指数（指数不会变），是为了改常数
- **订正research/03**："符号codegen是唯一在册的两个量级杠杆"是**WDS口径，
  对引擎今天不成立**。要拆三件：代数优化端到端只有**2.8×**（FErari实测，
  同篇摘要另一句是"provides very little speedup"）；结构性优化（和积分解）
  才是真量级但**前提是多项式阶+张量积基，引擎两样都没有**；编掉解释执行
  的Amdahl上限是**1.44×**（research/06实测算术只占1.66%）。
  代价：一次JIT编译要约**17600次装配**才摊平
- **订正决策0039第四节**：把"该抄的装配形制"与"单元库"并成了一件。
  该抄的是"局部块+局部→全局映射表"，**10²—10³行的重构、不要求单元库**，
  且0028的`hessian_entries`已经是它的一半
- **0039之后的下一格不是"更好的直接解"，是"按拓扑分派"**：判据是**图的拓扑维数**——
  链/带状→带状LU（已有）；树→稀疏直接；2D/3D网格→嵌套剖分注定
  `O(N^1.5)`/`O(N²)`（**是下界不是某实现的水平**），那时才轮到迭代解+预条件。
  破零依赖之前还有更便宜的一格：**对称分解**（省一半算术、
  **不选主元因而不破坏带状**、且非正主元本身就是Hessian非正定的检测器）
- **`p≈1.05`让哪些手段结构性无效**：一切"摊薄一次性成本"的手段
  （符号/数值分解分离、JIT缓存）、matrix-free／partial assembly
  （前提是多项式阶+张量积基）、GPU批量、以及WDS那条"更少更大的调用"

### T6 资产（materials）

- **`physics_engine.materials`（实验档）**：材料记录形制的参考实现——一份记录
  聚合多域字段，`applicable_domains`+属性级`domains`标签（一个字段可同时服务
  力学与光学，共享值共享证据）；参数级证据分级（WDS六档与FTS五档的统一映射，
  两处不一致如实登记为待收敛）；单位后缀校验走`identity`；内容寻址自指自校验
  与文件字节锁（轴3规则1/4）；`engine_material_record` 0.1面
- **mm制与米制边界钉死**：记录声明`length_unit`，同一条记录混用两制失败关闭
  （静默1000倍的入口）；跨制取值必须显式`converted_to`，换算表只覆盖纯长度
  量纲，`N/mm²`一类复合量纲无登记换算即拒——不猜因子
- 规范页`docs/spec/14_材料记录与物理性质_v0.md`（v0草案，六条最低红例全验，
  冻结条件三条）；决策记录0023

### T9 光学干涉

- **`physics_engine.optics.interference`**：双光束干涉的闭式解——
  `I = I1+I2+2√(I1I2)|γ|cosΔφ`、两个极值、可见度（走闭式不走极值相减）、
  杨氏双缝（`Δx = λL/d`、傍轴与**精确**光程差、傍轴适用性的可算判据）、
  迈克尔逊（`OPD = 2d`）、条纹级次与相位精度的申报。15个新公开名，
  **只进`optics.__all__`不进包门面**；纯Python零依赖，无数组无FFT
- **部分相干进来了，只做`|γ|`不做`arg(γ)`**：可见度因此拆成
  "强度失配×相干度"两个可分别检验的因子；`arg(γ)`的不做理由与触发条件写在
  模块docstring。**这条同时改写了0031那句"相干一个都没有"的负空间声明**——
  声明过期了要改
- **不相消的暗纹式**`Imin = (√I1−√I2)² + 2√(I1I2)(1−|γ|)`：朴素式
  `I1+I2−2√(I1I2)`在`I1≈I2`时相对误差到6.6%，且**40万对随机近等强里
  12.5%返回负强度**（物理上不存在的东西）；本式两项皆非负。
  代价如实登记：`I1`与`I2`相差远时它反而多一次舍入（3.0/0.75那对低1 ulp）
- **精确光程差走恒等变形**`2xd/(√A+√B)`：定义式`√A−√B`在近轴丢约6位
  （实测两条路差8.3e-11相对）
- **案例`two_beam_interference`**（4条oracle、38条量、B档interactive）：
  金标走三条与内核不同的路（`Fraction`精确相位约化、60位`Decimal`的Taylor
  余弦**不调libm**、精确光程差按定义式）；**能量守恒是一条独立门**——
  条纹的相位平均恒为`I1+I2`，四构型实测残差**恰为0**；
  **与FTS的桥**：ILS首零`1/(2L)`＝整个扫描程`2L`上滑过一个整条纹，不引入FFT
- **相位精度随条纹级次线性退化**被申报并被验：常量
  `PHASE_ACCURACY_RAD_PER_FRINGE_ORDER = 2.1e-15`，协议门在400个OPD上
  对`Fraction`精确参考两个方向都断（实测最坏是申报值的0.43倍）
- **11条错法注入实测全红**（含"L与d在两处一起写反、内部自洽"那条——
  只有精确几何这个外部证人拆得穿）；**唯一没有门能抓的错法**
  （调用方把度数传进`phase_difference_rad`参数位）单列一条测试钉住
- 决策记录0044；`docs/spec/15`模块表与`cases/README.md`各补一行

### T10 场景装配（Scene）

- **`scene.py`长出装配容器**（spec/10第一节的执行体，决策0045）：
  `SceneAssembly`（五个`declare_*`只登记）→`finalize()`（一次性统一校验）
  →`FinalizedScene`（冻结）。此前`scene.py`只是**文件加载器**，
  spec/10第一节要的`declare_contact_between`+`finalize`一个都没有
  （plans/03第三节A档、plans/04第九节各登记过一次）
- **加一层，不改加载器**：`load_scene`/`Scene`/`SHAPE_KINDS`/
  `register_shape_kind`的行为与字节**一个都没动**（只有`SceneError`的类文档
  改了一句，说明它现在同时服务两条路）。理由是场景文件表达不了运动源与
  驱动器，而给它们定字节形制要先进面清册——真实需求到来之前不预支
- **接触按对显式声明**（spec/10第一节第2条）：`declare_contact_between(a, b)`
  只记账；**没有全体两两自动检测**——那样接触对随体数平方增长，且没有人
  声明过那些对的意图
- **`allowed_pairs`沿用既有语义不另造**（"允许重叠、不报事件"，由
  `collision.BroadPhaseCollisionQuery.check_state`定义）：与声明的接触对
  **必须互斥**，同一对两样都声明当场炸——两条声明会对同一对给出相反的处置
- **位姿只能有一个来源**：一个体要么静态位姿、要么运动源，两样都给即拒。
  本层**不发明**"挂载位姿∘运动源位姿"的复合约定（spec/10第二节没定义
  运动源给的是世界位姿还是挂载系位姿）
- **十四条必红逐条验过**（含用户点名的六条：重复`body_id`、接触对指向不存在
  的体、运动源绑到不存在的体、finalize后仍试图修改（五个入口逐个验）、
  同一对声明两次（正反两序）、`allowed_pairs`与声明矛盾），
  加不可变性门与**跨`PYTHONHASHSEED`子进程**的逐字节确定性门；48条测试
- **`examples/winding_line_scene.py`**：plans/04第九节场景⑥的装配跑通——
  13个体（放线盘3件+两导向轮+张力轮+收线盘3件+4段带材）、10对显式接触
  （含蹭边2对）、4对允许重叠、两族运动源（转动／转动+横动偏移）、
  1个驱动器声明。**不算任何接触**（接触内核不存在，plans/04登记的那面墙）
- 不落任何新字节：**面清册一个字未改**（`assembly_manifest_bytes`是进程内的
  确定性比较基准，不跨边界，与`modelgen.MODELGEN_PROFILE`同一条口径）

### T7 电磁域（electromagnetics）

- **`physics_engine.electromagnetics`（实验档，第三个物理域）**：同轴圆环互感的
  Maxwell闭式`M = μ0·√(r1·r2)·[(2/k−k)·K(k)−(2/k)·E(k)]`，
  `k² = 4·r1·r2/((r1+r2)²+d²)`；`CircularLoop`（半径/轴向位置/匝数/载流）是引擎里
  第一个"回路"几何词汇；完全椭圆积分走AGM，**模k与参数m=k²两种约定各一对函数**
  （本仓两种都在用，混用差10%且不会一眼看出）；决策记录0042
- **方括号不按教科书分组求值**：`(2/k−k)K−(2/k)E`在小k（=远场）下相消放大约`1/k⁴`，
  实测k=1e-4处相对误差**1950%**。改用AGM中间量的恒等形式`K·T/k`（`T = Σ_{n≥1}2ⁿcₙ²`），
  全程无减法，同一区间相对误差7.7e-16。**这不是优化是前提**：照教科书分组写，
  "远场退化到偶极子"那条判据根本做不了
- **AGM本身两个坑，都是实测撞出来的**：①循环不终止时`cₙ`卡在一个ulp而`2ⁿ`还在翻倍，
  注入`T`上一个绝对地板，危害与k成反比（k=1e-4处5.4e-4、k=1e-8处5.4e12，
  且**迭代上限从40改成64就会把污染扩大1.7e7倍**）；②`c₁=(1−√(1−k²))/2`在小k下相消
  （k=1e-4处2.7e-8）。两条各配一条必红门
- **μ0申报为`measured`不是`benchmark_constant`**（research/08第4.4节的结论第一次成为代码）：
  2019 SI重定义后μ0由`2αh/(ce²)`定出，不再是精确的4π×10⁻⁷；采信CODATA 2022
  1.25663706127e-6 H/m，与旧值的相对偏差1.3203e-10被钉成一条判据
- **mm几何↔米制电磁的边界显式且有门**：`EM_LENGTH_UNIT="m"`、
  `CircularLoop.from_millimetres`是mm进本域的唯一入口、因子只存在一份，
  配mm→m→mm零容差往返 + "同一几何两种声明给出逐位相同的M"两条判据
- 案例`cases/mutual_inductance_coaxial`（**6条oracle、六层独立判据**）：闭式值对
  **Neumann双回路线积分**（周期梯形、全正被积函数改写，与被验实现无共用代码）；
  椭圆积分对Carlson（复用大挠度悬臂生成器那一份）；互易**零容差**；
  远场退化到偶极子**收敛阶实测1.980619→1.999923**（不写死为2）；
  匝数`N1·N2`逐位精确；单位往返零容差
- **12种错法逐条实测必须红，12种全部被抓住**，但抓住它们的门各不相同：
  **`K`/`E`次序颠倒只有椭圆那一层抓得住**（本仓的M不经过`K−E`，五条互感判据全绿）；
  偶极子近似漏因子2只有远场那一层抓得住；**μ0量级错恰好不被远场那一层抓住**
  （比值判据里μ0精确抵消——设计使然，也正是它必须单独成一条判据的理由）
- **域环登记只加一行**（spec/15第七节冻结条件2的实证）：门①②③⑤一个字未改。
  research/08预判"电磁大概率触发要改门"，**实测没有**——互感这条路不进`State`、
  不读材料记录、不装配能量项，research/08列的六道门槛一道都没撞
- **一条如实登记的规范面缺陷**：spec/14规则1"一份记录聚合多域字段"在力学+电磁
  这一对上**实测不成立**，且`converted_to`那条出路也走不通（`n_mm2`是复合量纲、
  无登记换算）。本块不读材料记录所以没撞上，边界仍然开着——见0042第五节

### T8 刚体（rigidbody）

- **`physics_engine.rigidbody`（实验档，力学域）：引擎第一次有姿态。**
  此前力学侧只有质点（三自由度），`geometry.py`算好的惯量张量没有任何东西去用。
  本块给出体坐标系Euler方程`I·ω̇ + ω × (I·ω) = τ`、单位四元数姿态运动学
  `q̇ = ½·q ⊗ (ω, 0)`、13自由度的自由飞行状态（质心位置/速度 + 体系角速度 + 姿态）。
  惯量**直接取`geometry.mass_properties`**，不重推；`geometry`的失败关闭
  （带法兰圆柱、网格资产）原样继承
- **姿态取单位四元数不取欧拉角**：万向锁是定义上的洞（三参数表示在SO(3)上必有
  坐标奇点，ZYX的运动学映射带`1/cos θ`），而自由飞行必然穿过它；且本仓已有
  xyzw体→世界的四元数约定（`shapes.PosedBody`、`motion.Pose`），有门逐位对拍
  两处矩阵。归一化漂移**每步投回单位球**，而**门守在归一化之前那一侧**——
  归一化之后`|q|`恒为1，那一侧的断言永远通过
- **两个转动积分器**（`rk4_rigid_body`、`explicit_euler_rigid_body`），
  各带spec/12第4.2节五项出生声明，声明类型复用`integrate.IntegratorDeclaration`；
  两个`production_ready`都是`False`并有门守着（RK4非辛、显式Euler反耗散）。
  实测阶：RK4漂移比16.1183/16.0617（角动量）与15.6772/15.8690（动能）；
  Euler 2.0016/2.0008。一份公式源两个求值后端，逐字节对拍是构造保证的
- **`StateField.is_dimensionless`（加法式，既有布局指纹逐字节不变）**：
  单位四元数分量是真无量纲，此前只能在名字上挂假单位才进得来
  （轴2规则5早就把"无量纲"当成需要显式列出的类别，`StateField`缺这个口子）。
  两个方向都堵：没单位又没声明拒、声明无量纲却带单位后缀也拒。
  该标志**不进`to_document()`**，有门用手写的旧口径文档证明指纹未变
- **`StateLayout.fingerprint()`第一次承重**：`integrate_free_flight`拿它当进函数
  的门，布局次序换一下即拒；模块内的块偏移量一律由布局导出，
  并有一条AST门禁止出现带整数字面量的切片
- **新案例`cases/rigid_body_free_flight`（B档，local_batch，11条oracle）**：
  守恒量（惯性系角动量矢量+转动动能，漂移随步长降16倍）、轴对称闭式进动
  （Drake `free_body`/Kane 1983，**带符号**，扁体正长体负）、
  Dzhanibekov中间轴定理（闭式增长率+闭式振幅上界+确定性整数翻转次数）、
  四元数范数四层，外加一条常力矩闭式
- **七道门 × 五条注错的必红矩阵**，测试断言红门集合与登记**完全相等**——
  瞎的地方必须诚实地记成瞎的。转动动能对四条转动错法**整行全绿**；
  叉乘次序反时中间轴增长率`|σ|`不变；四元数次序反与体系/惯性系搞反
  **只被惯性系那两道门抓住**
- **主动登记一条盲区**：无力矩下`ω̇ = −I⁻¹(ω × I·ω)`对`I`零次齐次，
  所以**四层判据对惯量整体缩放`I → c·I`全盲**（实测乘1e6后末态逐分量只差舍入），
  而mm²↔m²恰是1e6。同行的Drake `free_body`与MuJoCo `dzhanibekov.xml`同样只跑
  无力矩、同样有这条盲区。本案例为此补了两道原不在计划内的判据
  （惯量对教科书闭式、常力矩斜坡）
- 域环登记`rigidbody` → `mechanics`（`test_domain_isolation.py`与spec/15第二节同批）；
  决策记录0043（第三节专答`StateLayout`的边界：姿态是定长块、
  **不能**容纳数量随构型变的接触锚点，0033那条零进展也未变坏）

### T11 点载荷与屈曲

- **`energies.PointLoad`（第五个能量项）**：指定节点上的固定外力势能
  `U = −Σ F_i·x_i`，梯度为常量`−F`、Hessian恒为零。
  **单位边界与`UniformGravity`相反**：力已经是N、位置是mm，`N × mm`
  直接就是能量单位，**这里没有`MM_PER_M`**——照抄相邻代码除以1000会差1000倍。
  三条门守它：手算数（`F=(2,−3,5)`、`x=(7,11,13)` → `U = −46 N·mm`）、
  量纲（1 N移1 mm恰为−1 N·mm）、自由体（`a·m == F·1000`，对每个质量都成立）
- **零Hessian的陷阱有正向断言**：只含`PointLoad`的注册表切线刚度全零，
  `solve_equilibrium`在稠密与带状两条路径上都必须报`singular`而不是返回垃圾解
- **`solve.tangent_stiffness_is_positive_definite`**：约化切线刚度的正定判别
  （带状Cholesky，`O(m·b²)`，全部主元为正 ⟺ 正定）。
  `solve.py`开头"适用域：Hessian在解附近正定"**此前是一个不可验证的申报**，
  本函数把它变成可查的；失稳判据就建在它上面
- **`cases/euler_buckling`（C档，决策0040那张表的第3条）**：
  `Fc = π²EI/(bL)²`，**b=1／2／0.5三种边界条件由特征方程求根得到而不是抄表**
  （文献值Timoshenko & Gere 1961只作交叉验证，两端固支还比较了另一支根
  `tan v = v`，因为"b用错"最常见的形态就是拿了大的那一支）。
  四层判据：临界载荷值（三种边界条件，实测相对偏差−1.275e-04／−2.947e-05／
  −4.332e-04）、**保长扰动的能量在0.7Fc升在1.3Fc降**（定性转变，
  且是本案例唯一验`energy()`符号的门）、网格收敛（3.9916／4.0033／4.0226）、
  模态（正弦半波在四个试验形状里给出最低临界载荷，间隙5.9e-7）。
  外加一条**比连续解紧100倍**的门：命中铰接刚性链的**精确**特征值
  `(4EI/h²)sin²(π/2n)`到2e-6，残余的`+Fc/EA`是轴向可伸长性，扣掉后 ≤ 1.9e-10
- **`3h/2`固支求积修正的第三个独立实例，而且换了物理问题**（前两次是静挠度，
  这次是特征值）：系数扫描1.00/1.25/1.50/1.75/2.00全是干净一阶，
  **只有1.50例外**且误差小两个数量级；反解"误差恰为零"的最优系数随h线性趋近1.5
- **`cases/large_deflection_cantilever`的1e-15 kg寄生质量hack清除**
  （决策0029第八节登记的诚实边界）：端载荷改由`PointLoad`直接施加，
  `UniformGravity`退出该案例。**金标一个数都没改**（`expected`逐字节不变），
  改的是清单`inputs`（去掉`gravity_mm_s2`与`parasitic_mass_kg`），
  `regenerated_by`指向0046。换路径的数值代价按spec/13义务2**第二档**声明：
  端点误差变化 +1.9e-11／+1.7e-10／+1.4e-09／**+1.3e-05**（最细档最大，
  正是寄生质量最多那一档），比残差容差1e-7 N所隐含的位移带窄800倍
- **如实登记：牛顿法穿不过分岔点**。超临界处从带扰动的初值出发全部不收敛
  （回溯40—389次），这正是决策0029第八节那条缺陷。本案例**没有靠调参绕过去**，
  是换判据绕开的——代价是它不给任何后屈曲平衡路径
- `tests/test_energies.py` +9条协议门；决策记录0046

### T12 超导薄带解析基准

- **`physics_engine.electromagnetics.superconductor`（实验档）**：Norris 1970
  薄带临界态的conformal mapping闭式——片电流密度`K(x)`、磁通前沿`b`、
  以及薄带与椭圆截面两条交流损耗式。**电磁域（决策0041预登记的第三个物理域）
  的第一块代码**，纯标准库、零运行时依赖
- **案例`cases/norris_thin_strip`**（B档，interactive，4条oracle）：
  场景①（超导带电流分布）第一个可跑通的解析基准。金标由**50位十进制定点算术**
  生成（`decimal`，`arctan`由生成器自己用半角约化＋Taylor写、`π`走Machin公式），
  与被验实现**连代数形式都不同**
- 四层独立判据：分布对50位参考rel 1e-12；**电流守恒`∫K dx = I`两条求积**
  （`x = b·sin t`代换式1e-9 对 直接式1e-6——**同一恒等式在两种求积下差三个
  数量级**，直接式那条逐位复现research/12记的1.3e-7与5e-10）；`b`的两个零容差
  极限；损耗渐近`i⁴/3`与`i³/3`（**幂次4与3是这两条式的招牌**）
- **必红矩阵逐格实测**：五种典型错法 × 五条门冻结在`RED_MATRIX`，
  断言整张矩阵一致而不是"至少一条红"。**发现：`b`公式漏写`sqrt`（或漏写平方）
  被两个零容差极限完整放过**——它们在`i=0`与`i=1`上恰好给出相同的值且照样单调，
  只有电流守恒抓得住（与0022记的那次同形）
- **订正research/12两处**：（一）它实测的"薄带`i=1e-3`给3.331e-13"**是对数相消的
  数值噪声不是物理**，真值3.3333346666673814e-13（本实现`i ≤ 0.7`走无相消幂级数，
  实测最坏2.36e-15）；（二）**椭圆式的下一阶是`O(i)`不是`O(i²)`**，
  两条渐近的容差因此必须差三个数量级
- **数值形态两处实测改进**：`K`的arctan自变量按`(a·i)/sqrt((b−|x|)(b+|x|))`写
  （`a²−b² = (a·i)²`恒等，分子无相消；`b−|x|`由Sterbenz引理精确），
  样点最坏误差1.07e-12 → **4.48e-14**；损耗按`i ≤ 0.7`级数/以上`log1p`分段
- **量纲门**（`tests/test_superconductor.py`，19条）：公开参数名**与函数名**
  逐个过轴2规则3（`inspect`读真实签名，新函数带裸名即红）；
  长度按2的幂缩放时`b`与`K`**逐位**跟着缩放（零容差）；
  mm↔m因子取自`materials`的换算表而不是另写一个常数；损耗按`Ic²`缩放逐位精确；
  `per_`那个方向反了的1e12坑**逐条实测未踩上**
- **两条缺口绊线**：`Jc`/`Ic`是材料属性却进不了材料记录——
  `identity.BASE_UNIT_SUFFIXES`里**没有任何电学单位**，`critical_current_a`当场被拒；
  且`..._a_per_m2`（米制）能悄悄进一条mm制记录（`_SYSTEM_SUFFIXES`漏枚举`per_m2`）。
  两条都写成"缺口还在"的断言，补上那天变红。**spec/14规则1"一份记录聚合多域字段"
  在电磁域上今天整条不成立**，三条选法列在决策0047第六节交裁
- **未核实标记的门**：幂律指数`n`"典型20—30"这一说按字符窗扫本轨道全部产物，
  附近500字符内没有《未核实》即红（research/09那次事故的形制）
- 决策记录0047；`cases/README.md`补一行；
  `tests/governance/test_domain_isolation.py`的`ENGINE_RINGS`补
  `electromagnetics`一行（0041第三节预登记）

### T13 收口欠账清理（决策0049）

- **`main`上曾有12行未解合并冲突标记，被本批清掉**：`CHANGELOG.md`三处嵌套9行、
  `cases/README.md` 3行——**六条轨道合入时只解了一部分，标记连同内容一起提交了，
  而`accept.py` full当时全绿**。两个撞上的文件都是`.md`，
  **冲突标记在Markdown里不会让任何东西红**
- **新门`tests/governance/test_no_conflict_markers.py`**：扫全部**受版本控制的
  文本文件**（`git ls-files -z`，中文文件名不能按行切）。判据形制上
  `=======`**条件化**（同文件里有别的标记才算），
  理由是setext式一级标题的下划线与它逐字节相同——
  **会误红合法文档的门最终会被加豁免然后被拆掉**。
  必红拿**真实历史字节**验：`git show HEAD:...`的12行标记一条不漏全部命中
- **README与`__init__.py`的能力边界改成可证伪的**：此前写着"没有光学域"
  （`optics/`已有7个模块）、"没有几何精确DER弯曲"（`DiscreteElasticBending`早已落地），
  **电磁域与刚体在README里根本不存在**。新版按用户六场景逐条写
  "能算什么／算到什么程度／缺哪一块"，**两条分母并排**（主：六场景端到端0/6；
  从：同行C档6/13）
- **`cases/README.md`新增第一节之二**：19个案例按**穿过引擎哪几层**分类
  （0048第三节立的通则，那一波没落）。整条路3条、闭式计算器5条——
  **"验公式"与"验引擎"混在一个计数里，计数就不再有意义**
- **`peer_fcl_distance`补进案例表**：它**早已落地**（`criteria.json`+5条测试全绿），
  而本页一直写着"在建……落地后补一行"。**案例页校验器只要求案例名"出现在页上"，
  而它确实出现在"在建"那句话里**——门认得"被提到"，认不得"被登记"
- **wheel体积记账：0.5.0实测193066字节，是0.4.0的6.24倍**（`wheel_budget_history`补一行）。
  发版时被0018那道"记账义务"门拦下才发现——**门按设计红了**。
  核对结论：同期源码涨7.88倍（60341→475269，v0.4.0标签到本版），
  **wheel涨得比源码慢，不是打包缺陷**（wheel内42个条目全是源码，无夹带数据）。
  据此把spec/13第一条、README与plans/01里写死的**"wheel 30KB离线装"改掉**——
  那个数在本版已不成立。plans/02当时预测"约49KB"，**实测比预测还差约4倍**
- 新文档：[plans/06 四条结构性缺口](docs/plans/06_四条结构性缺口_20260805.md)
  （体积与厚度／接触／耦合／历史，**每条带可证伪的"算补上了"判据**）；决策记录0049

## 0.4.0 — 2026-08-05

新增：

- **narrow phase第一片（球/胶囊族）**：线段-线段闭式最近距离；同族对给
  `confidence="narrow_phase"`与精确`penetration_mm`；**broad命中但narrow
  判分离的对不再报事件**（假阳性消除）。圆柱/盒/网格对诚实保留
  `broad_phase`（下一片再做，不冒充）
- `PosedBody.transform_point_mm`/`rotate_local_mm`位姿变换
- `segment_segment_distance_mm`进公开面

## 0.3.0 — 2026-08-04

新增：

- `physics_engine.scene`：场景文件格式（`physics_scene`面1.0.0，draft）——
  严格加载、未知键失败关闭、`extensions`声明式扩展加载（Gazebo形制）
- `pe-scene`命令行入口：`validate`、`check-collisions`（linter式退出码；
  `--out-dir`走run package原子发布+同边界复读）
- `SHAPE_KINDS`注册表与`register_shape_kind`（重复登记拒收）
- 示例场景`examples/collision_preview_cell.scene.json`（真实参数）

## 0.2.0 — 2026-08-04

首个发布版。新增：

- `facets`：面清册+失败关闭读取端（轴1参考实现）
- `canonical`：声明式规范化JSON读写对（轴3规则2）
- `identity`：四段身份、命名空间ID、单位后缀校验（轴2）
- `provenance`：耐久独占写、no-replace目录改名、四点签名保护读、
  目录快照与机械严格复读（轴5机械半边）
- `run_package`：装配（manifest最后写）、语义闭包复读、生命周期法则（轴4/5）
- `shapes`+`collision`（实验档）：形状声明层与broad-phase碰撞查询
- `tools/accept.py`自吃药验收器（30/120双档、三轴正交）与
  `engine_acceptance_receipt`面

## 0.1.0 — 2026-08-04

骨架占位（未发wheel）：包结构、章程、七条规范轴、操作单。
