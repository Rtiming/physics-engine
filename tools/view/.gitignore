# 查看器档的产物与环境**都不进版本控制**。
#
# `.venv/`已被仓根.gitignore的同名行覆盖（实测），这里再写一遍是为了
# **本目录被单独拷走时边界仍然成立**——tools/view/.venv实测521 MB。
.venv/

# rerun的记录。它是**一次运行的快照，不是判据**（见README第四节），
# 进了历史就是单向门——Bullet issue #2918那297 MB的账正是这么来的。
*.rrd
*.rbl
