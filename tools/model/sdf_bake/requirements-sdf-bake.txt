# SDF烘焙档的依赖清单——**只装进tools/model/sdf_bake/.venv，永不进主环境**。
#
# 与validation/requirements-peer.txt、tools/view/requirements-view.txt同一条纪律
# （决策0025第一节第3条）：版本钉死（==）。
#
# 选型理由在决策0074第5.1节，一句话：**我们的网格是脏的**
# （实测非流形边、非watertight、自相交），而pcu的API名字里就写着"soup"——
# 广义缠绕数用体积判据定内外、不靠面法向投票，"一条边被三个三角共用"
# 对它不构成问题。其余候选（trimesh.proximity / Open3D / pysdf）
# 在API契约上就要求封闭流形，出局的是**能力**不是可得性。

point-cloud-utils==0.34.0   # MIT

# ---------------------------------------------------------------- 传递闭包 ---
#
# 下面几行**不是约束，是2026-08-18在本机实测到的事实**，写在这里备查。
# 刻意不钉死它们，理由与tools/view/requirements-view.txt那条逐字同源：
# 本仓拿不到别的平台的实测，**钉一个没验过的数比不钉更坏**。
#
# 实测值在安装后由setup脚本的自检段打印；本文件不预填，
# 因为预填一个"应该是这样"的数正是本仓禁止的那种冒充。
