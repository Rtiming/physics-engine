# 并行执行与验收（2026-08-12立，2026-08-13收口）

**这一页是本轮并行的入口与进度台账。** 随做随更新——
用户随时打开它就能知道：现在到哪了、验收要看什么、哪些留空了。

前置见[0053 并行开工前置](../decisions/0053_并行开工前置_十一个冲突面与开工自检_20260812.md)，
裁决见[0052 七条裁决](../decisions/0052_阶段1至3开工前的七条裁决_20260812.md)。

## 零、这一轮的目标与总原则

**目标**：完成plans/08阶段1—3的最小诚实版本，并让能力位分子由真实物理路径推动。

**总原则（用户2026-08-12原话）**：**最真实永远是首要目标，其次是性能。**

**它已经改变过一次选型**：0052第一节按这条否掉了会产生吸引力的分离约定。
当时以为代价是恢复系数多出下界`exp(−2)`；research/15随后推到过阻尼区并证明
那只是`ζ=1`的连接值，0055/0057已按全定义域`0<e≤1`实现与裁决。

## 一、计算落位（契约第四条的判据，先跑再派）

| 活 | 实测量级 | 落位 | 理由 |
|---|---|---|---|
| `accept.py full` | **76.1秒**（本次收口实测） | **本机** | 仍低于120秒产品预算与"预计>5分钟就外派"的线 |
| 单文件靶向测试 | 秒级 | 本机 | 同上 |
| 阶段3的漏斗（512粒子） | **2895秒/物理秒** | **不在本轮** | 0052第七节裁定本轮只做10—20个球 |
| 阶段3的十球漏斗（0.3物理秒） | 测试模块约**4—6秒** | **本机批级** | 真实走45球对+30球面声明；规模结论只到10球 |

**本轮不外派master。** 若阶段3扩规模，那时才按`rtime-compute-pick`重新判。

## 二、四条原始轨道与收口

切法**按文件所有权**，不按主题——两条轨道不许写同一个文件。

| 轨道 | 执行者 | 独占 | 决策号 | 状态 |
|---|---|---|---|---|
| **A 积分线** | 主代理（主仓） | `integrate.py`、`tests/test_integrate.py`、新案例 | **0054已写** | ✅**阶段1完成** |
| **B 能量线** | 主代理（主仓收口） | `energies.py`、`tests/test_energies.py` | **0055已写** | ✅**阶段2完成** |
| **C 计数线** | 子代理（worktree） | 能力位清单、`tools/`、`accept.py`及其治理测试 | **0056已写** | ✅**已合并** |
| **D 推导线** | 子代理（worktree） | `docs/research/` | **不需要**（三件全是推+验，无可裁） | ✅**已合并** |

工作树一律在**仓外**`../physics-engine-wt/<轨道名>/`——
放`.claude/`下的树过不了开工自检第2条（实测扫0个文件）。

## 三、每条轨道的验收标准（**先定标准再开工**）

### 轨道A｜阶段1：把瞬态罚接触从既成事实变成被声明的能力

| # | 判据 | 档 |
|---|---|---|
| A1 | 接触时长`t_c = π/ω`，`ω = sqrt(k_eff/m_eff)` | 第1档解析 |
| A2 | 最大穿透`δ_max = v_in/ω` | 第1档解析 |
| A3 | 无阻尼恢复系数`e = 1` | 第1档解析 |
| A4 | 步数/接触与`|e−1|`的单调表 | 第1档 |
| A5 | 步长顾问是**纯函数**、`integrate.py`包内import仍为**0** | 结构 |
| A6 | `step_bound`补第二段（接触分辨界） | 结构 |
| A7 | `production_ready`四条翻转条件写死、**本轮不翻** | 结构 |
| **必红** | 把`δ_max`判据写成准静态的`N/k` → **刚度换一档必须红** | — |

必红的理由：那是plans/08第零节实测的口径混用，`k=1e5`时准静态公式差**1010倍**。

### 轨道C｜能力位清单与计数门

| # | 判据 |
|---|---|
| C1 | 六场景各拆成可核对的能力位，**分子从清单算出来**，不手写 |
| C2 | C档13条做成清单；**算出来的分子若不是6，说明散文里那个数错了** |
| C3 | 门判据落在**结构位置**上（教训二），不落在"出现过"上 |
| C4 | 门挂进`accept.py`的`REQUIRED_TOOL_COMMANDS` |
| **必红** | 门有几个判据分支就有几组必红（教训三），每条写明"注错方式" |

### 轨道D｜推导补全

| # | 判据 |
|---|---|
| D1 | ζ≥1三段（过阻尼/临界/连接）的闭式，**两个约定各一份** |
| D2 | 每条闭式有**独立数值积分**对拍（**闭式代闭式不算核验**），偏差数写出来 |
| D3 | 明确回答：截断约定下恢复系数的**绝对下界** |
| D4 | research/13第五节那张表**带重力**重做，标明原来哪几行错、错多少 |
| D5 | 三球金字塔`μc = 2−√3`独立重推（不读实现再复述） |

### 轨道B｜耗散记账（已完成）

| # | 判据 |
|---|---|
| B1 | `EnergyRegistry`每项自带`potential`/`dissipation`标签 |
| B2 | **求和次序不变**——既有产物指纹逐字节不变 |
| **必红** | 改了求和次序必须红 |

实际收口还包括：同一注册表内的耗散力/功率、独立的物理耗散累计入口、
不替换旧`velocity_verlet`的新二阶`velocity_verlet_damped`，以及平面/球-球线性dashpot。
欠阻尼与过阻尼都由真实碰撞案例对拍，见0055。

## 四、收口时主代理要做的四件

1. **验收**：四条合并后跑`accept.py full`，**并复跑每条轨道申报的必红门**——
   轨道自己说"验过红"不算数，合并后的树上再红一次才算数（轴6规则5）；
2. **整理**：案例阶梯打钩、`cases/README.md`索引、CHANGELOG四个锚点合并、
   plans/07统一插行、README两条分母表统一改；
3. **优化**：合并后统一性能复测——四条轨道各自的加速不可加，
   eager import模块数与冷启动是全局量；
4. **主仓复验**：开工自检在主仓再跑一遍。
   **worktree里的绿让"副本里的绿"可信，不让它充分。**

## 五、进度日志

| 时间 | 事件 |
|---|---|
| 08-12 | 裁决七条落纸（0051、0052）；17个陈旧worktree+18个分支清空，释放55MB |
| 08-12 | 开工自检落地（四条断言各自实跑红过），三个可选位升必需，`accept.py`挂上第四道工具门 |
| 08-12 | 冲突面重新测绘3→11个，四条轨道预分配（0053） |
| 08-12 | 仓外双worktree建成并各自4/4自检通过；**轨道C、D派出** |
| 08-12 | 轨道A开工 |
| 08-12 | **轨道A阶段1完成**：步长顾问落地（`advise_step`）、`step_bound`补接触分辨界、`production_ready`四条翻转条件写死、新案例`bouncing_ball_restitution`（6条判据+必红实跑红过）。决策0054 |
| 08-12 | **实测把一条已发布的声明打脸**：`explicit_euler`的`step_bound`写着"h < 2/ω_max"读起来像可用的界，实测`h·ω=0.1`跑40周期能量涨**7.2e10倍**——任何步长都发散。已改写并让顾问对它失败关闭 |
| 08-13 | **性能测量链复核**：失败子进程不再被记成墙钟结果；源码递归口径统一；旧wheel须与当前源码文件集合及逐文件字节一致才算当前构建 |
| 08-13 | **能力位独立二次复核与四项裁决完成**（0057）：冻结每场景位数`7/5/10/7/6/7`；开放`0<e≤1`；过阻尼稳定率取快根；自由刚体只归主分母S3.1 |
| 08-13 | **阶段2完成**（0055）：类型化`EnergyRegistry`、新二阶阻尼积分器、耗散账、全阻尼法向dashpot；单次碰撞跨`ζ=1`对拍 |
| 08-13 | **阶段3最小版本完成**：十球、三个平面、球-壁与球-球阻尼接触同跑；S3.10由todo升partial，端到端仍0/6 |

## 六、验收清单——**你要验什么、到哪看、预期是什么**

### 6.1 一句话结论

**当前从清单算出来的数是`11/42位、C档 6/13条`，端到端仍是`0/6`。**
其中穿过物理机械的done位为`4/11`。十球平面漏斗只让S3.10升为partial：
它不是不规则体、没有转动/摩擦，接触对也不是动态发现，所以没有把场景③冒充完成。

### 6.2 逐项验收

| # | 验什么 | 到哪看／怎么跑 | 预期 |
|---|---|---|---|
| 1 | 全套绿 | `.venv/bin/python tools/accept.py full` | `overall=PASS`；本次76.1秒/120秒，1155 passed、2 skipped |
| 2 | **主分母的数** | 同上，输出末尾"capability ledger"那几行 | `主分母 11/42位、C档 6/13条`；六场景逐个报"第几位／共几位" |
| 3 | 能力位清单本体 | [`docs/capability_ledger.json`](../capability_ledger.json) | 42位各带状态、理由、缺在哪、`case:`/`test:`证据 |
| 4 | 瞬态接触的判据 | [`cases/bouncing_ball_restitution/case.md`](../../cases/bouncing_ball_restitution/case.md) | 无阻尼三条闭式 + 跨临界阻尼两档 + 耗散账 |
| 5 | 过阻尼分支 | [`research/15`](../research/15_过阻尼分支与推导补全_20260812.md) | 三段闭式 + 19档ζ最大偏差5.27e-15 |
| 6 | 步长顾问 | `PYTHONPATH=src .venv/bin/python -c "from physics_engine.integrate import advise_step; ..."` | 两个界并排，`stability_margin = π/(2N)` |
| 7 | 十球漏斗 | [`cases/ten_ball_funnel/case.md`](../../cases/ten_ball_funnel/case.md) | 10球真自由度、两类接触都出力、耗散与能量残差有界 |
| 8 | 决策记录 | `docs/decisions/0051`—`0057` | 七份，编号0055与0057承载本次裁决；0056为原清单决策 |

### 6.3 **三条我要你知道的、结论被改掉的事**

1. **恢复系数没有绝对下界**——我几小时前告诉你"最低只到0.135、湿沙橡皮泥做不出来"。
   那句话只在ζ<1里成立。过阻尼分支推出来之后，`e`可以任意低（e=0.05→ζ=1.945）。
2. **你选的那个约定，是过阻尼区唯一能用的**。另一个约定（重叠归零）在ζ≥1上
   **根本没有有限根**——物体被拉力粘住。裁决时我们并不知道这一条。
3. **`explicit_euler`的步长上界是一句误导**，实测任何步长都发散。
   它出生时有门守着，但门守的是"这一格非空"，不守"这一格说的是不是真的"。

### 6.4 留空的GAP与已裁决项

**GAP（都带触发条件，已进[plans/07第六节](07_缺口清册_20260805.md)）**：
转动积分器的步长系数未实测；恢复系数误差为何比形式阶收敛快；散文分数虽已人工对齐但缺机械对账门；
能力位清单是否登记面；`exercises_physics`只有两档；主分母42没有外部锚。

**原四项待裁已全部由0057解决**：

| # | 原待裁 | 裁决 |
|---|---|---|
| 1 | **主分母42要不要冻结** | 冻结总数及每场景`7/5/10/7/6/7`；拆并位须新决策+改红门 |
| 2 | 接口要不要放开`e ≤ 0.135` | 放开到全部有限可表示的`0<e≤1`；`e=0`失败关闭 |
| 3 | ζ≥1时步长顾问用哪个`ω_max` | 稳定界取快根，分辨界另取真实合力归零接触时长/N |
| 4 | `rigid_body_free_flight`的分母归属 | 保留主分母S3.1；不扩写固定的C档13条 |

### 6.5 诚实的限制

- **42位已有不同执行者逐位二次复核**，但拆法仍没有消费方给出的外部锚；冻结只防口径漂移，
  不把内部拆法变成外部真理；
- **C档第7、8、9三条的partial判读抄自案例页自己的声明**——诚实，但同一批人写的；
- **11/42不是加权完成度**，是"可核对的能力位里做掉了几位"。清单不提供权重，
  理由是权重一旦可调，分子就又可以商量了。

### 6.6 **推送授权（要你一句话）**

截至本次收口前，当前分支相对`origin/main`**领先15个既有本地提交**；
本次阶段2/3与文档对账仍在工作树，按规矩**没有提交、没有推送、没有发版**。
远程两个：`origin`（orangepi）与`github`（公开镜像）。
