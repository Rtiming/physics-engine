# 交接（2026-08-06）

**接手先读这一页，再按AGENTS.md的“开工必读”往下。**

> ## **2026-08-20：物理基础设施归属与张力测量小场景（本页最新一段，先读）**
>
> 用户确认四条：①近期优先补**物理基础设施**；②基础设施留在physics-engine；
> ③WDS只读消费GCW/WII模型产物与本仓力学/物理量，不反向修改上游；
> ④先用“放线—测力轮—张力传感器—收线”小场景验证引擎物理。
>
> 文档与文献正本之后，首批物理代码已经推进到T-M0/T-M1：
>
> * [research/19](../research/19_绕制偏差物理基础设施与上游模型接入调研_20260820.md)：
>   审计现有地基、WII/GCW模型可直接使用边界、同行证据和GB/T 7714参考文献；
> * [0096](../decisions/0096_绕制偏差物理基础设施归本仓与WDS单向消费边界_20260820.md)：
>   固定四仓所有权与单向消费；
> * [plans/19](19_绕制偏差物理基础设施与张力测量小场景计划_20260820.md)：
>   第一批从“两侧张力矢量合力→LTS敏感轴→mV/ADC”开工，再接小型收放线。
> * [0097](../decisions/0097_测力轮张力测量物理与T-M0_T-M1_20260820.md)：
>   `tension_measurement`已落一般三维合力、敏感轴、tare、gross/net、显式支承份额、
>   旧`TensionSensor`组合和严格样点面；T-M0/T-M1两个独立静力oracle已落。
>
> **最重要的缺口已经向后移动**：通用静力层不再缺；现在缺T-M2五点标定/留出点/采样时延、
> T-M3小型放线—收线动态链，以及现场LTS的真实支承、敏感轴、导轮重量、包角和VR451比例。
> 所以今天仍不能声称“绕线机测出来的张力已经准”，但已经能独立判出合力符号、包角、
> 安装轴、tare和支承分配是否算错。
> WII/GCW模型可以复用，但只消费带SHA、单位、frame、用途和交付状态的产物；visual模型
> 不能冒充collision/physics，WII碰撞报告也不能冒充接触力。

> ## **2026-08-19：基础设施批次二收口（本页最新一段，先读这一段）**
>
> **一句话**：五条轨并行做完（网格窄相、光学收口、电磁收口、场景⑤、同行深读），
> **主分母13/42 → 17/42**，场景③与④的`todo`**清零**，场景⑤拿到**它的第一位done**。
> `accept full`在master四轴全绿。
>
> | 量 | 值 |
> |---|---|
> | 案例 | **47条**（+5） |
> | 主分母／C档／端到端 | **17/42**（+4）／6/13（不动）／**0/6**（不动） |
> | 源码字节 | 1506932／4194304，用量0.359 |
> | 快档 | 2525 passed、约35秒 |
> | `accept full`（master） | **四轴全绿**，`elapsed=117.1s/180`（**预算已按0089从120抬到180**） |
>
> ### 接手最该知道的七件
>
> 1. **"逐位指纹"在本仓是一条同平台声明，而此前没人说过。** 这一批抓到两例：
>    九个oracle跨架构不逐字节复现（`expected`里的**金标值本身**在最后几位不同，libm的超越函数）、
>    以及轨戊新冻的窄相指纹在x86-64上2对不同（16 ulp与1 ulp）。
>    **两例的案例判据在两个平台都是绿的**——带各自声明的容差。
>    已按同一形制拆成"跨平台判红／同平台判skip"，
>    并登记：`engine_oracle_manifest`**不记产出平台**，**在它记上之前"逐字节"不许被当成跨机承诺引用**；
> 2. **轨壬（同行深读）更正了本仓三处裁决**，其中一条会改变下一批的排序：
>    **S5.2（自接触）的前置不是网格窄相**——同行做绕线/打结的两家用段-段最近距离＋半径，
>    而`collision.py:42`那条本仓已经在跑。**S5.2比旧文本隐含的要近，排下一批时按0094那页排**；
> 3. **`sampled_field`进不了接触管线**，因为域隔离门不许`collision`（基座圈）import`contact.field`（力学域圈）。
>    **答案是协议不是import**，那条协议还没裁——**它是"网格窄相真正接上力学"的下一块**；
> 4. **场景④只剩S4.7一位partial**，而它缺的是**角分布**（`S_1`/`S_2`、`π_n`/`τ_n`、`g`）与
>    **一份可引用的已发表基准表**——今天挡住共同转写错误的只有三条**间接**判据；
> 5. **我这一轮自己犯了三个"没算就下结论"的错，三次都被别人或门核出来**：
>    把一个案例的比对结论外推到九个、把0085的"比值4.0（二阶）"转述成"四阶"、
>    把16 ulp读成1 ulp（`float.hex()`一个十六进制位是**四个比特**）。
>    **纪律的结论是**：0089第四节那条"合入方逐格核"**需要反过来也成立**，执行者也要核派活书；
> 6. **`accept full`预算抬到180，而分档被推迟了不是解决了**：180的余量约够再来三批
>    （按+14.9%测试/批的增速）。**撞破那天不许再抬**——0080那条"要裁的是分档"仍然有效；
> 7. **所有入口在一页表上**：[`docs/entry_points.md`](../entry_points.md)。
>    **选择进入档现在有统一入口**（`tools/verify_optin.py`）——此前那三条通道从来没被整体跑过一次，
>    而`accept full`只会报skipped、**skip在回执里看起来和pass一样绿**。
>
> ### 收尾整理（同日，决策0095）
>
> **`contact/penalty.py`拆成了一个包**（2378行、六族 → 一族一个文件，最大738行）。
> **公开面与落盘金标一个字节没动**，master上墙钟117.5秒与拆前同离散度。
> 判据是2026-08-17拆`contact.py`那次自己写下的"改200行要先读多少行"——
> **2157是上一次触发的实测值，2378是这一次的**。
> 下一个够到线的是`rod.py`与`contact_dynamics.py`（各1448行），已登记。
>
> README的"能力边界"同批订正三处已经不成立的话
> （"没有复数场与FFT"、"没有圆柱/盒窄相"、"没有力矩"）——**结论都还对，错的都是理由**。
>
> ### 下一步（按"离动分母最近"排）
>
> * **`collision` ↔ `contact.field` 那条协议**：它挡着"网格窄相真正接上力学"，
>   而S3.2剩下的四条里有两条在它后面；
> * **`MeshAsset`带几何**：S3.2的label里"网格"两个字今天连输入都没有；
> * **自接触（S5.2）**：按0094重排之后它比原先近——不等网格窄相；
> * **Mie的角分布**：场景④唯一剩下的那一位；
> * **场景⑥**：仍是端到端0/6里最近的一条，但3.3那一步**卡在仓外**（WII至今没导出过v2时间线）。

> ## **2026-08-18深夜：基础设施批次收口（本页最新一段，先读这一段）**
>
> **一句话**：四条轨并行做完（网格与距离场、二维复数场与FFT、判据与度量、场景⑥接线），
> `accept full`在master四轴全绿，**主分母只从12/42动到13/42**——
> 而这不是失手，是**逐格核过之后只有一位站得住**。
>
> | 量 | 值 |
> |---|---|
> | 案例 | **42条**（+2：`sdf_contact_convergence`、`double_slit_propagated`） |
> | 能力位／C档／端到端 | **13/42**（+1）／6/13（不动）／**0/6**（不动） |
> | 源码字节 | **1360616**／上限**4194304**（0084按所有者裁决抬到4 MiB），用量0.3244 |
> | 快档 | 2236 passed、6 skipped、约32秒 |
> | `accept full`（master） | **四轴全绿**，`elapsed=106.8s/120`（合入前同一台机器是87.7） |
>
> ### 接手最该知道的六件
>
> 1. **源码上限抬到了实测的3.50倍，而这违背本仓自己十五次的纪律**（前十五次压在1.25—1.65，
>    每次都写着0039那句"绊线一旦长期不响就等于被拆了"）。所有者裁决余量不设限，
>    照办了，**但后果写成了GAP**：这道绊线从此不再是增量守门人，
>    **今天还会响的只剩台账那条5%漂移门**——而它在本批合入轨甲时就响过一次。
>    要把信号找回来，该做的是**把绝对上限换成每批增量上限**，不是把这个数调回来；
> 2. **墙钟从87.7涨到106.8，余量只剩11%。** 测试从2067条涨到2374条，
>    **这一次是套件真的在长，不是宿主污染**（同一台master、同一分区、同样核数）。
>    下一批再加一轮案例几乎必然撞破120秒。**要裁的是分档还是抬预算，本批不裁**；
> 3. **"派活方写的那张解开哪几位的表，本身没有门在看着"**——0084第一节把S3.2记进了轨甲，
>    而轨甲交回时更正：0074第一节自己写过"几何判定与接触力学是两道题"。
>    **那张表是一句期望，而期望会被当成结论**；
> 4. **S4.6那一位是合入时逐格核出来的**：label三件（单缝／双缝／**矩孔**）只做了两件，
>    因为仓里每一处`rectangular_aperture`调用的y半宽都取`1e9 × pitch`——**那是缝不是孔**。
>    形状与S3.7、与C档第7条那次退回**完全相同，这是本仓第三次栽在同一个形状上**；
> 5. **0082自己那条`k_t`上限式子是错的**（只算了偏航模态，最紧的是平动＋俯仰耦合），
>    **按原式算出的上限几乎放行了实测发散的那个`k_t`**。0087不再补模态，改算整个六维算子的谱；
> 6. **三条案例的收敛阶一条都没测出来**，而四条"测不出来"的理由各不相同——
>    其中`box_rocking`拿到了一个**没有警告的裸数0.5267**，
>    因为既有判据只看绝对差递减、看不见带符号差变号。已补第二条判据。
>
> ### 下一步（按"离动分母最近"排）
>
> * **矩孔那一格**：补一条两方向都有界的孔的传播判据，S4.6随之可判——**代价很轻**；
> * **角谱那一侧的案例**：S4.5在等它；
> * **场景⑥**：五位partial、零位todo，仍是端到端0/6里最近的一条，
>   但3.3那一步**卡在仓外**（WII至今没有导出过一份v2时间线产物）；
> * **`box_rocking`的病因**：它挡着三条案例的收敛阶。

> ## **2026-08-18夜：波次五收口（本页最新一段，先读这一段）**
>
> **一句话**：0074路线的阶段一余项、阶段二、阶段三三件一次并行做完，
> **`accept full`四轴全绿**（`overall=PASS`、96.3秒/120、38个案例）。
> **但两条能力位一位都没进**，而且这不是失手——是两条轨各自把"以为会进"实测成了"没进"。
>
> | 量 | 值 |
> |---|---|
> | 案例 | **38条** |
> | 能力位／C档／端到端 | **12/42**／6/13／**0/6** |
> | 源码字节 | **1133684**／上限1310720（本波抬过，决策0077），余量177036、用量0.8649 |
> | `accept full` | **四轴全绿** `elapsed=96.3s/120`（跑前load 2.67） |
>
> ### 三件落了什么
>
> * **轨C2（0079）｜转动自由度进准静态**：SO(3)指数映射的局部图，
>   三个无约束参数**挂在节点块之后**——于是`node_dof_count`仍是`3n`，
>   **重力、罚法向、球-球接触这些只认前缀的项一个字都没改，`solve_equilibrium`也一个字没改**。
>   三球金字塔`μc = 2−√3`在仓内用`Q(√3)`精确有理算术重算（全程无浮点），实测相对偏差**1.766e-07**；
> * **轨A2（0078）｜活站点的扫掠槽壁**：新第六族`PenaltyGrooveSweepLive`，
>   **能量用求值期重定位的站点**，于是梯度自然带上`A·t`那一项，
>   FD梯度对能量**二阶收敛比恒4.0000**；冻结帧那族同构型反向复测**无收敛**——两族形态相反本身就是分辨力。
>   **第五族一个字节没动**，0075的十条案例判据与逐位退化门一条没红；
> * **轨A2第二件｜第一条读真实工件的案例**：本仓38个案例里的**第一条**。
>
> ### 接手最该知道的五件
>
> 1. **两条能力位都没进，而且是门抓的不是嘴说的**。轨C2一度把S3.7标成`done`，
>    `check_capability_ledger.py`的`MISSING_ON_DONE`门**当场判红**——S3.7的label是
>    "力矩平衡、**翻倒**、**无滑滚动**"三件事，它只做了第一件。
>    **0063第五节那句"第1步一次解锁S3.7与C档第7条"高估了它**，而我派活时照抄了那句话；
> 2. **`plans/14`第2.2节那张表有两格互换**（`v1-coil-1`与`v2-coil-02`的`τ_max`），
>    **两次独立计算（轨A2一次、收口方一次、差分口径不同）都给出与印刷表相反的排序**。
>    订正写在那一节里，**表里的数字按快照纪律没改**；
> 3. **比互换更要紧：`τ_max`不是一个稳的量**——两种差分取法差**19%—25%**，
>    第三种口径又低约30%；而`∫|τ|ds`两种取法差**不到1%**。
>    于是立了两条纪律：**`τ_max`只读量级不许当靶值**（0075那个"丢失5.3707%"因此也只有量级意义），
>    **零容差的门设在`∫|τ|ds`上**；
> 4. **`plans/15`第2.1条那句"9个几何都对"在本仓无法兑现**：九行里只有**五行**可达，
>    另四行的语料住在`pose-solver`仓，而0073裁真实资产永不进本仓。
>    轨A2已把两处"兑现"改成"部分兑现"。**要验另外四个，得先裁"跨仓只读语料怎么进判据"**；
> 5. **Hessian仍不精确**（`dκ_s/da`过站点变号取不出来），代价量成了数：
>    **牛顿二阶掉到一阶**，收缩率随τ大致线性（0.00748／0.02008／0.04565），多两到四次牛顿。
>
> ### 注错验证这一波的实况（**两条轨加起来抓到八道空门**）
>
> 轨C2两道（θ=0恒等分支、级数少一项），其中一道顺带抓出`_SERIES_THRESHOLD`取值错误
> ——**一行改动、误差降3500倍**。
> 轨A2**22条注错里第一轮有6条是空门**，形态各不相同，其中`σ`那条是
> **"判据只走了+1侧"——与0075第三节同一个坑的第二次**。
> **它事先写的预测表是错的，实跑后重写了。**
>
> ### 计时轴那条终于有了闭环证据
>
> 同一棵树、同一功能面：**load 2.67时96.3秒（PASS）／load 7.19时225秒（FAIL）**。
> 而资源轴两次都判`QUALIFIED`（限值15.0）。**四次同形态之后，这条已经不是"疑似"了。**
> 台账的87.3秒**没有更新**——本次只有一个合格样本，
> 而"单次墙钟不冒充新中位数"是本页早就写下的纪律。

> ## **2026-08-18晚：波次四收口（上面那段是它之后的进展）**
>
> **一句话**：0074裁的执行路线走完了第一与第五阶段——
> **带材真正的接触伙伴（槽）落地了，本仓也第一次有了能看的时间序列产物**。
> 但**阶段二（读真实工件）与阶段三（转动自由度）一步没动**。
>
> | 量 | 值 |
> |---|---|
> | 案例 | **36条**（新增`groove_sweep_wall`） |
> | 能力位／C档／端到端 | **12/42**／6/13／**0/6**（本波一位没动——partial不进位） |
> | 源码字节 | **1043642**，上限1048576，**余量只剩4934** |
> | `accept full` | `functional=PASS`，**`timing=FAIL` 135.7秒/120** —— 见下 |
>
> ### 两条轨落了什么
>
> * **轨A（0075）**：`PenaltyGrooveSweep`＋`groove_sweep_walls`，沿真实中心线的两段外倾锥面。
>   **它改了派活书给的两处几何，理由同一条**：`solve.py`申报的适用域是"二次连续可微"，
>   而`|u|`的不可微点是**槽心**、`max(v,0)`的不可微点是**槽底**——**两个都是标称构型**。
>   拆成左右两个单边壁＋把锥面线性延拓到槽底以下，于是`∇²g`恒为零（不是近似为零）；
> * **轨V（0076）**：`tools/view/`＋rerun 0.34.1，`src/`零字节改动。
>   **本仓第一条落盘的时间序列产物**——此前所有`oracle.json`的`arrays`字段全是空的。
>
> ### 接手最该知道的四件
>
> 1. **plans/15第2.2条那句话缺一个前提，已订正**：
>    "锥面给逐渐爬升的回正力、平面给硬碰"——**深度被钉住时是错的**，
>    那时两者横向力`k|g|`**逐位相同**，差别全在锥面多出的举升分量（合力比恒为`secα`）。
>    "变软"是**深度自由**时举升分量改变平衡位置的结果。深度自由＋压紧3N时锥面横向力
>    **饱和在`F_hold/tanα` = 17.0138 N**（五档横移同一个数），平面按`k`线性上升；
> 2. **轨A量出一条它没修的欠账，而这个数比初稿严重**：冻结帧丢掉`A·t`，
>    全九档丢失**0.9721%—14.2522%**、力方向偏**0.56°—8.11°**。
>    初稿把`τ=2.550°/mm`写成"最扭那一档"，**它其实是中位数**（最扭是`v1-coil-1`的6.648）。
>    **最扭那两档不能就这么进案例**——摩擦锥按力方向算。
>    可拆两半：**梯度那一半今天就补得了**（`dτ/da`实测−2.5e-14）；
>    Hessian那一半卡在`dκ_s/da`过站点变号——**拦路虎是`κ'`不是`τ'`**；
> 3. **源码余量只剩4934字节**（用量0.9953）。本批没抬（门未破，0069的规矩是撞破者自己带理由抬）。
>    **下一个动`src/`的批次几乎必然撞破它**；
> 4. **`accept full`的`timing`红了，而它与本波改动无关**：新案例隔离实测**0.05秒**，
>    同一批案例的墙钟却从70.87涨到112.86——机器上有**两个不属于本仓的**WII进程各吃满一个核。
>    **这是同一天第二次同形态**（功能全绿、墙钟被邻居顶穿、资源轴仍判QUALIFIED），两笔实测都已登记。
>
> ### 对抗审核挑出四条，全部改掉了（不是记账）
>
> 1. **轨V那道新门在主仓当场就是红的**——它`rglob`全仓，把`.claude/worktrees/`里
>    **别人的副本**判成违规。改成只扫git跟踪的文件，拿不到清单就失败关闭；
> 2. **静态判据对动态import完全失明**（`importlib.import_module("re"+"run")`等四种全能绕过）。
>    补了一把**量结果的尺子**：干净子进程import内核后`sys.modules`里不许有禁名；
> 3. **`replay.py`时间轴不判单调**，递减/重复/乱序三种全部静默通过。已补；
> 4. **CHANGELOG把0074的判据原文"在另一台机器上打得开"抄成了既成事实**，而跨机从没跑过。
>    已改回如实措辞并登记。**这一条是"文档在说谎"，性质比另外三条重。**
>
> ### 顺手关掉的两笔陈账
>
> * **0072那条"锥面槽壁"欠账**触发条件到达并由0075兑现，已划掉；
> * **案例分层表的计数**：波次三登记过一句"计数由收口时一次点清"，**没人执行**，
>   于是表头停在33、覆盖35、目录36。本次点清到36/36/36，
>   并**补了一道门**（`layer_table_problems`，三条判据各带必红、四条注错全红）——
>   **登记不等于会被做，除非有门看着**。

> ## **2026-08-18：0.8.0已发、WDS已采纳（上面那段更新了它之后的进展）**
>
> **一句话**：上一段说的"一个消费方都没有"已经不成立了——**阶段三的3.1与3.2都做完了**。
> 但**3.3还没做，而且它今天没有输入**（见下）。
>
> | 量 | 值 |
> |---|---|
> | 引擎版本 | **0.8.0**（wheel 383259字节、SHA `b9dc37b8…`、tag `v0.8.0`，wheelhouse与两个远程都已推） |
> | 消费方WDS钉的版本 | **`physics-engine==0.8.0`**，其V2 `verdict=pass functional=pass` 62.6s/120 |
> | `accept full` | PASS 84.6秒/120，四轴绿 |
> | 案例／能力位／端到端 | 35条／**12/42**／**0/6**（一位没动，采纳不解锁能力位） |
> | 源码字节 | 1022317，余量**26259** |
>
> ### 这次采纳换的是什么
>
> **只有依赖字节。WDS一行调用都没改**，0.7.0/0.8.0新进的绕线机端到端能力它**一处都没用**。
> 所以三前提第二条"有真实消费方在用"**仍然没有被兑现**——兑现它的是3.3那次真实回放。
> **采纳做成了升级前后对照**：升级前基线`2 failed/1367 passed`、升级后逐条相同。
>
> ### 这一轮抓到的四件事
>
> 1. **3.3今天没有输入，这条plans/15初稿没写**：WII侧只有exporter代码，
>    整个`lab`里按`material_feed_length_mm`grep所有JSON**零命中**——
>    **没有任何一份已发布的v2时间线产物**；WDS侧schema文件名还是`motion_timeline.v1.schema.json`。
>    **3.3的第一步是让WII真导出一次官方run，不是写代码。**
> 2. **仓库同步不会把master的conda env顺手带上**。升级后V2多出三条红，
>    全部指向master受管环境仍是0.6.0；`sync_workspace.sh check`一句话点破。
>    **换依赖必须另跑`sync_workspace.sh env`。**
> 3. **CHANGELOG会静默陈旧**。发版前发现`Unreleased`里横漂那条还引着6.5mm平轮那组
>    临界偏斜角，而案例页早已订正成2.0mm槽底那组（差3.25倍）。**案例页有门，CHANGELOG没有。**
> 4. **一条门差点在错误的分支上变绿**：WDS那个"影子包"测试里影子声称的版本原先恰好等于钉住版本
>    （都是0.6.0），只改钉版不改影子，拒绝的理由就从"来源不对"偷偷变成"版本不对"。已一并改掉。
>
> ### 顺手撞到的一条，登记但没修
>
> **资源轴的load限值15.0挡不住"两个邻居把核吃满"这一档。** 同一棵树（发版那次84.6秒、
> 四轴绿）在docs-only提交后重跑给**132.5秒、timing=FAIL，而resource仍判QUALIFIED**——
> 当时`load_1m = 4.25`（远在15.0以下），但机器上有syncthing 111%与一个WII的python 100%
> 在吃核。**0053那条注记针对的是"四条并行轨道、load 23—29"那一档，这一档它看不见。**
> 判据要不要改成"可用核数"而不是load average，**本轮不裁**——只登记：
> 墙钟结论在这台机器上仍然会被邻居污染，而资源轴今天不会替你喊。
>
> **同日第二次，而且这次把`overall`判红了**：波次四收口的`accept full`给
> `functional=PASS timing=FAIL elapsed=135.7s/120`、`resource=QUALIFIED`
> （跑前load 3.08、跑后4.37，机器上有**两个不属于本仓的**WII pose-solver进程各吃满一个核）。
> **这一次能证明它与改动无关**：本波新增的案例隔离实测**0.05秒**，
> 而同一批案例的墙钟从70.87秒涨到112.86秒。
> **两次都是同一个形态：功能全绿、墙钟被邻居顶穿、而资源轴判QUALIFIED。**
> 登记一条明确的触发条件：**下一次有人要动`accept.py`的资源轴时，这两笔实测就是它的输入**。
>
> ### 下一步
>
> plans/15阶段三只剩**3.3**，且它卡在上游导出上（第1条）。**在等它的同时该做的是阶段二2.1**
> ——`tools/model/centerline_csv.py`（纯标准库，0073第五节第2步），
> 它是"35个案例没有一个读过真实工件"这句话的唯一解法，且不依赖任何外部输入
> （真实CSV在GCW与WII里都是现成的）。

> ## **2026-08-17收档（波次二＋波次三；上面2026-08-18那段更新了它的结论，本段保留原文）**
>
> **一句话**：引擎侧的物理零件基本齐了，**但它们今天一个消费方都没有**——
> WDS仍钉在`physics-engine==0.6.0`，而0.7.0之后的全部东西都没发。
> **下一步不是再加物理，是发版与采纳**（plans/15阶段三）。
>
> ### 现在的位置（实测，不是印象）
>
> | 量 | 值 |
> |---|---|
> | 案例 | **35条**，`accept full`四轴全绿 |
> | `accept full`墙钟 | **87.3秒**/120（安静机器三次87.9/84.8/87.3取中位数，均`QUALIFIED`） |
> | 源码字节 | **1022317**，上限1048576，**余量26259**（用量0.975） |
> | 机械能力位 | **12/42**（partial 11位）——**波次二三加起来一位没动** |
> | 同行C档 | 6/13 |
> | **端到端场景** | **0/6** |
> | 消费方WDS钉的版本 | **`0.6.0`** |
>
> ### 两波做了什么
>
> **波次二**（0064—0068）：`rod`（整杆各向异性弯曲＋扭转）、`transport`（线速度与输运）、
> `laydown`（落位点几何）、`autodiff`（jet提升为共享）、各向异性摩擦椭圆。
> **波次三**（0069—0072）：`tension_control`（真闭环＋控制器协议）、
> `disturbance`（臂动与人手触碰）、步进器接椭圆、`rod`接槽壁接触。
> 另有0073（工具住哪与模型形态）与`tools/model/mesh_aabb.py`。
>
> ### 接手最该先知道的四个数
>
> 1. **额定磁粉离合器比跨段谐振慢19倍**（`ω_n·τ = 18.98`）——闭环把10%线速度阶跃的
>    响应**变坏**（峰值×1.073、ISE×1.389），额定档**不存在**快档设计的可行域
>    （Routh第一列−16525.92）。**这不是调增益能解决的，是执行器带宽不够。**
>    真机压这条谐振靠**摆杆/储带机构**，而本仓没有摆杆模型（0070，已登记）。
> 2. **`σ' = Ω_切向/κ`**——臂动扰动张力的通道是**曲率在变**，不是切向在转。
>    切向转再快、曲率恒定则送带率恒定（0071）。
> 3. **真实工件是非平面3D槽**，9个不同几何**没有一个是平面件**（离面18.4—116.9 mm），
>    整匝帧扭转`∫|τ|ds`跨236°—657°。hard-way应变按0.4%线**9个几何全超**
>    （2.8%—12.5%）；5.9%超0.6%那个数是`v2-01-bracket`的**特例**，
>    且**一半来自"4mm带宽"这个假设**（plans/14第2.2节）。
> 4. **槽壁挡住48.171%的扭转**（强制帧扭转0.3316 rad → 实际0.1719 rad），
>    对不import内核的分段线性闭式差4.07e-06，四档罚刚度整齐差十倍（0072）。
>
> ### 三条被订正的说法（**接手不要照旧版理解**）
>
> 1. **自由跨段不动**，动的是线圈。两个端点（最后那只导向轮、入带点）**都是世界系固定的**。
>    我第一版写成"一端固定一端随臂动"，错了（plans/14第3.3节）；
>    0066第九、十节仍留着旧措辞，plans/07有一行指名订正。
> 2. **`tool_to_tape_exit`不是"上游不给"**，是**两侧不同**：时间线侧结构性拒绝（真划界），
>    但manifest侧**槽位已定义、失败关闭、空着等标定**。**解法是排期去现场，不是重谈契约**
>    （plans/15第4.2节，我核过`check-real-assets.mjs`第401—412行）。
> 3. **横漂案例先撞的不是那只轮**：半间隙6.5 mm是现场平轮的，而带材先过的R1槽底只有
>    8.000 mm宽 ⟹ 2.0 mm。临界偏斜角全体缩小3.25倍
>    （5.381/2.489/1.546/0.852° → **1.656/0.766/0.476/0.262°**）。**已回改。**
>
> ### 下一步（plans/15是执行正本）
>
> * **阶段一还差一条**：**总验收案例**——臂动＋张力环＋人手触碰＋两个弯曲轴＋扭转＋
>   椭圆**同时在场**的那一条。三条轨道的产物都在了，但没有一条案例把它们装在一起。
> * **阶段二的前置已做一半**：`tools/model/mesh_aabb.py`落地；
>   **缺`centerline_csv.py`**（0073第五节第2步，纯标准库）——没有它读不了真实中心线，
>   而**35个案例没有一个读过真实工件**。
> * **阶段三是价值闸门**：不发版、不让WDS采纳，前两阶段对项目零价值。
>
> ### 三条会咬人的坑
>
> 1. **源码余量只剩26259字节**（下一件事就会撞上限）。我裁的是**不抬**——
>   门没破，且0064第7.2节记着我上次预抬时分母算错了23倍。
>   **该由撞破它的那个批次带着自己的理由去抬。**
> 2. **并行波的中止线要按"合并后"写，不能按"任一轨道"写**。波次三三条各自都在
>   1000000线下（951899/958355/941667），**合并后1022317、越过22317**——
>   把共享预算按单条判，这是同一类错的第二次（0069第5.1节）。
> 3. **`accept full`现在重到连跑会自己把自己判成不合格**（开跑load 3.45、跑完超15.0）。
>   台账那句"三次取中位数"需要每次之间等负载落回去。
>
> ### 我这两波自己犯的错（都已修，但形态值得记）
>
> * 拆`contact.py`后**只跑了pytest没跑`accept.py`**，而ruff是它第一条命令——红25条，
>   四条轨道谁跑谁红。**门是有的，我绕过去了。**
> * `Callable`那条：**推导脚本算出来了，手抄进文件头时漏了**。
> * 给轨道D的任务书里，我说的"正确做法"（把椭圆拉成圆、径向返回、再变回来）
>   **展开就是我自己点名的那个错**——两条式子逐分量差8.88e-16。它判出来了。
> * 给轨道G的1.6验收判据**是一道空门**：平面绞盘上`z`全钉住 ⟹ 试探力恒在椭圆主轴上，
>   而主轴正是"最近点与径向缩重合"的例外。**按字面做完会得到一道全绿而什么都没验的门。**
> * 冲突面清单漏过三处，其中`spec/15`那张表**不会红、只会静默过期**——
>   0069第一节因此加了一列"**有没有门**"，那一列本仓此前不存在。


> **2026-08-13基线接续（下方2026-08-16段更新为现在时；下文保留历史快照）**：plans/08阶段1—4的
> 最小诚实版本已落。当前机械计数`12/42`、同行C档`6/13`、端到端`0/6`；案例26个。
> **以下1210条与69.0/49.1秒是0060提交时的历史基线，不是0.6.0最终验收**：
> 非batch 1162 passed、2 skipped，batch 46 passed；功能/计时/资源三轴PASS且仓库稳定。
> 单次墙钟不冒充新中位数。
> 源码递归口径646850字节、运行时依赖0。阶段2/3见0055/0057/0058；阶段4先由0059
> 落矩形纤维局部本构，再由[0060](../decisions/0060_WDS运动学截面站点全局装配_20260813.md)
> 把easy-axis曲率从一个WDS式三节点/两边扭角站点生成，并把M与一致切线装入全局
> 残差/Hessian；逐点历史只在全局收敛后提交。独立`Fraction`金标验证加载、切线与
> 回弹，WDS固定提交/两份源SHA夹具验证非平面、非零扭角的曲率、能量与11维梯度。
> 0061随后在两棵独立worktree建立0.6.0/WDS采纳候选；正式交付现已完成：引擎
> `v0.6.0`与两个远程均指向`4d28b928...`，wheelhouse正式wheel SHA为`53114e43...`；
> WDS main `f88986f7...`以默认关闭的单站配置真实调用
> `KirchhoffFiberSectionBending`，只替换R250 v002目标easy-axis，hard-axis、其余站点、
> 全杆Newton与接触仍归WDS，动态与塑性历史失败关闭。原WDS主树的groove-fit改动没有
> 被碰。既有14案已做双版本门：14/14解析字节一致、10个收敛物理指纹一致、3个既有
> 受控失败诊断一致、1个保持input-only；正式性能回执、隔离master V1/V2/VA和开发Mac
> Node门均通过。当前只叫“正式接缝可用”，不叫整个力学域迁移完成。同行主源仍统一
> 指向同一语义：截面点是求积/本构点，增加逐点状态与内存，不增加全局运动学未知量；
> S5.1仍partial，S1.2仍todo。
>
> **2026-08-16接续（R25塑性history第一片已闭合）**：执行正本见
> [plans/12](12_R25塑性history单站采纳计划_20260816.md)。WDS验收树
> `4b7b42a328515a832bde4feacaec3eaff2c14225`
> 已完成固定名义材料顶点64、accepted continuation事务提交/rejected rollback、growth携带、
> 1.2独立plugin与严格history伴随产物。R25 ep10n v002在不启用snap的条件下完成80节点、
> 39个feed步，最终64次提交、58/64个纤维history非零；旧15案身份与解析闭包保持。两份
> 性能正本重签，master受管环境经项目唯一同步入口精确切到正式0.6.0；最终V2与隔离VA
> 均通过。0.6.0公开API足够，本仓没有源码改动或新版本。用户随后授权正式合入，验收树
> 已无冲突快进到WDS main，状态收口提交为`013ffbef90b905e49817a5e95e6f87522101a192`。
> 这只关闭`hypothesis_only`固定单站机制：整杆塑性、多站、snap历史、轴向N/M、hard-axis、
> 扭转、动态和材料标定仍未完成。若出现真实stretch+bending需求，再由边伸长生成
> `epsilon_0`并联立N/M与2×2截面切线；压扁/翘曲或S1.2必须另立独立场未知量，机械积分点
> 与Norris闭式采样不能充数。
>
> **2026-08-16最新接续（R25轴向—弯曲N/M单站已闭合）**：上述真实stretch+bending
> 触发条件随后由用户明确启动，WDS以独立1.3案例/plugin和1.1 history身份完成，不原地
> 改写v002。固定材料顶点64同时消费两相邻边形成的对偶单元轴向应变与引擎公开
> WDS/Bergou easy-axis曲率；0.6.0公开`evaluate_section_response`返回N、M和完整2×2切线，
> WDS负责11维链式装配，并显式减去左右原生edge-stretch各一半避免双计数。真实R25运行
> 完成80节点、39个feed步和64次accepted提交，accepted路径最大绝对轴力超过`1.60N`、
> 轴向—曲率耦合切线超过`1477N·mm`；旧1.2 resolved/dependencies闭包逐字节不变。
> 最终V2为1369 passed/10 skipped、timing 4 passed/1个master无npm明示skip、Mac Node
> 101/101；VA为1369 passed/10 skipped、timing 4/4。最终WDS main与远端均为
> `bef42882de776b231e67d3d09c45f8e6b6981008`。本仓源码、API、wheel、`v0.6.0`tag均未改。
> 下一片不自动推广：只有真实需求到来时，才在整杆多站与hard-axis/扭转之间立新决策；
> 当前结论仍不覆盖snap、动态、压扁/翘曲、实测材料标定或S1.2独立场。

> **2026-08-17接续（绕线机端到端主线，六片全落）**：用户把目标定为
> **绕线机项目的真实物理端到端**，点名"张力控制的过程"与"带材蹭边的可能"。
> 裁决正本[0062](../decisions/0062_绕线机端到端物理主线开工裁决_20260817.md)，
> 执行台账[plans/13](13_绕线机端到端执行与验收_20260817.md)。用户当场拍了三条：
> **主战场在引擎（WDS消费）／目标为机制级（判据取解析靶标，产物永久`hypothesis_only`）／
> 张力控制建到"磁粉离合器+理想PID"层（不复现拿不到参数的ATC600黑箱）**。
>
> 六片全落：**甲1**圆柱侧面罚接触＋**多槽位同时迭代**（兑现0050第一节挂了11天的欠账、
> S3.6的blocker）＋绞盘案例；**甲2**法兰内环面单边接触（蹭边）；**乙1**磁粉离合器＋理想PID；
> **乙2**LTS式传感器与包角换算；**丙1**喂料前沿；**丙2**端到端案例。
> 能力位由`12/42`不变（partial不进位），**场景⑥七位再无todo**（2 done、5 partial），
> 场景⑤ S5.4进partial。**主分母仍是0/6**——全done才算端到端完成，这是清单的算法。
> 案例26→28，`accept full`安静机器上三次中位数**54.8秒**（台账已按合格测量重记）。
>
> **这一轮量出来的三条界，接手要知道它们守着什么**：
>
> 1. **罚接触的精度地板是`k·ulp(间隙表达式里被相减的那个量)`**。半空间那条
>    "跨六个数量级一个ulp都不动"之所以成立，是因为它那个量恰好是0；
>    圆柱上是`k·ulp(R=50mm)`、环带上是`k·ulp(W/2=8.5mm)`。**两次我都先判定
>    "这里没有地板"，两次都被实测否掉。**
> 2. **端到端跑一次抓到了一个单元门永远抓不到的bug**：`PenaltyAnnulusLimit`原先
>    把法兰朝向编码在限位坐标的**符号**里，收线盘横动过原点时符号翻了、蹭边力凭空归零。
>    单元门里槽心恒在原点，那里位置符号与朝向恒等——**补门之前那条注错红0条**。
> 3. **注错测法自己有一条坑**：同字节数的变异（`range(3)`→`range(2)`）会留下一份
>    被`.pyc`当成新鲜的旧缓存，**还原源文件后测试照样红**。注错脚本必须每轮清`__pycache__`。
>
> **审计出三条能力位清单表达不了的缺漏**（plans/13第六节，**不改42这个分母**）：
> 传感器位置≠被控点（μ=0.3、90°包角让落位点张力比设定高**60.2%**，且方向搞反误差是平方）、
> 真机是**双执行器**（ED3L伺服定线速＋POC离合器定张力，S6.3只表达了后者）、
> 多轮路由（R4→R1加两只活动小导轮，`PenaltyCylinderContact`已能表达多圆柱而缺路由层）。
>
> **0.7.0已发**（2026-08-17）：wheel 274678字节、SHA `f105cbae...`、tag `v0.7.0`，
> wheelhouse与两个远程均已推。**WDS尚未采纳**——绕线机端到端主线的能力全在0.7.0里，
> 消费侧切换要走其冻结diff→重签回执→全量门禁。
>
> **下一批的入口是[0063](../decisions/0063_转动自由度进接触的两条路与它们各自解的题_20260817.md)**，
> 它兑现了0051第五节挂着的"两条路的代价没量过"，并**订正了那条触发条件**：
> 按字面等"摇晃/滑移的实测现象"会永远等下去，**因为本仓今天连摇晃这个自由度都没有**。
>
> 0063量出来的两件事：
>
> 1. **物理偏差约四成**，两条独立精确闭式——金字塔`μc`差**1.392倍**
>    （`1/(3√3)`对`2−√3`）、斜面滚球加速度差**1.4倍**。
>    今天的模型不会摇晃，它只是安静地给出一个偏了四成的数；
> 2. **两条路解的不是同一批题**。四元数进不了准静态路径
>    （`solve_equilibrium`只有`fixed_indices`，而四元数是4个数带1个约束）。
>    所以不是选一条：金字塔与斜面阈值只有**路径A**（准静态＋**指数映射**）能兑现，
>    无滑滚球与翻倒只有**路径B**（`rigidbody`接接触）能兑现。
>    路径A的先例仓里已经有——`section_beam`的**边扭角**就是准静态路径上的无约束转动自由度。
>
> **落地顺序见0063第五节**：第1步（路径A、接触力对质心取矩）一次解锁S3.7与C档第7条，
> 而且**它的金标已经用精确有理算术算完了**（research/15第三节那张表就是验收表）。
>
> 另外两个候选原样排在后面：材料注入（WDS称"当前最大的单项缺口"，欧拉式边界，
> 与丙1的拉格朗日喂料不是一回事）；光学阶段5（plans/08，**降级不是取消**，
> 它一位都不碰绕线机，但它是六个场景里唯一剩余能力位不依赖任何结构性缺口的）。

> **2026-08-17晚：目标变了，接手先读这一段。**
>
> 用户把项目定位从"补能力位"改成**用引擎当算法开发的实验台**，两件具体的事：
>
> **甲｜用仿真开发张力算法。** 架构是用户当场定的：**我们自己的张力环跑在Trio上**
> （读`VR451`张力、写`VR481`手动电平），**ATC600设成手动模式只当功率级用**——
> 它内部那个PID完全绕过。**不是**在ATC600外面套外环。
>
> **乙｜几个轮子不共面时的带材偏移**，走**正向**：给各轮偏斜量 → 算偏移。
>
> 另外用户点名要能注入**人手突然碰到带材**这类外部扰动；机械臂按**正常WII场景边走边绕**
> （不是只上下料），所以臂的运动是一等扰动源。
>
> **乙已经有第一块结果**（`cases/roller_skew_lateral_drift`，2026-08-17）：
> **稳态横漂不需要材料输运**——WDS `research/05`把材料注入列为横漂的最大缺口，
> 但那条只对**瞬态**（τ≈3 s）与**持续横走**成立。Shelton正规入轮条件`y'(L)=θ_r`
> **本身就是输运的结果**，写成边界条件后稳态是静力边值问题，引擎今天就能解。
> 产出两个可直接用的东西：**装配公差表**（半间隙6.5 mm下，跨长100/200/300/500 mm
> 的临界轮轴偏斜为5.381°/2.489°/1.546°/**0.852°**，超线性）与一条会改算法设计的结论
> ——**提高张力不会减小稳态横漂，反而略微增大它**（`f(u)`单调增，10→40 N给+11.3%）。
>
> **甲还差五个缺口，第1条是根**：
>
> | # | 缺口 | 为什么挡死 |
> |---|---|---|
> | **1** | **没有线速度／输运** | 真机张力来自放线与收线的**速度差**；今天`drives`里张力纯粹是`M/R`，**一个要抑制速度扰动的控制器在这个模型里无事可做** |
> | 2 | 臂的运动不是扰动源 | 臂动 → 自由段长度与入带角变 → 张力变 |
> | 3 | 人手触碰没有通道 | 用户点名要的扰动 |
> | 4 | 控制器不可插拔 | `PidController`是具体类；要试前馈/增益调度/锥度张力得能换 |
> | 5 | 控制回路与带材求解是分离的 | `winding_line_endtoend`取控制稳态值当力边界条件，开发算法必须耦合 |
>
> **有了1，2与3都退化成"改变自由段几何/长度"的同一类扰动**，4与5是接口工作。
> **建议下一步就打1。**
>
> **乙剩下的**（案例页第六节逐条）：多轮偏斜的累加规则未验；`coulomb_return_map`是
> **各向同性径向返回**，而WDS已证伪它在`μ_∥:μ_⊥ = 5:1`时混合角耗散短缺最高60%、
> **会系统性偏置`e_lat`**（要补摩擦椭圆）；**面外倾斜要扭转（GJ），引擎没有**；
> 横漂进控制回路（两只竖直可调小导轮主动纠偏）是第二个控制回路，未开工。

上一份交接是[plans/05](05_什么才算真实的完成_20260805.md)（2026-08-05）。
它列的六件事已全部收口，本页接在它后面。

## 一、今天的仓在哪（实测，不是印象）

> **下表是2026-08-06的快照，不是现在时。** 2026-08-12实测保留在第十节；
> 当前状态以本页顶部2026-08-13接续与plans/11收口为准。
> 快照保留不改写——**改写快照等于销毁当时的证据**。

| 量 | 值 |
|---|---|
| 版本 | **0.5.0已发**（wheel 193066字节）；此后又改了字节，**要发就是0.6.0** |
| 测试 | 991条（accept full 31.3s/120s，三轴PASS） |
| 案例 | 22条（案例页22/22，0 problems） |
| 源码 | 529440字节，38个`.py`；运行时依赖**0** |
| 决策记录 | 0001—0050（**0021/0036/0037缺号**，0049第八节登记不追补） |
| 远程 | origin与github与HEAD**同一个commit** |

**两条分母**（报的时候必须并排）：

- **主｜用户六场景端到端 0/6**（②④有理想化的部分答案）
- 从｜同行C档 **6/13**，其中**第7、8条各做了一半，都不进位**

## 二、今天做完了什么

**接触从零到六片**（决策0050）：锚点布局（定长、指纹跨步不变）、罚法向
（半空间+球-球）、粘着弹簧、库仑return-map、准静态步进器、曲面法向的多趟迭代。
**三个案例**：斜面阈值`θc=arctan μs`、摩擦迟滞（**引擎第一次记住历史**）、
三球金字塔（**第一个多体接触**）。**瞬态接触也通了**——罚接触+显式积分即DEM形制。

走完`state`→`energies`→`solve`整条路的案例从**3条到6条，其中3条带接触**。

**四条结构性缺口的状态变了**：历史**已解**（0050选了0043三条路之外的第四条）、
接触**开工中**、体积与厚度与耦合**未动**。

**五份并行调研 + 四路对抗审核**，产出[plans/08 子系统补充计划](08_子系统补充计划_20260806.md)
与[research/13 推导与独立核验](../research/13_接触与积分判据的推导与独立核验_20260806.md)。

## 三、2026-08-06当时的下一步——**历史快照：按plans/08第五节，阶段1**

**不要另起炉灶重排。** plans/08的排序原则是"**能让主分母动的优先**"，
理由是从分母涨过好几次而主分母一直是0/6。

1. **阶段1**：把瞬态罚接触从既成事实变成**被声明的能力**——弹跳案例（C档第9条前半，
   无阻尼`e=1`已实测跑通）、给三个积分器的`step_bound`补"接触分辨界"
   （今天写的`h < 2/ω_max`在0.785倍处恢复系数就错14%）、**步长顾问**
   （把`step_bound`那个字符串字段变成可计算的数）
2. **阶段2**：dashpot + 耗散记账 → **C档第9条落地，6/13 → 7/13**
3. **阶段3**：平面围成的漏斗 + 球 → **主分母第一次动，0/6 → 1/6**

**阶段2开工前必须先裁一件**（research/13第4.5节有出处与两条闭式）：
**LSD分离瞬间的拉力截不截断**。不截断⟹`e = exp(−ζπ/√(1−ζ²))`；
截断⟹另一条闭式（临界阻尼给`exp(−2)≈0.135`而不是0）。
**两条都对，钉错了会把一个正确实现判红**，而ζ=0.9处两者差100倍。

**明确不做三件**（plans/08第六节，都带实测理由与重开条件）：
隐式积分族、自适应时间步、通用四面体网格生成。

## 四、今天新立的三道门，接手要知道它们在守什么

| 门 | 守什么 | 为什么建 |
|---|---|---|
| `tools/check_conflict_markers.py` | 未解合并冲突进仓 | main曾带着12行标记，而full全绿 |
| `tools/check_gap_register.py` | 欠账登记了但没被汇总 | 53处触发条件散在18个文件里，没一处能回答"欠了多少" |
| `tests/governance/test_public_surface_registry.py` | 模块不在`__init__`的API清单里 | `contact`与`cli`都曾是未登记的公开面 |

外加`tests/perf/test_budgets.py`两条**台账陈旧度**门——
台账的`measured`此前**没有生产者也没有门**。

## 五、**接手最该带走的三条教训**

### 教训一：同一条洞会被修两三次才修全，因为病根是"有两个来源"

"节点数由哪里说了算"这条洞修了**三次**：先修能量项，再修加速度桥"不崩"，
**而实测桥仍会把重力放到锚点槽上**——因为两处都还在信上下文的质量表。
第三次才把布局立成权威（`StateLayout.node_dof_count`）。

**通则：修完一处，先问"这个假定还有第二处实现吗"。**

### 教训二：门认得"被提到"，认不得"被登记"——这个形态出现了三次

案例页校验器只要求案例名"出现在页上"，于是`peer_fcl_distance`落地后长期挂在
"在建"那句话里；缺口清册门第一版认子串，删掉整行照样绿；
公开面清册门（今天新建的）也被对抗审核指出扫的是全文件不是docstring。

**通则：判据要落在"结构位置"上（表格首格、全限定名），不落在"出现过"上。**

### 教训三：一条从没被必红用例走过的分支，等于一条没有门的分支

域隔离门对**相对import**完全失明，而它有九条必红——**全部用绝对import**。
门全绿不是因为它挡得住，是因为那条分支从没被执行过。

**通则：必红用例要覆盖判据的每个分支，不是每个规则。**

## 六、已知的坑（接手会踩的）

1. **发版会被拒**：`0.5.0已在wheelhouse——版本不可覆盖`。今天0.5.0之后又改了字节，
   **要发得跳0.6.0**（改`pyproject.toml`与`__init__.__version__`，
   并把CHANGELOG的`Unreleased`收成版本条目）。
2. **`accept.py full`现在31秒**（此前21秒），预算120秒。它随案例增长，
   **台账有门守着不许陈旧**——涨了要回来记账。
3. **pre-commit钩子已装**，但它跑的是`--strict --no-git`。
   **`--no-git`是必需的不是放松**：`git-dirty`在提交过程中必然成立，
   不加它这条钩子永远无法通过（0049第九节）。
4. **worktree里跑`rtime-project-check`等于空跑**（路径含`.claude`是跳过目录），
   且**那个校验器扫0个文件仍报"通过"**——对抗审核确认这条前科**未修**，
   已登记未做。真验收要在主仓跑。
5. **接触体是质点+半径，没有力矩**。所以多点接触在今天的自由度下不多算任何物理，
   `max_points > 1`的真前置是**转动自由度**不是网格窄相
   （0050第一节原文挂在形状族上，**那是错的**，plans/08第四节裁决二已订正）。
   ✅ **2026-08-12已补**：独立决策记录见
   [0051](../decisions/0051_多点接触的前置是转动自由度_20260812.md)，
   **`max_points_per_pair`的前置以0051为准，不以0050第一节为准**。

## 七、**没做完的，逐条**

**代码/门**（对抗审核提出的九条。**2026-08-12逐条实测复核，八条已关**）：

| 条 | 状态 |
|---|---|
| `accept.py`三个`check_*`是可选位 | ✅**已关**（0053第二节，四个升必需并补5条治理测试——此前该行为一条测试都没有） |
| `MAY_BE_EMPTY`钉着batch豁免 | ✅**已关**（0056第五节。**判据形态也变了**：从"名单必须等于某常数"改成"名单里的档位必须实测收集到0条"，理由消失豁免自动红） |
| `rtime-project-check`扫0文件仍报通过 | 🔶**外面套了一层**（开工自检断言2），**本体没修**——它是范式脚本的仓内拷贝，改它会与上游漂移。已登记，触发条件：上游更新时提给上游 |
| `check_case_pages`三个洞 | ✅**已关**（0056，新建17条测试——**这道门此前也是一条测试都没有**） |
| 冲突标记只认恰好7连 | ✅**已关**。**实测确认属实**：判据要求`stripped == marker`或`startswith(marker + " ")`，对10连的两条都不成立。改成≥7连，补16条必红（含4条反向必红守着"不许开始对setext标题误红"） |
| 预测-修正趟数用尽没有失败关闭 | ✅**已关**，但**做成选择进入**（`require_pass_convergence=False`）。理由：`yield_excess_n`量的是**修正前**的试探力，`max_passes=1`加滑移时"用尽且残差>0"是**正常且正确**的，无条件抛会打断所有既有调用方 |
| 步进器不校验slot与node | 🔶**做了能证明的一半**。`ContactSlot`与`ContactDeclaration`**都不带node**，完整对应要改0050的布局承重设计。已补四条落位校验（节点越界、负节点、槽指进节点块、槽越过向量末尾），各带必红 |
| 两个接触项没有专属单测 | ⚪**仍未做** |
| `source_bytes`门没有下限 | ✅**已关，而且是08-06那批顺手关掉的**——实测把路径两处都写错，`test_the_recorded_source_bytes_is_not_stale`当场红。**这一行陈旧了三天没人划掉**，本身就是本页第五节教训二的又一个实例 |

**推导**（`research/13`第八节 + 对抗审核补的空白）：

| 条 | 状态 |
|---|---|
| 同轴圆环`dM/dh`未推未验 | ⚪**仍未做**（属阶段6，本轮不在范围） |
| research/13第五节是闭式代闭式、表里没有重力 | ✅**已关**（轨道D）。**三行全错**不是一行，真值/原值是**2.398 / 1.104 / 1.010倍**。该节升到三条腿：闭式、独立RK4、**引擎实测** |
| `ζ ≥ 1`两条闭式都没写 | ✅**已关**（轨道D，research/15）。三段闭式 + 19档ζ独立数值对拍最大偏差**5.27e-15**。**两条改变结论的**：恢复系数**没有绝对下界**（`exp(−2)`只是ζ<1分支的下界，全轴值域是开区间(0,1)）；**约定A在ζ≥1上根本没有有限根**，故用户选的力归零约定是过阻尼区唯一能用的 |
| 弹跳级数的临界入射速度截断 | 🔶`cases/bouncing_ball_restitution`的驱动函数对此**失败关闭**（4N步内不结束就抛，消息写明是那件事的兜底）。**研究页上的那条截断说明仍未补** |
| 稳定判据`ρ ≤ 1`的重根洞 | ⚪**仍未做** |
| 金字塔`μc = 2−√3` | ✅**已关**（轨道D）。**精确成立，差0.0e+00**——Q(√3)上精确有理算术，**秩恰为8、解唯一**，故"静定"这句话本身也复核了。**顺手撞出一条原推导没提的前提**：两底球恰好相切是真实存在的第五个接触，它不传力是因为顶球把它们往外推；**这条不成立则问题超静定，那个闭式就没了** |

**文档**：~~plans/08第四节裁决二要落到0050第一节，那份独立决策记录还没写~~
✅ **2026-08-12已补**（[0051](../decisions/0051_多点接触的前置是转动自由度_20260812.md)）。

## 八、一句话

**接触从零变成了"能算静定的多体摩擦平衡与瞬态弹跳"，这是实打实的；
但主分母从头到尾没动过。** 下一步该做的不是再加案例，
是把plans/08阶段1—3走完，让那个0/6变成1/6。

## 九、2026-08-09后续架构分析（不改第三节排序）

用户要求补做GCW、WII、WDS、FTS四项目的多表示模型、固定场景、前端/Blender、硬件
适配与端到端提速分析。已新增：

- [架构与数据流入口](../architecture.md)：区分仓内依赖图与产品级数据流；
- [research/14 多表示模型资产与硬件适配架构](../research/14_多表示模型资产与硬件适配架构_20260809.md)：
  记录S1—S8固定场景族、共同底座、硬件三类后端、性能口径与反证；
- [plans/10 模型资产与呈现架构演进计划](10_模型资产与呈现架构演进计划_20260809.md)：
  记录阶段0—6、退出门与待裁决点。

三份文档均为**研究/候选计划**：没有新facet、代码、依赖或跨仓迁移已经发生；也不替代
本页第三节与plans/08阶段1—3。后续若用户批准实施，仍先过两消费方、面清册、必红用例和
旧产物逐字节不变四道边界。

### 2026-08-09范围纠正：资产层不是总产品

用户进一步明确：目标不是把物理结果交给Blender或其他软件，也不只是一套物理仿真工作台，
而是建设**自有的工程设计与计算平台**；外部工具只能作可替换backend/adapter。已新增
[platform/README](../platform/README.md)及00—05六页，重新确定：

- `physics-engine`是无头科学计算内核，不吞前端和重型依赖；
- 平台另有自有工程工作流、视觉交互内核、产品设计系统与RTime Engineering Studio；
- 工程模型、多表示资产、计算任务、时间线、工作区、插件和部署形成闭环；
- GCW、WII、WDS、FTS是首批纵切与反证样板；
- Blender、OCCT、Three.js、VTK、USD等只是adapter/backend/格式；
- research/14与plans/10降为平台的资产/端点专项，不再代表总开工顺序。
- `physics-engine`可在通过迁移门后保留历史进入上位monorepo，但不是普通文件移动；
  GCW、WII、WDS、FTS先保持独立仓，以插件、adapter和版本化制品接入。

旧spec/00“前端栈不统一”与最新方向存在范围冲突。本轮只登记推荐口径——统一runtime、
资源生命周期、workspace、交互和插件接口，不强制统一领域renderer/shader/picking语义——
没有静默改spec；进入实现前必须另留决策记录。当前物理主线和本页第三节排序仍不变。

## 十、2026-08-12：七条裁决落地、旧并行副本清空（**第三节排序不变**）

用户定下这一轮的**总原则**：**最真实永远是首要目标，其次是性能。**
下面每一条裁决凡遇权衡都按它排序——**它当场改掉了一处选型**（见0052第一节）。

### 已落

- **[0051](../decisions/0051_多点接触的前置是转动自由度_20260812.md)**：
  兑现本页第六节第5条登记的欠账；`max_points_per_pair`的前置**以0051为准**；
- **[0052](../decisions/0052_阶段1至3开工前的七条裁决_20260812.md)**：
  plans/08阶段1、2、3三个"先裁"清单一次性收口。七条摘要：

  | # | 裁决 | 最要紧的后果 |
  |---|---|---|
  | 1 | LSD分离瞬间的拉力**截断**（判据=力归零） | 同行三家逐字相同的反解式**不能照抄**（它是从不截断式反解的），要数值求根；且**可声明的`e`下界是0.1353** |
  | 2 | 主分母改成**能力位**，报"第几位/共几位" | 顺带兑现plans/07第四节"没有门看着分子"与0049"C档13条做成清单"两笔账 |
  | 3 | 耗散记账**给注册表加类型标签** | 不破"注册表里全是势能"这条不变量，也不开第二条装配路径；**求和次序不许变** |
  | 4 | 保二阶的积分步**新增一个，老的不动** | 既有轨迹逐字节不变（进仓三前提第三条） |
  | 5 | 步长顾问放`integrate.py`，**只吃纯数字** | `integrate.py`零包内import这条独立性保住（实测确认今天只有4行stdlib import） |
  | 6 | `production_ready`**先写死翻转条件，这轮不翻** | 四条翻转条件见0052第六节，今天一条都没全中 |
  | 7 | 阶段3**先小规模跑通，如实报级别** | "加速档边界移到能量装配"**首次被显式登记**——plans/08七个阶段里没有它 |

- **本仓独立复核了恢复系数的两条闭式**（0052第一节）：10万点单调性、极限值、
  ζ=0.9处**101.91倍**（与plans/08记的"100倍"一致）。
- **旧并行副本清空**：`.claude/worktrees/`里17个工作树 + 18个分支全部删除，
  释放55MB。删前逐个确认**独有提交为0**。
  **删它们不只是省盘**——见下。

### 新发现的一条坑（比本页第六节那四条更狠）

**站在worktree里跑测试，测的是主仓的代码，不是worktree自己的。**
本页此前只说"worktree内跑`rtime-project-check`等于空跑"，
2026-08-12把机理测穿了：共享`.venv`里editable安装的`.pth`内容是
**主仓`src`的绝对路径**，所以worktree里`import physics_engine`
**加载的是主仓的包**（实跑验证，打出来的路径就是主仓）。

实测17个副本里**14个**带着这条软链。含义是：
**一个代理可以在副本里跑完整套门、全绿、交差，而它验的是别人的代码。**
AGENTS.md把这条写成过警告，现在它有机理和实物证据。

**下一次并行开工前必须先建"开工自检"**（四条断言：HEAD对齐、`PYTHONPATH`生效、
`import`指向本树、`rtime-project-check`扫到的文件数>0），否则并行产出的绿不可信。

### 并行的冲突面：从3个长到11个

0041为2026-08-05那一波清了3个共享面，**那一波仍然冲突5次**，形态是
AGENTS.md记的"同点插入"。2026-08-12按近40个提交的改动频次重新测绘，
今天要写一行的地方是**11个**：CHANGELOG(20次)、`cases/README.md`(11)、
域隔离门(5)、plans/07缺口清册(5)、性能台账(4)、README两条分母表(3)、
`__init__.py`公开面清册(3)、spec/15域登记(3)、决策记录编号、
`engine_facets.py`(21个面)、公开面清册门。
**其中6个0041没覆盖，3个是0041之后才长出来的门。**

### 今天没做（下一批的开工内容）

1. **开工自检脚本**（甲类，不建就产生假绿）；
2. **11个冲突面的预分配表**（0041形制扩容）；
3. **`accept.py`三个可选位升为必需**——今天文件不在就打印`absent`然后PASS，
   多代理下这等于门是虚的（本页第七节第1条，仍未做）。
