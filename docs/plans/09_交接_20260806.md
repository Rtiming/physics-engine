# 交接（2026-08-06）

**接手先读这一页，再按AGENTS.md的“开工必读”往下。**

> **2026-08-13最新接续（本段是现在时；下文保留历史快照）**：plans/08阶段1—4的
> 最小诚实版本已落。当前机械计数`12/42`、同行C档`6/13`、端到端`0/6`；案例26个。
> **以下1210条与69.0/49.1秒是0060提交时的历史基线，不是0.6.0最终验收**：
> 非batch 1162 passed、2 skipped，batch 46 passed；功能/计时/资源三轴PASS且仓库稳定。
> 单次墙钟不冒充新中位数。
> 源码递归口径646850字节、运行时依赖0。阶段2/3见0055/0057/0058；阶段4先由0059
> 落矩形纤维局部本构，再由[0060](../decisions/0060_WDS运动学截面站点全局装配_20260813.md)
> 把easy-axis曲率从一个WDS式三节点/两边扭角站点生成，并把M与一致切线装入全局
> 残差/Hessian；逐点历史只在全局收敛后提交。独立`Fraction`金标验证加载、切线与
> 回弹，WDS固定提交/两份源SHA夹具验证非平面、非零扭角的曲率、能量与11维梯度。
> 0061随后在两棵独立worktree建立0.6.0/WDS采纳候选；正式交付现已完成：引擎
> `v0.6.0`与两个远程均指向`4d28b928...`，wheelhouse正式wheel SHA为`53114e43...`；
> WDS main `f88986f7...`以默认关闭的单站配置真实调用
> `KirchhoffFiberSectionBending`，只替换R250 v002目标easy-axis，hard-axis、其余站点、
> 全杆Newton与接触仍归WDS，动态与塑性历史失败关闭。原WDS主树的groove-fit改动没有
> 被碰。既有14案已做双版本门：14/14解析字节一致、10个收敛物理指纹一致、3个既有
> 受控失败诊断一致、1个保持input-only；正式性能回执、隔离master V1/V2/VA和开发Mac
> Node门均通过。当前只叫“正式接缝可用”，不叫整个力学域迁移完成。同行主源仍统一
> 指向同一语义：截面点是求积/本构点，增加逐点状态与内存，不增加全局运动学未知量；
> S5.1仍partial，S1.2仍todo。
>
> **2026-08-16接续**：用户已选择先准备R25塑性history这一片，执行正本见
> [plans/12](12_R25塑性history单站采纳计划_20260816.md)。第一实现先在WDS消费侧完成
> 固定材料站点、continuation接受点事务提交/回滚、growth携带和history结果回传；
> `physics-engine 0.6.0`现有公开trial/next-state能力先做充分性探针，不预设改源码或重发。
> 若出现真实stretch+bending需求，再由边伸长生成`epsilon_0`并联立N/M与2×2
> 截面切线。压扁/翘曲或
> S1.2都必须另立独立场未知量；机械积分点与Norris闭式采样不能充数。所有2026-08-13
> 新增的正式交付事实以上述提交/tag/wheel哈希为准。

上一份交接是[plans/05](05_什么才算真实的完成_20260805.md)（2026-08-05）。
它列的六件事已全部收口，本页接在它后面。

## 一、今天的仓在哪（实测，不是印象）

> **下表是2026-08-06的快照，不是现在时。** 2026-08-12实测保留在第十节；
> 当前状态以本页顶部2026-08-13接续与plans/11收口为准。
> 快照保留不改写——**改写快照等于销毁当时的证据**。

| 量 | 值 |
|---|---|
| 版本 | **0.5.0已发**（wheel 193066字节）；此后又改了字节，**要发就是0.6.0** |
| 测试 | 991条（accept full 31.3s/120s，三轴PASS） |
| 案例 | 22条（案例页22/22，0 problems） |
| 源码 | 529440字节，38个`.py`；运行时依赖**0** |
| 决策记录 | 0001—0050（**0021/0036/0037缺号**，0049第八节登记不追补） |
| 远程 | origin与github与HEAD**同一个commit** |

**两条分母**（报的时候必须并排）：

- **主｜用户六场景端到端 0/6**（②④有理想化的部分答案）
- 从｜同行C档 **6/13**，其中**第7、8条各做了一半，都不进位**

## 二、今天做完了什么

**接触从零到六片**（决策0050）：锚点布局（定长、指纹跨步不变）、罚法向
（半空间+球-球）、粘着弹簧、库仑return-map、准静态步进器、曲面法向的多趟迭代。
**三个案例**：斜面阈值`θc=arctan μs`、摩擦迟滞（**引擎第一次记住历史**）、
三球金字塔（**第一个多体接触**）。**瞬态接触也通了**——罚接触+显式积分即DEM形制。

走完`state`→`energies`→`solve`整条路的案例从**3条到6条，其中3条带接触**。

**四条结构性缺口的状态变了**：历史**已解**（0050选了0043三条路之外的第四条）、
接触**开工中**、体积与厚度与耦合**未动**。

**五份并行调研 + 四路对抗审核**，产出[plans/08 子系统补充计划](08_子系统补充计划_20260806.md)
与[research/13 推导与独立核验](../research/13_接触与积分判据的推导与独立核验_20260806.md)。

## 三、2026-08-06当时的下一步——**历史快照：按plans/08第五节，阶段1**

**不要另起炉灶重排。** plans/08的排序原则是"**能让主分母动的优先**"，
理由是从分母涨过好几次而主分母一直是0/6。

1. **阶段1**：把瞬态罚接触从既成事实变成**被声明的能力**——弹跳案例（C档第9条前半，
   无阻尼`e=1`已实测跑通）、给三个积分器的`step_bound`补"接触分辨界"
   （今天写的`h < 2/ω_max`在0.785倍处恢复系数就错14%）、**步长顾问**
   （把`step_bound`那个字符串字段变成可计算的数）
2. **阶段2**：dashpot + 耗散记账 → **C档第9条落地，6/13 → 7/13**
3. **阶段3**：平面围成的漏斗 + 球 → **主分母第一次动，0/6 → 1/6**

**阶段2开工前必须先裁一件**（research/13第4.5节有出处与两条闭式）：
**LSD分离瞬间的拉力截不截断**。不截断⟹`e = exp(−ζπ/√(1−ζ²))`；
截断⟹另一条闭式（临界阻尼给`exp(−2)≈0.135`而不是0）。
**两条都对，钉错了会把一个正确实现判红**，而ζ=0.9处两者差100倍。

**明确不做三件**（plans/08第六节，都带实测理由与重开条件）：
隐式积分族、自适应时间步、通用四面体网格生成。

## 四、今天新立的三道门，接手要知道它们在守什么

| 门 | 守什么 | 为什么建 |
|---|---|---|
| `tools/check_conflict_markers.py` | 未解合并冲突进仓 | main曾带着12行标记，而full全绿 |
| `tools/check_gap_register.py` | 欠账登记了但没被汇总 | 53处触发条件散在18个文件里，没一处能回答"欠了多少" |
| `tests/governance/test_public_surface_registry.py` | 模块不在`__init__`的API清单里 | `contact`与`cli`都曾是未登记的公开面 |

外加`tests/perf/test_budgets.py`两条**台账陈旧度**门——
台账的`measured`此前**没有生产者也没有门**。

## 五、**接手最该带走的三条教训**

### 教训一：同一条洞会被修两三次才修全，因为病根是"有两个来源"

"节点数由哪里说了算"这条洞修了**三次**：先修能量项，再修加速度桥"不崩"，
**而实测桥仍会把重力放到锚点槽上**——因为两处都还在信上下文的质量表。
第三次才把布局立成权威（`StateLayout.node_dof_count`）。

**通则：修完一处，先问"这个假定还有第二处实现吗"。**

### 教训二：门认得"被提到"，认不得"被登记"——这个形态出现了三次

案例页校验器只要求案例名"出现在页上"，于是`peer_fcl_distance`落地后长期挂在
"在建"那句话里；缺口清册门第一版认子串，删掉整行照样绿；
公开面清册门（今天新建的）也被对抗审核指出扫的是全文件不是docstring。

**通则：判据要落在"结构位置"上（表格首格、全限定名），不落在"出现过"上。**

### 教训三：一条从没被必红用例走过的分支，等于一条没有门的分支

域隔离门对**相对import**完全失明，而它有九条必红——**全部用绝对import**。
门全绿不是因为它挡得住，是因为那条分支从没被执行过。

**通则：必红用例要覆盖判据的每个分支，不是每个规则。**

## 六、已知的坑（接手会踩的）

1. **发版会被拒**：`0.5.0已在wheelhouse——版本不可覆盖`。今天0.5.0之后又改了字节，
   **要发得跳0.6.0**（改`pyproject.toml`与`__init__.__version__`，
   并把CHANGELOG的`Unreleased`收成版本条目）。
2. **`accept.py full`现在31秒**（此前21秒），预算120秒。它随案例增长，
   **台账有门守着不许陈旧**——涨了要回来记账。
3. **pre-commit钩子已装**，但它跑的是`--strict --no-git`。
   **`--no-git`是必需的不是放松**：`git-dirty`在提交过程中必然成立，
   不加它这条钩子永远无法通过（0049第九节）。
4. **worktree里跑`rtime-project-check`等于空跑**（路径含`.claude`是跳过目录），
   且**那个校验器扫0个文件仍报"通过"**——对抗审核确认这条前科**未修**，
   已登记未做。真验收要在主仓跑。
5. **接触体是质点+半径，没有力矩**。所以多点接触在今天的自由度下不多算任何物理，
   `max_points > 1`的真前置是**转动自由度**不是网格窄相
   （0050第一节原文挂在形状族上，**那是错的**，plans/08第四节裁决二已订正）。
   ✅ **2026-08-12已补**：独立决策记录见
   [0051](../decisions/0051_多点接触的前置是转动自由度_20260812.md)，
   **`max_points_per_pair`的前置以0051为准，不以0050第一节为准**。

## 七、**没做完的，逐条**

**代码/门**（对抗审核提出的九条。**2026-08-12逐条实测复核，八条已关**）：

| 条 | 状态 |
|---|---|
| `accept.py`三个`check_*`是可选位 | ✅**已关**（0053第二节，四个升必需并补5条治理测试——此前该行为一条测试都没有） |
| `MAY_BE_EMPTY`钉着batch豁免 | ✅**已关**（0056第五节。**判据形态也变了**：从"名单必须等于某常数"改成"名单里的档位必须实测收集到0条"，理由消失豁免自动红） |
| `rtime-project-check`扫0文件仍报通过 | 🔶**外面套了一层**（开工自检断言2），**本体没修**——它是范式脚本的仓内拷贝，改它会与上游漂移。已登记，触发条件：上游更新时提给上游 |
| `check_case_pages`三个洞 | ✅**已关**（0056，新建17条测试——**这道门此前也是一条测试都没有**） |
| 冲突标记只认恰好7连 | ✅**已关**。**实测确认属实**：判据要求`stripped == marker`或`startswith(marker + " ")`，对10连的两条都不成立。改成≥7连，补16条必红（含4条反向必红守着"不许开始对setext标题误红"） |
| 预测-修正趟数用尽没有失败关闭 | ✅**已关**，但**做成选择进入**（`require_pass_convergence=False`）。理由：`yield_excess_n`量的是**修正前**的试探力，`max_passes=1`加滑移时"用尽且残差>0"是**正常且正确**的，无条件抛会打断所有既有调用方 |
| 步进器不校验slot与node | 🔶**做了能证明的一半**。`ContactSlot`与`ContactDeclaration`**都不带node**，完整对应要改0050的布局承重设计。已补四条落位校验（节点越界、负节点、槽指进节点块、槽越过向量末尾），各带必红 |
| 两个接触项没有专属单测 | ⚪**仍未做** |
| `source_bytes`门没有下限 | ✅**已关，而且是08-06那批顺手关掉的**——实测把路径两处都写错，`test_the_recorded_source_bytes_is_not_stale`当场红。**这一行陈旧了三天没人划掉**，本身就是本页第五节教训二的又一个实例 |

**推导**（`research/13`第八节 + 对抗审核补的空白）：

| 条 | 状态 |
|---|---|
| 同轴圆环`dM/dh`未推未验 | ⚪**仍未做**（属阶段6，本轮不在范围） |
| research/13第五节是闭式代闭式、表里没有重力 | ✅**已关**（轨道D）。**三行全错**不是一行，真值/原值是**2.398 / 1.104 / 1.010倍**。该节升到三条腿：闭式、独立RK4、**引擎实测** |
| `ζ ≥ 1`两条闭式都没写 | ✅**已关**（轨道D，research/15）。三段闭式 + 19档ζ独立数值对拍最大偏差**5.27e-15**。**两条改变结论的**：恢复系数**没有绝对下界**（`exp(−2)`只是ζ<1分支的下界，全轴值域是开区间(0,1)）；**约定A在ζ≥1上根本没有有限根**，故用户选的力归零约定是过阻尼区唯一能用的 |
| 弹跳级数的临界入射速度截断 | 🔶`cases/bouncing_ball_restitution`的驱动函数对此**失败关闭**（4N步内不结束就抛，消息写明是那件事的兜底）。**研究页上的那条截断说明仍未补** |
| 稳定判据`ρ ≤ 1`的重根洞 | ⚪**仍未做** |
| 金字塔`μc = 2−√3` | ✅**已关**（轨道D）。**精确成立，差0.0e+00**——Q(√3)上精确有理算术，**秩恰为8、解唯一**，故"静定"这句话本身也复核了。**顺手撞出一条原推导没提的前提**：两底球恰好相切是真实存在的第五个接触，它不传力是因为顶球把它们往外推；**这条不成立则问题超静定，那个闭式就没了** |

**文档**：~~plans/08第四节裁决二要落到0050第一节，那份独立决策记录还没写~~
✅ **2026-08-12已补**（[0051](../decisions/0051_多点接触的前置是转动自由度_20260812.md)）。

## 八、一句话

**接触从零变成了"能算静定的多体摩擦平衡与瞬态弹跳"，这是实打实的；
但主分母从头到尾没动过。** 下一步该做的不是再加案例，
是把plans/08阶段1—3走完，让那个0/6变成1/6。

## 九、2026-08-09后续架构分析（不改第三节排序）

用户要求补做GCW、WII、WDS、FTS四项目的多表示模型、固定场景、前端/Blender、硬件
适配与端到端提速分析。已新增：

- [架构与数据流入口](../architecture.md)：区分仓内依赖图与产品级数据流；
- [research/14 多表示模型资产与硬件适配架构](../research/14_多表示模型资产与硬件适配架构_20260809.md)：
  记录S1—S8固定场景族、共同底座、硬件三类后端、性能口径与反证；
- [plans/10 模型资产与呈现架构演进计划](10_模型资产与呈现架构演进计划_20260809.md)：
  记录阶段0—6、退出门与待裁决点。

三份文档均为**研究/候选计划**：没有新facet、代码、依赖或跨仓迁移已经发生；也不替代
本页第三节与plans/08阶段1—3。后续若用户批准实施，仍先过两消费方、面清册、必红用例和
旧产物逐字节不变四道边界。

### 2026-08-09范围纠正：资产层不是总产品

用户进一步明确：目标不是把物理结果交给Blender或其他软件，也不只是一套物理仿真工作台，
而是建设**自有的工程设计与计算平台**；外部工具只能作可替换backend/adapter。已新增
[platform/README](../platform/README.md)及00—05六页，重新确定：

- `physics-engine`是无头科学计算内核，不吞前端和重型依赖；
- 平台另有自有工程工作流、视觉交互内核、产品设计系统与RTime Engineering Studio；
- 工程模型、多表示资产、计算任务、时间线、工作区、插件和部署形成闭环；
- GCW、WII、WDS、FTS是首批纵切与反证样板；
- Blender、OCCT、Three.js、VTK、USD等只是adapter/backend/格式；
- research/14与plans/10降为平台的资产/端点专项，不再代表总开工顺序。
- `physics-engine`可在通过迁移门后保留历史进入上位monorepo，但不是普通文件移动；
  GCW、WII、WDS、FTS先保持独立仓，以插件、adapter和版本化制品接入。

旧spec/00“前端栈不统一”与最新方向存在范围冲突。本轮只登记推荐口径——统一runtime、
资源生命周期、workspace、交互和插件接口，不强制统一领域renderer/shader/picking语义——
没有静默改spec；进入实现前必须另留决策记录。当前物理主线和本页第三节排序仍不变。

## 十、2026-08-12：七条裁决落地、旧并行副本清空（**第三节排序不变**）

用户定下这一轮的**总原则**：**最真实永远是首要目标，其次是性能。**
下面每一条裁决凡遇权衡都按它排序——**它当场改掉了一处选型**（见0052第一节）。

### 已落

- **[0051](../decisions/0051_多点接触的前置是转动自由度_20260812.md)**：
  兑现本页第六节第5条登记的欠账；`max_points_per_pair`的前置**以0051为准**；
- **[0052](../decisions/0052_阶段1至3开工前的七条裁决_20260812.md)**：
  plans/08阶段1、2、3三个"先裁"清单一次性收口。七条摘要：

  | # | 裁决 | 最要紧的后果 |
  |---|---|---|
  | 1 | LSD分离瞬间的拉力**截断**（判据=力归零） | 同行三家逐字相同的反解式**不能照抄**（它是从不截断式反解的），要数值求根；且**可声明的`e`下界是0.1353** |
  | 2 | 主分母改成**能力位**，报"第几位/共几位" | 顺带兑现plans/07第四节"没有门看着分子"与0049"C档13条做成清单"两笔账 |
  | 3 | 耗散记账**给注册表加类型标签** | 不破"注册表里全是势能"这条不变量，也不开第二条装配路径；**求和次序不许变** |
  | 4 | 保二阶的积分步**新增一个，老的不动** | 既有轨迹逐字节不变（进仓三前提第三条） |
  | 5 | 步长顾问放`integrate.py`，**只吃纯数字** | `integrate.py`零包内import这条独立性保住（实测确认今天只有4行stdlib import） |
  | 6 | `production_ready`**先写死翻转条件，这轮不翻** | 四条翻转条件见0052第六节，今天一条都没全中 |
  | 7 | 阶段3**先小规模跑通，如实报级别** | "加速档边界移到能量装配"**首次被显式登记**——plans/08七个阶段里没有它 |

- **本仓独立复核了恢复系数的两条闭式**（0052第一节）：10万点单调性、极限值、
  ζ=0.9处**101.91倍**（与plans/08记的"100倍"一致）。
- **旧并行副本清空**：`.claude/worktrees/`里17个工作树 + 18个分支全部删除，
  释放55MB。删前逐个确认**独有提交为0**。
  **删它们不只是省盘**——见下。

### 新发现的一条坑（比本页第六节那四条更狠）

**站在worktree里跑测试，测的是主仓的代码，不是worktree自己的。**
本页此前只说"worktree内跑`rtime-project-check`等于空跑"，
2026-08-12把机理测穿了：共享`.venv`里editable安装的`.pth`内容是
**主仓`src`的绝对路径**，所以worktree里`import physics_engine`
**加载的是主仓的包**（实跑验证，打出来的路径就是主仓）。

实测17个副本里**14个**带着这条软链。含义是：
**一个代理可以在副本里跑完整套门、全绿、交差，而它验的是别人的代码。**
AGENTS.md把这条写成过警告，现在它有机理和实物证据。

**下一次并行开工前必须先建"开工自检"**（四条断言：HEAD对齐、`PYTHONPATH`生效、
`import`指向本树、`rtime-project-check`扫到的文件数>0），否则并行产出的绿不可信。

### 并行的冲突面：从3个长到11个

0041为2026-08-05那一波清了3个共享面，**那一波仍然冲突5次**，形态是
AGENTS.md记的"同点插入"。2026-08-12按近40个提交的改动频次重新测绘，
今天要写一行的地方是**11个**：CHANGELOG(20次)、`cases/README.md`(11)、
域隔离门(5)、plans/07缺口清册(5)、性能台账(4)、README两条分母表(3)、
`__init__.py`公开面清册(3)、spec/15域登记(3)、决策记录编号、
`engine_facets.py`(21个面)、公开面清册门。
**其中6个0041没覆盖，3个是0041之后才长出来的门。**

### 今天没做（下一批的开工内容）

1. **开工自检脚本**（甲类，不建就产生假绿）；
2. **11个冲突面的预分配表**（0041形制扩容）；
3. **`accept.py`三个可选位升为必需**——今天文件不在就打印`absent`然后PASS，
   多代理下这等于门是虚的（本页第七节第1条，仍未做）。
