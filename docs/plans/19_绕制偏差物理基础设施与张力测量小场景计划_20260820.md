# 19 绕制偏差物理基础设施与张力测量小场景计划

日期：2026-08-20
状态：**执行中。P0、P1和T-M0/T-M1已实现；T-M2、T-M3、P3、P4待后续批次。**

地基裁决：[0096](../decisions/0096_绕制偏差物理基础设施归本仓与WDS单向消费边界_20260820.md)。
研究正本：[research/19](../research/19_绕制偏差物理基础设施与上游模型接入调研_20260820.md)。
首批实现裁决：[0097](../decisions/0097_测力轮张力测量物理与T-M0_T-M1_20260820.md)。

## 零之一、2026-08-20执行进度

| 项 | 状态 | 已落内容 |
|---|---|---|
| P0输入与观测语义 | **完成首片** | 六层量分字段；`tension_measurement_sample/0.1`draft面；严格回读、重算派生量和内容哈希 |
| P1测量几何 | **完成首片** | 一般三维矢量合力、敏感轴、tare、gross/net、显式支承份额；旧`TensionSensor`外层组合 |
| T-M0 | **完成** | 0°、60°、90°、180°、镜像与闭式独立oracle |
| T-M1 | **完成** | 单支承、双对称支承、非对称份额、安装轴与tare独立oracle |
| T-M2 | **未完成正式案例** | mV/ADC组合路径已有单元门；五点正反程标定、留出点、采样时延仍缺 |
| T-M3 | **未开始** | 小型放线—收线动态链仍是下一片 |
| P3/P4 | **未开始** | official WII时间线、真实GCW/WII输入闭包、WDS采用与真实短窗口均未动 |

本进度不改变17/42和端到端0/6。两个合成静力案例永久`hypothesis_only`，不能替代现场LTS安装识别与标定。

验收状态：本机与srv01的功能轴均通过，srv01还完成Linux x86_64 full全套功能；但本机quick为63.0s/30s、srv01 full为244.6s/180s，均属timing FAIL。master的`--test-only`预计到2026-08-30才能起跑，本批没有绕过SLURM提交。因此0097的功能面可继续作为下一片基础，master四轴绿仍是待补回执，不得写成已完成。

## 零、目标

先把能够承接GCW/WII模型事实、计算带材/张力/接触、并预测真实张力传感器读数的物理基础设施建在physics-engine内。WDS只作为消费方采用，不在本批修改WDS、WII或GCW代码。

第一条可验收纵切：

```text
已知两侧张力/方向
→ 测力轮合力
→ LTS敏感轴读数
→ mV与ADC
→ 小型放线—收线动态链
```

## 一、开工前的当前事实

| 项 | 当前 |
|---|---|
| 能力账 | 17/42；端到端0/6 |
| 已有张力案例 | `capstan_tension_ratio`、`free_span_tension_step`、`closed_loop_tension_step`、`winding_line_endtoend` |
| 当前传感器模型 | 满量程、mV、ADC；没有合力几何和敏感轴 |
| GCW接入 | 22列中心线reader已落；真实资产选择进入 |
| WII接入 | 通用位姿时间线已落；official `wii_motion_timeline.v2`产物仍缺 |
| WDS采用 | 钉0.8.0，但只调用0.6.0时期的单站能力 |
| 性能 | 当前批档热点主要在既有张力/杆案例；新小场景要控制重复计算 |

## 二、阶段P0：先冻结输入与观测语义

### P0.1 物理量分层

在代码前写清并用类型/字段隔离：

1. `span_tension_n`：带材跨段张力；
2. `roller_resultant_force_n_xyz`：带材对测力轮的合力；
3. `sensor_axis_force_n`：敏感轴分量；
4. `zeroed_bridge_output_mv`：声明tare已经清零后的理想桥路输出；
5. `digitized_net_axis_force_n`：量化后的net敏感轴力；
6. `displayed_span_tension_n`：标定增益换算后的显示跨段张力；
7. `estimated_span_tensions_n`：未来观测器输出，必须带不确定度和可观测性。

任何层不得复用同一个裸字段名`tension`。

本片不声明原始gross桥路、tare在模拟/数字哪一侧扣除或gross载荷下的物理过载；这些属于T-M2现场电气/标定结构。当前复用的旧`TensionSensor`只代表“tare已经清零后的正标量”路径，字段名必须带`zeroed`，不得冒充未清零桥路。

### P0.2 候选facet

代码批开工时先在`engine_facets.py`登记草案面，候选：

- `winding_physics_model_input`：只引用上游模型/中心线/timeline和物理参数；
- `tension_measurement_configuration`：测力轮、敏感轴、支承、tare和电气链；
- `tension_measurement_sample`：真值、合力、轴向力、mV、ADC、证据等级；
- `winding_physics_case`：小场景输入与oracle绑定。

名字和字段须在代码批决策页中最终裁，不以本计划自动冻结。

### P0.3 必红合同

- 缺单位、frame、SHA、用途或证据等级；
- visual资产进入collision；
- WII timeline投影时丢E1、workpiece或process frame；
- 单个LTS声明直接测得T0—T4；
- 未标定参数产物声称`measured`；
- 同一个字段同时表示真值张力和传感器显示值。

## 三、阶段P1：张力测量几何

### P1.1 一般矢量平衡

输入：入口/出口切向、两侧张力、敏感轴、导轮重力、支承配置。

输出：web合力、总载荷、敏感轴分量、横向分量、每个支承分配。

判据：

1. 任意二维构型对直接矢量和逐位或声明容差对拍；
2. 等张力时退化到`2*T*sin(beta/2)`；
3. 0°包角合力为0；180°包角合力为2T；
4. 敏感轴转90°时读数退化到0；
5. 镜像构型的模长相同、横向符号相反；
6. 双对称支承各承担一半；非对称支承没有输入就失败关闭。

必须红：入口力符号反、使用包角补角、拿模长冒充敏感轴分量、忘记tare、敏感轴非单位。

### P1.2 电气链适配

保留`drives.TensionSensor`旧公开行为，新增完整测量模型在其外层组合，不原地改变旧类输入语义。

判据：

- 旧`TensionSensor.read_n()`全部逐位不变；
- 完整链的mV和ADC对手算闭式；
- 五点标定拟合只使用标定点，留出点单独裁决；
- 超量程、反向力、零点和tare状态显式；
- 未声明ADC位数、采样周期或时延失败关闭。

## 四、阶段P2：四个小场景

| 场景 | 问题 | 独立oracle | 退出条件 |
|---|---|---|---|
| T-M0静力轮 | 已知T与包角时传感器承受多大力 | 矢量平衡+`2*T*sin(beta/2)` | 0/30/60/90/180°全过，镜像与符号门全过 |
| T-M1安装/自重 | 安装角、导轮重量、单双支承怎样改变读数 | 刚体静力分解 | tare前后、敏感轴、支承分配均能判红 |
| T-M2电气测量 | 合力怎样变成mV/ADC/显示值 | 线性比例+量化半台阶 | 旧类逐位不变，新链有独立闭式 |
| T-M3收放线 | 放线制动力矩+收线速度如何形成张力和读数 | 自由跨线性ODE/频率/稳态 | 开环、好/坏控制器、速度阶跃和半径变化均有分辨力 |

每个场景一份`case.md`、`oracle.json`、生成器、测试和已知失效清单；物理实现与金标生成器不得共享被验函数。

## 五、阶段P3：上游模型输入闭包

### P3.1 GCW

复用`tools/model/centerline_csv.py`，把选择进入从松散环境变量升级成一份明确输入闭包：

- bundle/centerline/source SHA；
- assembled frame；
- `delivery_allowed`；
- centerline CSV/meta；
- collision资产引用；
- 单位、角色和误差声明。

不复制真实资产进仓。测试使用合成小件；真实包走opt-in并在回执中写来源SHA。

### P3.2 WII

只消费official timeline和模型资产引用：

- A1—A6、E1、workpiece与process frame；
- `material_feed_length_mm`和严格递增`time_s`；
- `model_id`、`kinematics_hash`和源资产SHA；
- collision/visual角色；
- 证据等级和不确定度。

当前工作台轨迹或selected joint path不直接进入物理。WII若尚未发布official产物，只允许合成fixture和`hypothesis_only`，不得把接口测试写成真实回放完成。

### P3.3 WDS

P0—P3代码全部在physics-engine完成。WDS采用另开消费批：

1. WDS只增加调用/适配，不复制物理；
2. 不修改GCW/WII；
3. 既有WDS产物默认逐字节不变；
4. 新场景用独立plugin/案例身份，默认关闭；
5. WDS自己的全量门与证据水印单独执行。

## 六、阶段P4：与真实绕线链合拢

依赖P1—P3全部通过后，才把测量模型接入：

```text
GCW中心线/槽帧
+ WII official运动/送带时间线
+ WDS路线侧车和标定参数
→ physics-engine杆/接触/输运/张力/测量
→ 物理结果与预测传感器读数
→ WDS代表窗口与偏差统计
```

第一条合拢场景仍只做短窗口，不恢复完整多匝DER承诺。至少同时输出：模型张力、测力轮合力、LTS预测读数、接触状态、落位偏差和证据等级。

## 七、现场标定门

以下参数不到位时，相关结果永久`hypothesis_only`：

1. LTS实际单/双传感器与支承方式；
2. 测力轮入口/出口切向、实际包角和敏感轴；
3. 导轮/轴承组件重量和tare方法；
4. 5kg/20mV实际线性、零点、回程、温漂和动态响应；
5. VR451到工程单位的比例、滤波、采样周期与延迟；
6. 参考张力传感器或已知砝码的溯源；
7. R4—R1与活动导轮的真实路线、半径、轴向和摩擦。

本计划不授权采集或写控制器；现场动作另行审批。

## 八、性能与运行地

- P1静力与电气小场景必须是交互级；具体预算在首次实现后按三次中位数登记，不在计划里编秒数；
- T-M3属于本机批级，先复用现有自由跨与闭环案例，避免每条断言重复跑相同时间序列；
- 多角度/多参数扫描上master，案例级并行优先于GPU；
- CUDA后端等测量/装配热点形成显式数组内核后再评审，仍守profile、等价、端到端≥2×三门槛；
- Mac CPU路径和零依赖参考实现始终是第一公民。

## 九、预定代码归属与冲突面

代码批开工前再裁具体文件。当前只分责任面：

| 面 | 归属 |
|---|---|
| 测力轮矢量平衡与读数模型 | mechanics域，新模块候选，不塞进`sensors.py`声明层 |
| 既有LTS量化 | `drives.TensionSensor`保持兼容 |
| 上游模型reader | `tools/model/`与薄内核声明，不导入上游源码 |
| 案例 | `cases/tension_*`独立目录 |
| 跨边界面 | `engine_facets.py`，由主线统一登记 |
| 能力账/README/CHANGELOG | 批末统一收口，不由轨道并行修改 |

需要特别避免的同点插入：`engine_facets.py`、`docs/capability_ledger.json`、`README.md`、`cases/README.md`、`CHANGELOG.md`和性能台账。

## 十、批次验收

### 10.1 每片

- 对应测试先红后绿；
- 旧公开行为逐位不变或按声明容差对拍；
- 物理实现与oracle不共享代码；
- 未知参数失败关闭或保留`hypothesis_only`；
- 靶向测试在正确解释器和正确checkout运行。

### 10.2 批末

```bash
.venv/bin/python tools/accept.py full
python3 tools/rtime-project-check.py . --strict
git diff --check
```

另跑模型选择进入和案例生成器复现；master墙钟只更新为同机同分区同核数三次中位数。

### 10.3 不能拿来代替验收的

- 画面看起来像绕线机；
- 能打开WII模型；
- LTS输出约等于设定值；
- 一个合成场景绿；
- GPU利用率变高；
- WDS只换了依赖版本。

## 十一、停止条件

遇到下列任一情况停在当前阶段，不用假设补洞：

- 真实LTS支承/敏感轴与候选模型不一致；
- official WII timeline仍不存在；
- GCW包`delivery_allowed=false`或frame不闭合；
- 一个LTS对T0—T4的可观测性秩不足；
- 小场景无法区分符号错、包角错或tare错；
- 新物理只能通过放宽既有残差/穿透/适用域门；
- GPU后端没有满足三门槛。
