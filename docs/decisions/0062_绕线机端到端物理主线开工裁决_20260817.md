# 0062 绕线机端到端物理主线的开工裁决

日期：2026-08-17
状态：已裁决，执行台账见[plans/13](../plans/13_绕线机端到端执行与验收_20260817.md)

## 一、问题

用户2026-08-17把目标从"补能力位"改成一个**具体的机器**：绕线机的实际物理要能
仿真出来——原话点名两件事，**张力控制的过程**与**带材蹭边的可能**。

这句话落到能力位清单上不是泛指。它精确覆盖场景⑥的S6.3（执行器物理）、S6.4
（传感器读出）、S6.5（带材过导向轮的接触）、S6.6（蹭边：带材碰法兰）、S6.7
（端到端），外加场景⑤的S5.4（喂料与移动前沿）。**主分母0/6一直不动，动它的
入口就在这六位上**——而不在此前推荐的光学线上（光学一位都不碰绕线机）。

真机链路是有实测记录的，不是设定：`winding-machine/HARDWARE_TOPOLOGY.md`记
LTS1-5悬臂张力传感器（5kg、0—20mV）→ ATC600张力控制器（**内部PID闭环**）→
BOSENSE TBA420功放（0—10V入、0—24V/4A出）→ POC-050L对轴型磁粉离合器 → 卷材
张力对象；ED3L-08AEA伺服经POC耦合收/放线轴，当前配置在CSP位置模式。

## 二、用户当场裁的三条

| # | 裁决 | 最要紧的后果 |
|---|---|---|
| 1 | **主战场是引擎，WDS消费** | 导轮接触、蹭边、执行器/传感器物理、喂料前沿在`physics-engine`原生长成通用机制；WDS改为调用。兑现0015"把物理搬进来"，代价是WDS已跑通的粘滑/自由轮/移动边界要在引擎侧重做一遍 |
| 2 | **机制级先跑通，不冲定量** | 判据一律取**解析靶标**（绞盘闭式、互补条件、镜像反对称、闭环阶跃闭式、能量耗散账），不取现场数据。产物永久标`hypothesis_only` |
| 3 | **张力控制建到"磁粉离合器+理想PID"层** | 建电流→扭矩→张力的被控对象与理想PID；**不复现ATC600的实际回路** |

裁决2的出处不是保守，是WDS`research/05`第三节已经点清的事实：轮有效接触半径、
带-轮μ_∥/μ_⊥、轮轴实际偏斜角、WII物理时间线与`tool_to_tape_exit`标定、带材
EA/EI/GJ与密度——**五项只有现场实测能补，代码堆不出来**，且横漂幅值对其中三项
是一阶敏感的。在它们补齐前，任何定量数字只有机制意义。

裁决3的出处：ATC600无公开的完整通信/参数/寄存器手册（`ED3L_张力控制资料包`
2026-06-03已核，官网只有一页产品资料），POC-050L的L后缀线圈参数厂家资料也没有。
**复现一个拿不到参数的黑箱回路，写出来的PID系数是编的**——按0052通则，
猜出来的裁决比不裁更坏，这里同理：猜出来的控制器比没有控制器更坏，
因为它看起来像真的。

## 三、0034第四节的触发条件已到达——重开并重新裁决

0034第四节原文写明触发条件：**"WDS碰撞预演批次或case2给出带法兰导轮的书面需求
时，本条重开并另立决策记录。"** 用户2026-08-17点名"带材蹭边"就是那份书面需求。
本节即所立的记录。

**裁决：仍然不改spec/11的形状词汇。** 但理由与0034当时不同——0034当时是"没有
真实牵引"，今天牵引来了，改成"**牵引来了，而它要的东西不在词汇层**"：

1. 蹭边接触要的是法兰的**内环面**（带材跑偏后边缘顶上去的那个面），不是带法兰
   圆柱这个复合形；
2. `modelgen.generate_spool`按0032已经把带法兰带盘**精确分解**为`barrel`、
   `flange_low`、`flange_high`三件独立`FiniteCylinder`，法兰贴在筒两端外侧
   `|z| ∈ [W/2, W/2+w]`，三件互不重叠，且产出的每件上`flange_outer_radius_mm`
   恒为`None`。**内环面就是`z = ±W/2`这张平面限制在环带`r ∈ [R_筒, R_法兰]`上**——
   既有原语已经表达得了；
3. 因此本轮新增的词汇在**接触侧**（环带限位面），进`contact.py`，
   **不进`shapes.py`**。

**同时维持`geometry.mass_properties`对带`flange_outer_radius_mm`的圆柱失败关闭。**
那条缺口问的是"带法兰圆柱的质量分布"，而接触求解不需要复合体的惯量——
**它的触发条件（有人要算带法兰圆柱的质量属性）没有到来**，不能借这一轮顺手关掉。
把两件事分开，是因为0034把它们写在同一节里，而它们的牵引不是同一个。

## 四、两条轨道与文件所有权

用户选了"两条并行"。**并行按文件所有权切，不按主题切**（plans/11第二节形制），
且本轮**不派子代理、不开worktree**：AGENTS.md第77节记着共享`.venv`的editable
`.pth`指向主仓`src`，副本里`import physics_engine`加载的是主仓的包（2026-08-12
实测17个副本中14个带这条软链），**一个代理可以在副本里跑完整套门、全绿、交差，
而它验的是别人的代码**。单树内按轨道交替落地，文件所有权照样分开，冲突面为零，
且每次绿都验的是本树。

| 轨道 | 独占文件 | 位 |
|---|---|---|
| **甲 接触线** | `contact.py`、`tests/test_contact*.py`、`cases/capstan_*`、`cases/flange_rub_*` | S6.5、S6.6 |
| **乙 控制线** | `actuators.py`、`sensors.py`、`tests/test_actuators.py`、`tests/test_sensors.py`、`cases/tension_control_*` | S6.3、S6.4 |
| 汇合 | `scene.py`、`cases/winding_line_endtoend` | S6.7、S5.4 |

共享面按AGENTS.md"同点插入"那条**预先分配行位**：CHANGELOG新条目一律插在
`## Unreleased`下第一条之前；`cases/README.md`按案例名字典序插；
`capability_ledger.json`按位ID就地改`status`不新增位；`__init__.py`公开面清册
按模块名字典序插。

## 五、判据来源——"机制级"靠这张表兑现，不靠自称

| # | 靶标 | 档 | 守哪一位 |
|---|---|---|---|
| J1 | 绞盘（Euler-Eytelwein）`T₂/T₁ = e^{μθ}` | 第1档解析 | S6.5：带材绕圆柱的粘滑摩擦 |
| J2 | 单边接触互补条件：`g > 0 ⟹ f ≡ 0` | 零容差 | S6.6：蹭边不许两侧同时出力 |
| J3 | `±θ_r`轮轴偏斜的镜像反对称 | 零容差（符号）＋量级 | S6.5：横漂机制 |
| J4 | 一阶对象＋PI闭环阶跃响应闭式（超调、时间常数、静差） | 第1档解析 | S6.3：张力控制过程 |
| J5 | 扭矩—张力换算`T = M/R(n)`，`R`随匝数生长 | 第1档解析 | S6.3＋S5.4 |
| J6 | 能量耗散账：摩擦耗散＝`∫f·v_slip dt`，与总能量残差对账 | 已有`DissipationTerm`形制 | 全轨道 |
| J7 | 传感器只读真测得到的量（R8/R9已有门） | 结构 | S6.4 |

**J1是本轮判据强度最高的一条**：绞盘公式是精确闭式，且它同时验法向罚接触、
库仑return-map、粘滑分界与包角积分四件事——一条判据穿四层。

## 六、诚实边界（同批写进模块docstring与案例页，不写在这里就算没写）

1. **杆模型原理上看不见边缘载荷下的板屈曲模态**（WDS`research/04`第5.3节，
   `research/05`第2.3节复述）。所以蹭边**只给事件区间、边缘力密度与滑距的上界
   估计**，不给"会不会屈曲"。真定量必须过壳FE交叉裁决，那不在本仓；
2. 全部产物标`hypothesis_only`，理由是第二节裁决2的五项实测缺口；
3. 本轮的带材**没有扭转自由度**（`section_beam`只有一个easy-axis站点），
   所以边缘点由中心线加半宽生成时，**假定材料标架不绕切线转**。这是一条声明
   不是一个实现细节——扭转是0029已登记的欠账，触发条件是本条被真实需求顶穿；
4. 接触体仍是质点＋半径、**没有力矩**（0051）。本轮不改这条，
   所以导轮自旋是**给定的运动学**（`motion`时间线），不是解出来的动力学。

## 七、本轮明确不做

| 不做 | 理由 |
|---|---|
| 复现ATC600实际PID | 第二节裁决3：参数拿不到，写出来是编的 |
| 带材扭转自由度 | 0029已登记；本轮用第六节第3条的声明绕开，顶穿了再开 |
| 接触体转动自由度 | 0051；导轮自旋走`motion`给定，不解动力学 |
| 网格窄相 | 本轮全部接触对象是解析原语（圆柱、环带面、半空间） |
| 壳/板单元 | 第六节第1条：那是交叉裁决的活，不是本仓的 |
| 光学FFT（原推荐的阶段5） | 它一位都不碰绕线机。**降级不是取消**，plans/08阶段5原样留着 |

## 八、顺带修的一道门：陈旧度判据把墙钟放进了功能路径

轨道甲收口时`tests/perf/test_budgets.py::test_the_recorded_accept_timing_is_not_wildly_out_of_date`
红了一次又绿了一次，**而代码一个字没动**。查清楚不是片状，是这道门本身有问题。

`tests/perf/test_budgets.py`的开头写着：

> **墙钟不进门**……这三条都不受宿主负载影响，因此可以放心进功能路径——
> 0014法则2禁止的是"负载改变功能结论"。

**而这道门把墙钟放进了功能路径。** 2026-08-17实测：同一份代码、同一套测试，
机器空闲时回执157.9秒（比值2.71，绿），邻居项目GCW开跑后186.3秒（比值3.20，红）。
**负载改变了功能结论**，正是那条法则禁止的、且是那个文件自己开头声明禁止的事。

更难看的一层：**运行器已经宣布过那个数没有意义**——同一份回执里写着
`performance = NOT_EVALUATED`、`performance_reason = resource_unqualified`、
`resource = UNQUALIFIED`。**一道门拿另一道门明文作废的数去判红，判出来的红不带信息。**

**裁决**：判据补一条前置——回执必须`resource == "QUALIFIED"`，否则跳过并说明。
判据本体抽成纯函数`accept_timing_staleness`，三个结局各带一条必红
（不合格回执不许判红／合格回执照样判红／回执缺`elapsed_s`必须判红）。
**这不是放松**：记账义务一条没少，只是不再由一个作废的数触发，
且"要在安静机器上重测"已按触发条件登记进缺口清册。

## 九、轨道乙落地时的两条裁决

### 9.1 执行器物理不长在`actuators.py`里

0038登记的欠账原文是"`actuators`的`apply`物理——落地那天它会import `state`，
域登记要同批改"。**兑现了，但没有按那个形状兑现。**

`actuators`登记在spec/15的**基座**`scene`圈，而基座**不依赖任何物理域**。
把`apply`的物理塞进去，等于让基座反向依赖力学——那正是0035那次抓到的
"方向反了"，当时的判词是**"我最初把它登记成基座接口，是把愿望当成了事实"**。

**裁决**：物理长成独立模块`drives.py`进力学域，单向import `actuators`；
`actuators`继续只做声明与时延机械，**它那套门一条没动、逐字节不变**。

**代价如实写**：spec/10的`Actuator.apply`这个方法名今天仍然不存在。
将来若有消费方按接口名调用，要么在外面套适配层，要么重裁`actuators`的环归属
——**那是一次决策，不是一次重构**。

### 9.2 安培后缀是`amp`不是`a`

spec/14第一节早就登记过"单位后缀基础集**一个电学单位都没有**——有开尔文`k`、
有瓦特`w`，**没有安培**"，0048把它挂成欠账，触发条件写的是
"第一个要问这卷带材的Ic是多少的消费方出现时"。

**来的是另一扇门**：磁粉离合器的线圈电流。债是同一笔，触发条件的措辞太窄。

**裁决**：加`amp`，不加SI符号`a`。理由是实测——仓里已有**15个**以`_a`结尾的名字，
其中`body_a`、`name_a`、`node_a`、`box_a`、`segment_a`、`other_a`一类是
**成对命名**（`_a`/`_b`），根本没有单位。加`a`会让它们全部通过单位检查，
**那等于把轴2规则3废掉**。已有的单字母后缀（`n`/`m`/`s`/`w`/`k`/`c`）没有这个问题，
因为没有"以它们结尾的成对命名"这个惯例。`_amp`加入时实测**零个已有名字命中**，
是一次纯扩张。有一条门守着`body_a`一类仍被拒——**它守的是我没有走那条省事的路**。

`identity`是稳定倾向档，但本次是**纯扩张**（只让此前被拒的名字通过，
不拒任何此前通过的名字），故不触发弃用缓冲。0048剩下的那半
（spec/14规则1的真bug）没有动，触发条件不变。

## 十、后续触发条件

- **扭转**：边缘点位置的误差被判据顶穿时（表现为J3镜像反对称在大包角上破缺），
  重开0029那条，另立决策记录；
- **转动自由度**：导轮自旋要从"给定"变成"解出来"时（表现为需要算轴承阻力矩
  与张力的耦合），按0051重开；
- **定量**：第二节裁决2的五项实测任一到位时，对应判据从解析靶标升到对拍，
  `hypothesis_only`按项摘除——**摘一项写一行，不许整体摘**；
- **`geometry.mass_properties`带法兰失败关闭**：第三节已说明它的触发条件与本轮
  无关，仍等"有人要算带法兰圆柱的质量属性"。
