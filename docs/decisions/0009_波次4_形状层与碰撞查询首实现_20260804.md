# 0009 波次4：形状声明层与碰撞查询首实现（2026-08-04深夜）

状态：已决并执行（用户"接着345一起做完"授权）。

## 落了什么

- `physics_engine.shapes`：spec/11的首个实现——解析原语（Sphere/Capsule/
  RoundedBox/FiniteCylinder含法兰）、`MeshAsset`（内容寻址+单位+usage+
  **凸性显式声明**）、`GeneratedShape`（`algorithm:`前缀身份）、
  `CollisionShape`（**保守方向必填**envelope/fitted）、`SimBody`
  （碰撞形必填、外观缺省单行道）、`PosedBody`（八角点保守世界AABB）。
- `physics_engine.collision`：spec/10 `CollisionQuery`首实现——broad-phase
  AABB查询，事件一律`confidence="broad_phase"`且`penetration_mm=None`；
  装配期校验（重复body/未知白名单对当场炸）；白名单按对声明。
- 9条测试，**spec/11规则7的四条红例全部在册并验红**：visual资产进碰撞→拒、
  无效哈希→拒、保守方向缺省→拒、生成器无algorithm前缀→拒。
- 示例场景用真实参数：R1导轮（槽底R45/法兰R60/半宽9，
  `roller_flange_profile.v1`实值）、带盘R50（当晚spool场景实值）、
  KUKA第三连杆collision网格**真SHA**、圆角盒工装；broad phase恰好找到
  唯一相接对，白名单声明后归零。

## 边界

- narrow phase与`check_path`（沿MotionSource扫掠）未实现——事件可信度
  字段的存在意义就是让这件事诚实。
- spec/10第五节、spec/11升v0.2；**仍不冻结**：冻结要等消费方（WDS碰撞
  预演批次）拿它跑过自己的门禁+另一消费方评审。
- WDS对象集的正式声明文件（含骨架laydown与全臂链节）归碰撞预演批次，
  本批的示例场景是其雏形。
