# 0064 并行波次二：冲突面清零，以及整杆各向异性杆里那两条真决策

日期：2026-08-17
状态：**已裁决**。用户口径"先把缺口调查完毕，已确定的缺口开展下去，
派代理并行干，先把项目模块化一点"。

本页做三件事：**清冲突面**（0041是模板）、**分配槽位**、
**把整杆各向异性杆里两条不该由执行者临时决定的事先裁掉**。
勘察正本是[plans/14](../plans/14_现场场景勘察_真实工件是非平面槽_20260817.md)。

## 一、已经先做掉的一件：`contact.py`拆子包

原文件2157行，波次二里有三件活同时落它里面。**并行的代价不在合并冲突，
在改200行要先读2157行。** 按依赖单向切开
（`errors ← layout ← {penalty, friction} ← {damping, stepper}`），
函数体逐字节搬运，公开名原样再导出。提交`7331fad`，全量绿。

**两条登记都不用动**，因为二者都按子包名匹配：`ENGINE_RINGS`按最长前缀匹配，
公开面清单的`_package_modules()`同时收顶层`.py`与含`__init__.py`的目录。
这一点是查过的，不是猜的。

`energies.py`（1276行）**不拆**。波次二只有一条轨道碰它，而那条轨道的产物
是新模块不是改它——**为一个不存在的冲突拆文件就是预支**（0001第二前提）。

## 二、四个共享冲突面，逐个预处理

| 冲突面 | 为什么会撞 | 本页的处理 |
|---|---|---|
| `src/physics_engine/__init__.py`的公开面清单 | 每条轨道加新模块都要在同一段插一行——**同点插入，分槽规则挡不住**（AGENTS.md第93—96行，2026-08-05因此冲突五次） | **本页预插四行占位**（第四节），轨道只改自己那一行的内容 |
| `CHANGELOG.md`的`## Unreleased` | 同上 | **本页预建四个二级小节**，轨道只写自己那节 |
| `docs/decisions/`编号 | 并发写`00NN`必然撞号 | **预分配0065—0068**（第三节） |
| `benchmarks/engine_budgets.baseline.json`的`measured` | 四条各自增长、各自重记 | **轨道禁止改此文件**，收口时由主代理复测一次 |

`docs/capability_ledger.json`**不预分配**：本波次四条轨道**没有一条**会让某位
从`partial`进`done`（理由见各轨道的验收条），所以它不该被碰。
**乱改分母比缺漏本身更坏**——这条是plans/13第六节立的。

源码上限**不预抬**：现行1048576、实测734991，余量313585字节，
`ceiling/measured = 1.427`小于0053给的1.6调回线。0041那次预抬是因为
五条轨道会各自撞破；本次余量是实测增量的四十倍，**预抬等于把绊线拆了**。

## 三、四条轨道与编号预分配

| 号 | 轨道 | 主要文件 | 与谁不重叠 |
|---|---|---|---|
| 0065 | **A｜整杆各向异性弯曲＋扭转** | 新`rod.py`＋`state.py`读 | 只它碰材料帧 |
| 0066 | **B｜线速度与输运进`drives`** | `drives.py`、`feed.py` | 只它碰`drives` |
| 0067 | **C｜落位点几何层** | 新`laydown.py`＋`motion.py`读 | 只它碰`motion` |
| 0068 | **D｜各向异性摩擦（η-return）** | `contact/friction.py` | 只它碰`contact/` |

**四条各自的主要文件互不相交**——这不是运气，是第一节那次拆分与
"新能力落新模块"这条共同保证的。

## 四、A轨道里那两条真决策（**先裁，不许执行者临时决定**）

plans/14第3.1节量出来：WDS那套里有两条**不是移植、是决策**。
它们各自有一个"看起来更对"的选项，而那个选项都会在几百行之后才暴露代价。

### 4.1 梯度/Hessian：**先走jet，测了再说**，不许一上来写解析

两个选项：

* **解析闭式**——本仓`DiscreteElasticBending`的先例，常数因子最好；
  但各向异性版是11个变量、两个曲率分量、还含边扭角γ，二阶解析链式法则**很难写对**；
* **二阶jet**（`section_beam.py`的`_Jet2`，零依赖tuple-of-float）——好写对，
  但11变量每次运算分配11×11的tuple-of-tuples，逐顶点逐迭代。

**裁决：A轨道用`_Jet2`，并把它从`section_beam.py`的模块私有提升成共享模块。**

理由不是"jet更好"，是本仓自己的性能条款第一句：**优化先profile**。
一上来写解析等于在没有任何实测的情况下押注常数因子，而**押错的代价是几百行难验的二阶导**。
先用好写对的那条拿到正确答案与一条真实的墙钟，那时"要不要解析"才是一个有分母的问题。

**同批必须留下的东西**：A轨道要报一个**逐顶点能量项求值的墙钟中位数**，
并与既有`DiscreteElasticBending`的同规模数并排。没有这两个数，
下一次关于解析的讨论仍然没有分母。

### 4.2 自由度次序：**先按WDS的"γ在末尾"写，同批实测带状还触不触发**

WDS把γ块整个放向量末尾，于是一个内顶点同时耦合`x[i−1..i+1]`与`γ[i−1..i]`，
带宽`O(3N)`。本仓`solve.py`第203—215行的带状判据是**结构性**的
（半带宽要小于规模三分之一才走带状）。照抄会打掉带状快路——
而**WDS没有带状快路，所以它没有理由不这么放**。

**裁决：先按"γ在末尾"实现，但同批必须实测并报告三个数**：
该布局下的实测半带宽、带状路径是否触发、以及触发与不触发两条路的墙钟。

**不预先改成交错布局。** 理由同4.1：交错是一次不可逆的形制选择
（`StateLayout`的节点块语义、`fingerprint()`的打包次序都跟着变），
而"带状会不会失效"是可以**先量出来**的。**量之前改形制，就是把猜测冻成契约。**

### 4.3 另外三条不给选项，直接照做

1. **retransport外层循环由调用方承担**。因为`m_ref`冻结成常数，`TwistEnergy`
   对位置没有依赖——**单次`solve_equilibrium`里弯曲与扭转的交换根本不存在**。
   A轨道必须同批交出"求解→重输运→重建context→再求解"的外层循环，
   **且必须有一个"抄了公式不抄外循环就红"的门**。这是本轮最容易被漏掉的一条。
2. **`natural_kappa2`要显式开通道**。WDS有这个槽但生产里恒为零，**它从没跑过**。
3. **离散曲率与物理曲率的单位约定连着抄**（`κ`是已在对偶元上积分过的无量纲量），
   混用是静默的`l̄`倍错误。

### 4.4 测试债不许一起继承

WDS**没有任何测试验证各向异性弯曲的行为**，其验证协议因为没有oracle而
**明确拒绝**建各向异性对比算例。A轨道必须自己立三道门，**后两道WDS没有**：

| 门 | 判什么 | 为什么它是真的 |
|---|---|---|
| 螺旋线运动学 | `κ = R/(R²+p²)`、`τ = p/(R²+p²)`，且**`κ2`在螺旋线上必须是机器零** | 闭式，与被验内核独立 |
| **易/难轴互换** | 参考`d1`转90°，挠度比**必须等于**`EI_hard/EI_easy` | WDS自己点名了这个失效模式（取错`d1`挠度差1600倍、**不报任何错**）却没有门 |
| **闭式扭转** | 端扭矩杆`θ = TL/GJ` | 五行就能写，全仓今天没有任何扭转金标 |

## 五、收口时主代理要做的四件

1. **复跑每条轨道申报的必红门**——轨道自己说"验过红"不算数，
   合并后的树上再红一次才算数（轴6规则5：四条合并后那棵树变了）；
2. 复测源码字节，一次性重记`measured`；
3. `accept.py full`跑一次，三轴都要看；
4. 把四条各自量出来的墙钟并排，**再回来看4.1与4.2那两条裁决要不要翻**。

## 六、本页明确没裁的

* **`roller_skew_lateral_drift`的半间隙订正**（plans/14第3.4节，临界角要缩小3.25倍）
  ——它要回改案例页与`oracle.json`，与四条轨道都不相干，**另立一批**，不塞进本波次。
* **锥面槽壁**（真实槽壁是两段外倾锥面，本仓`PenaltyAnnulusLimit`是平面环带）
  ——形制对不上是实的，但它属于接触族的下一片，本波次不动。
* **`entry_boundary`要不要由本仓定义**——上游WII用测试断言这个字段必须缺席，
  并把它划给消费侧。那是跨仓契约的事，不由本页裁。
* **A轨道之后要不要改成解析梯度**——见4.1，**要等那两个数**。
