# 0090 网格碰撞窄相：EPA、MPR、距离场查询的三选一，以及S3.2那三个词是三道题

日期：2026-08-18
状态：**已裁决**。本页是[plans/18](../plans/18_基础设施批次二_六场景剩余前置_20260818.md)
第二节轨戊的落地记录，也是[0052第八节](0052_阶段1至3开工前的七条裁决_20260812.md)
那张"今天裁会是猜"的表上**第一条被销号的**待裁项。

0052第八节给的不裁理由原文是："plans/08明写**用数字裁不要用原则裁**——
先量MPR近似误差与罚刚度柔度误差谁大。两个数都没测。"
**本页不测那两个数，而且要说清楚为什么不测**：见第二节末段。

## 零、先更正派活书里的一个数

派活书（plans/18转述0085）写着"距离场对解析SDF的逼近误差随分辨率**四阶**"。
**那是错的**，0085第三节原文是"值、梯度、Hessian都是**二阶**"，
第九节9.2那一列比值4.0012／4.0002是**二阶的实测出口**（h减半、误差降4倍＝二阶），
不是四阶。**四阶要预滤波，而0085明写不做**（窄带块稀疏没有"整条带"可解三对角系统）。

这条更正照0089第四节那条纪律办：**派活书里的数是一句转述，不是结论。**
本页下面凡用到"场的精度"处，一律按二阶写。

## 一、裁决一：S3.2的`missing`里那三个词是**三道题**，不是一道

S3.2的`missing`原文是"`collision.py`只有球/胶囊族窄相；**圆柱/盒/网格窄相为零**；
plans/08阶段7的EPA还是MPR是0052第八节明写『今天裁会是猜』的待裁项"。

**把"圆柱/盒"与"网格"写在同一个逗号里，是这一格四个月没动的根本原因**——
它们的拦路虎完全不同：

| 题 | 拦路虎 | 本页怎么办 |
|---|---|---|
| **圆柱／盒** | 没有拦路虎。它们在`shapes.py`里是**参数化原语**不是网格，有符号距离**有闭式**，精确到舍入 | **今天闭合**（第四节） |
| **网格** | **内核不读网格字节**。`MeshAsset`只带路径＋SHA-256＋声明的AABB，**一个顶点都没有** | 三条路全都够不着；只能由外部烘好的场经**协议**进来（第五节） |
| **一般凸体对凸体的穿透深度**（胶囊对盒等） | 要么GJK族要么迭代，两条本页都不走 | **不做**，保持`broad_phase`，登记GAP |

**第二行是本页最要紧的一句**：EPA、MPR、距离场查询三条路争的是"拿到几何之后怎么算"，
而本仓卡在"几何进不来"。0052第八节把这一格记成"EPA还是MPR"的算法选型题，
**那个记法本身就把病灶记错了位置**。

## 二、裁决二：三条路各自解不了的题

三条路都按0074第一节那条"几何判定与接触力学是两道题"读——
本页问的是**几何判定**这一侧：给两个位姿好的体，分离量是多少。

### 2.1 EPA（GJK之后挖穿透深度）——出局

| 解不了的题 | 出处 |
|---|---|
| **只答穿透那一半**。分离构型要先跑GJK；EPA本身给不出分离距离。而本仓既有窄相是**一个数两用**（`separation < 0`才报事件，同一个数既是间隙也是穿透），换成GJK+EPA就是两套代码两条精度 | `collision.py`现行实现 |
| **要凸体**。非凸就要先凸分解，而**不vendored凸分解库已裁**（Bullet被V-HACD钉死五年、上游已EOL并导流CoACD的判例） | 0073第七节第1条 |
| 而本仓真实件**全部非凸**：凸性单侧检验通过率**0.15%／3.72%／26.19%** | 0074第二节第2条实测 |
| **多面体扩张的数值脆点要容差**（退化单纯形、共面与近共面面片），而**本仓给不出那个容差的来源**——给一个就是猜 | AGENTS.md诚实可信度条款 |
| **它需要真实几何**（顶点集或支撑函数），本仓的`MeshAsset`不带 | `shapes.py`的`MeshAsset`字段表 |

### 2.2 MPR（Minkowski Portal Refinement）——出局

| 解不了的题 | 出处 |
|---|---|
| **同样要凸体**：portal细化的收敛性建在Minkowski差的凸性上。撞同一堵墙 | 0073第七节第1条＋0074第二节第2条 |
| **需要一个内点**（体心）。对一个只有AABB声明的`MeshAsset`，本仓拿不到——**AABB中心不是体心**，非凸件上它可能根本不在体内 | `shapes.py`；0074第二节第2条 |
| **它给的是沿一条射线的穿透深度，一般不是最小平移距离**——是近似 | MPR的定义 |
| **同样需要真实几何** | 同上 |

**0052第八节要的那两个数（MPR近似误差与罚刚度柔度误差谁大），本页不测。**
理由不是"懒得测"，是**测了不改结论**：MPR在前置上（要凸体、要内点、要几何）
已经三条全不满足，那两个数量的是"如果能用它，它准不准"。
**如实写在这里比测一个用不上的数诚实**——这也是0052第八节自己那条通则的正读法：
"裁决要有出处或有实测"，本页给的是出处。

### 2.3 距离场查询——**选它，但它解不了的题一样多**

| 解不了的题 | 出处 |
|---|---|
| **它不给"形对形"，只给"点对场"**。φ(x)−r读成穿透深度**只对球型探针成立**；一般形状取min φ给的是**面到面的距离**，不是最小平移穿透深度。于是本页的场那条路**只接球型探针**，胶囊对场当场拒 | 本页第五节 |
| **分辨率误差O(h²)，且不是随机的**：对凸障碍**系统性偏松**（场报出来的距离比真值大，物体沉得更深），主项h²/(3ρ) | 0085第三节＋`cases/sdf_contact_convergence` |
| **窄带外失败关闭，不能外推**。0085实测"远在体外"与"深在体内"两种点的信号**一模一样**（都是`None`）——存下来的字节里没有任何一个带符号 | 0085第四节 |
| **内存按表面积长**；且块粒度**吃掉窄带的节省**（实测同球同盒h=0.5只省到**0.9010**） | 0074第5.1节＋0085第五节GAP第四行 |
| **内核不读网格字节**：场必须离线烘（`tools/model/sdf_bake/`，pcu独立venv）再传进来。于是"拿一个任意STL来就能查窄相"**今天做不到** | 0074第五节＋0085第一节 |
| **它今天没有落盘形制**（`SignedDistanceField`只能在进程内构造） | 0085第五节GAP第三行 |
| **域隔离门不许`collision.py`import它**——见下一节。这条0085没有、派活书也没有，是本轨自己撞上的 | `tests/governance/test_domain_isolation.py` |

## 三、裁决三：距离场进`collision.py`走**协议**，不走import

`tests/governance/test_domain_isolation.py`的登记表里：

* `collision`在**基座**`modelgen`圈（与`geometry`／`mesh`／`modelgen`／`shapes`同圈）；
* `contact`（含`contact.field`）在**力学域**。

而该门第三条规则原文是"**基座模块 → 只许到基座。基座不依赖任何上层**"。
于是`collision.py`写一行`from physics_engine.contact.field import SignedDistanceField`
**当场红**，而那道门是共享文件，本轨不碰它（0084第三节）。

**这不是障碍，是把形制指对了方向**：基座定协议、物理域给实现，依赖仍然只向下。
`collision.py`因此声明一个结构化协议`SignedDistanceSource`
（`spacing_mm`／`value_mm`／`hessian_per_mm`／`contains_stencil`四样），
`contact.field.SignedDistanceField`**恰好逐条满足**，鸭子类型直接可用，一行import都不需要。
装配由上层（案例、`contact_pipeline`、消费方）做——那本来就是它们的活。

**这条要写进决策而不是只留在代码注释里**，因为它是可复用的判据：
将来任何"基座要用物理域的一个能力"的需求，答案都是协议而不是import，
否则就是让门去背一次愿望。

## 四、裁决四：闭式那一半怎么落

**新增的窄相一律是精确闭式的有符号距离，不是任何迭代。** 落三样：

1. **形状的精确有符号距离**（局部系）：`Sphere`／`Capsule`／`RoundedBox`／
   `FiniteCylinder`（无法兰）。四条都是标准闭式，**体内体外都精确**；
2. **球型探针对任意上述原语**：`separation = φ_B(c_A) − r_A`。
   球的Minkowski和还是球，所以这个数**同时是分离距离与穿透深度**，与既有球/胶囊族同一个语义；
3. **半空间**：`HalfSpace`是**查询侧的目标，不是`shapes.py`的形状**。
   理由是硬的——半空间**没有有限AABB**，进不了`PosedBody.world_aabb_mm`，
   也就进不了broad phase。它对四种原语的分离量由**支撑函数**给，四条全是闭式；
4. **盒对盒，仅轴对齐**（两个体的姿态都是单位四元数）：逐轴闭式，
   分离时`sqrt(Σ max(gap_i,0)²)`、相交时`max_i gap_i`，再各减一个圆角半径。
   **姿态一旦不是单位四元数就退回`broad_phase`**——旋转盒对旋转盒没有闭式，
   而本页不做迭代。

### 4.1 `FiniteCylinder`的法兰**没有实体语义**——失败关闭

`FiniteCylinder.flange_outer_radius_mm`在全仓只有一个用途：**把AABB撑大**
（`local_aabb_mm`取`flange_outer_radius_mm or radius_mm`）。
**没有任何声明说得出法兰是什么形**：两端各一片还是整段？厚度多少？内侧有没有倒角？

于是精确窄相对带法兰的圆柱**当场拒**（`CollisionQueryError`），不按无法兰算。
按无法兰算会**系统性少报穿透**（法兰比槽底大15 mm，那不是小量），
而少报是静默的那一半——AGENTS.md诚实可信度条款判的正是这一类。

**这是本轨抓到的一个真缺口**：`shapes.py`里有一个只影响AABB、不影响任何实体判定的字段，
而`_cell()`那个真实导轮（R45／法兰R60／半宽9）正好带着它。登记成GAP。

## 五、裁决五：场那条路的形制与它的诚实声明

### 5.1 `confidence`加第三档`sampled_field`

`CollisionEvent.confidence`今天是`Literal["broad_phase", "narrow_phase"]`。
**场给的数不能叫`narrow_phase`**——那一档在本仓的既有语义里是"精确到舍入"，
而场带O(h²)的分辨率误差。**把它叫`narrow_phase`就是冒充**（派活书第戊2条第3点原话：
"一个在非凸体上给出『精确穿透深度』的实现是冒充"）。

于是加第三档`sampled_field`，含义逐字是：
**"这个数是从一个有限分辨率的采样场里读出来的，它带一个已声明的偏差估计。"**

### 5.2 偏差估计**算得出来，所以必须给**

0085第三节把拟插值的展开写死了：

    S(x) = φ(x) + (h²/6)·∇²φ(x) + O(h⁴)

而场自己就能给`∇²φ`（`hessian_per_mm`）。于是**主项偏差是可计算的**：

    estimated_bias_mm = (h²/6)·tr(∇²φ)

不是一个手写的"经验容差"，是那条展开式的直接读数。三条自洽出口：

* **半空间**：φ仿射 ⟹ `∇²φ ≡ 0` ⟹ 估计恒为**0**，与0085"任何分辨率下精确"对得上；
* **球（半径R，查询点在半径ρ处）**：`∇²(|x|−R) = 2/ρ` ⟹ 估计`h²/(3ρ)`，
  **与`cases/sdf_contact_convergence`那条已经量过的主项`−h²/(3z*)`逐字同形**（差一个符号是因为那里量的是位置偏移，这里量的是距离本身）；
* **符号**：凸障碍上`tr(∇²φ) > 0` ⟹ 估计为正 ⟹ 场报的距离偏大 ⟹ **偏松**，
  与0085第三节"对凸障碍系统性偏松"同向。

**它是估计不是界**：O(h⁴)余项没有被它覆盖，`NarrowPhaseResult`的字段名与docstring
都按"估计"写，不许被读成"误差上界"。**不知道就说不知道**，这里知道的是主项。

### 5.3 非凸上它给的是什么

场对非凸件**没有额外代价**（0074第二节第2条），但要说清楚它给的是什么：
**球型探针的`φ(c) − r`在非凸件上仍然是"球心到表面的最近距离减半径"**，
那是一个良定义的量，而且**就是这一对的穿透深度**（球的Minkowski和还是球）。
**不良定义的是"两个一般形状之间的穿透深度"**，而本页根本不给那个数。
这就是第二节2.3第一行那条限制存在的理由。

## 六、裁决六：失败关闭的两个不同口径

派活书第戊2条第4点要"查询不出来要抛"。本页把它拆成两条，因为**两个调用面的契约不同**：

* **直查API**（`narrow_phase_separation_mm`、`half_space_separation_mm`、
  `shape_signed_distance_mm`）**没有"不知道"这个返回值**，所以答不出来一律
  `CollisionQueryError`。带法兰的圆柱、`MeshAsset`、旋转盒对盒、窄带外的点，四条全抛；
* **场景查询**（`BroadPhaseCollisionQuery.check_state`）**有"不知道"这个返回值**，
  它就是`confidence="broad_phase"` ＋ `penetration_mm=None`——
  那是这个字段2026-08-04出生时的全部理由。所以场景查询在答不出来时**降级而不是抛**：
  事件照报（不漏报），深度留空（不冒充）。

**两条都不是"返回一个看起来合理的数"**，这才是那句要求的实质。
写成两条而不是一条，是因为把场景查询也改成抛会**破掉不漏报这条唯一硬承诺**：
一个查不了的对会让整场查询炸掉，于是同场景里其他能算的对也一起没了。

## 七、本页登记的GAP

| GAP | 触发条件 |
|---|---|
| **`FiniteCylinder.flange_outer_radius_mm`没有实体语义**，精确窄相因此拒带法兰的圆柱；而`_cell()`里的真实导轮（R45／法兰R60）正带着它 | 第一个需要"带材真的碰到法兰"的判据出现时（S6.6"蹭边"最近）。届时要裁的是**法兰的实体形制**（两段？厚度？倒角？），不是给窄相加个近似 |
| **一般凸对凸的穿透深度（胶囊对盒、旋转盒对盒、圆柱对圆柱）仍为零** | 第一个真实构型需要它时。届时的选项**不再是EPA/MPR**（第二节已排除），而是"给这一对写闭式"或"沿探针形状做有界采样＋Lipschitz夹逼"，两条都要新裁决 |
| **`sampled_field`事件今天进不了接触管线**：`contact_pipeline.py`第408行只认`confidence == "narrow_phase"`，场事件会被它拒 | 第一个要把场窄相接进接触管线的消费方出现时。**它今天拒是对的**（失败关闭），届时要裁的是"管线接不接受带偏差声明的事件、以及它怎么把偏差传下去" |
| **法向与见证点今天只到`CollisionEvent`，接触管线还没有读它们**（第十四节）：`contact_pipeline.py`仍然只用`penetration_mm`＋自己配的法向 | 第一个要按窄相法向算摩擦锥的消费方出现时。**扩档已经做完了，欠的是管线那一侧接不接**——那不是本轨的文件 |
| **一个窄相对只给一个接触点**，而0082那条"底面是一组物质点"要的是一组 | 第一个要让一个面（不是一个点）参与接触的构型出现时。同行（EPA/MPR）在这一条上同样只给一个点，**所以这不是抄得到的**，要新裁 |
| **场那条路只接球型探针**，胶囊/盒对场当场拒 | 第一个需要非球探针对真实件的构型出现时。届时的形制是"沿探针采样＋用SDF的1-Lipschitz性给夹逼区间，区间跨零就拒"，**不是取几个点的min了事**（min会静默漏接触） |
| **`physics_scene_collision_events`面的`confidence`取值集合多了一个值**，而本轨不碰`engine_facets.py` | 合入时。今天CLI落盘的字节里**不会**出现新取值（场景格式没有场的入口，见下一行），但形制上的取值集合确实变了 |
| **场景文件格式没有距离场的入口**，于是场窄相只能由Python调用方装配，`pe-scene check-collisions`用不到它 | 第一个要把烘好的场写进场景文件的消费方出现时——而它的前置是0085那条"场没有落盘形制"的GAP，**两条是同一条链上的两环** |
| **半空间进不了broad phase**（无有限AABB），所以`half_space_separation_mm`是场景查询之外的一条独立调用面 | 第一个要让半空间参与整场候选剔除的消费方出现时。届时要裁的是"半有界体在AABB broad phase里怎么表示" |

## 八、本页明确没做的

* **没有实现EPA，也没有实现MPR**（第二节）；
* **没有测那两个数**（MPR近似误差与罚刚度柔度误差），理由在第二节末段；
* **没有做凸分解**（0073第七节第1条已裁）；
* **没有让内核读网格字节**（0074第二节，且`tests/governance/`有门守着）；
* **没有改`contact/field.py`一个字节**（它是上一批的产物；本轨读它、按它的形状定协议，不改它）；
* **没有改既有球/胶囊那条路的任何一行**——逐位不变这条判据靠**结构**保证而不是靠对拍：
  既有分支原样留在最前面，新分支只在它返回`None`时才轮得到。

## 九、实测数字（本页的证据段）

平台：macOS 26.5.2 / arm64 / CPython **3.13.12**。**墙钟不作数**（本机），列在这里的全是确定性量。

### 9.1 与既有球/胶囊窄相**逐位不变**

语料：stdlib`random`、种子`20260818`、120对球/胶囊（含Shoemake均匀姿态），其中**34对相交**。

| 判据 | 实测 |
|---|---|
| `penetration_mm.hex()`对开工前（HEAD `6541cb7`）冻下来的值 | **34/34逐位相同** |
| 直查面`narrow_phase_separation_mm`对场景查询 | **34/34逐位相同**（共用`_segment_pair_separation_mm`一个表达式，结构保证） |
| 判据本身有牙齿 | 把指纹挪1 ulp：`pytest.approx`**抓不到**、`float.hex()`**抓得到** |

### 9.2 解析五族对闭式

| 格 | 实测 |
|---|---|
| 球-球（200组随机） | max\|Δ\| = **1.421e-14 mm** |
| 球-半空间（200组随机，一般法向） | max\|Δ\| = **7.105e-15 mm** |
| 盒-盒轴对齐（400组随机，分离支350／穿透支50） | max\|Δ\| = **7.105e-15 mm** |
| 球-旋转盒 外侧 / 内侧 | Δ = **1.332e-15 mm** / **0.0**（逐位） |
| 圆角盒SDF体内体外（500点） | 与"clamp再取距离"独立写法差 < **1e-12 mm** |
| 无法兰圆柱SDF体内体外（500点） | 同上，< **1e-12 mm** |
| 有符号距离对体自身位姿的不变性（200组一般姿态） | rel < **1e-12** |

### 9.3 场那条路的偏差声明

| 格 | 实测 |
|---|---|
| 半空间场（仿射）h=2.0/1.0/0.5 | 值误差 **1.110e-16 / 4.441e-16 / 4.441e-16 mm**（≤2 ulp）；偏差估计 **7.401e-17 / 1.850e-17 / 0.0** |
| 球场（凸）偏差估计 对闭式`+h²/(3ρ)` | rel **1.889e-3 / 4.709e-4 / 1.180e-4**（h=1/0.5/0.25） |
| 同上，**实际偏差 / 估计** | **0.999992 / 1.000000 / 1.000000** |
| 孔场（凹、非凸）偏差估计 对闭式`−h²/(6ρ)` | rel **9.437e-4 / 2.361e-4**（h=0.5/0.25）；实际/估计 **1.000189 / 1.000047** |
| 环面（非凸）场值误差 | **3.193e-2 / 7.893e-3 / 1.967e-3 mm**，比 **4.0459 / 4.0127**（二阶） |

**"实际偏差/估计"那两行是本轨最强的一组数**：`(h²/6)·tr(∇²φ)`把实际偏差抓到了小数点后第五位。
**但本页不拿它去修正读数**——理由写在案例页第六节第4条：估计只是主项，
且减掉之后那个数就不再是任何一个场的读数，而调用方分辨不出"读数"与"读数减一个模型"。

### 9.4 失败关闭

| 门 | 实测 |
|---|---|
| 带法兰的圆柱 | 直查面抛；**按无法兰算会少报**：真实导轮R45/法兰R60，同一探针从"体内10 mm"变成"离开5 mm"，**符号都反了**，差15 mm |
| `MeshAsset` | 抛（"no geometry"） |
| 旋转盒对盒、胶囊对盒、胶囊对场、两个体都配场 | 直查面抛，场景查询降级为`broad_phase`＋`None` |
| 窄带外（球心那种"深在体内"的点） | 抛（"narrow band"），场景查询降级 |
| 装配期：场挂在不存在的体上／对象不满足协议 | `ShapeError`当场炸，不拖到查询时 |

### 9.5 门的规模与耗时

`tests/test_narrow_phase_shapes.py` **31条 / 1.74秒**；
`tests/cases/test_narrow_phase_signed_distance.py` **11条 / 0.95秒**；
全仓 **2447 passed**（另有2条是worktree环境门，见第十一节第3条）。

## 十、本轨抓到的东西（比新功能值钱的那一项）

1. **派活书把"二阶"转述成了"四阶"**（第零节）。0085原文与它第九节的实测比值都写着二阶，
   四阶那一档要预滤波、而0085明写不做。**这是0089第四节那条纪律的第四个同形实例**；
2. **`collision`不能import`contact.field`**（第三节）。基座圈import物理域圈，域隔离门当场红。
   0085没写、派活书没写，是本轨自己撞上的。**结论是可复用的**：
   基座要用物理域的能力，答案是协议不是import；
3. **`FiniteCylinder.flange_outer_radius_mm`没有实体语义**（第4.1节）。
   全仓只用来撑AABB，而`tests/test_shapes_and_collision.py`的`_cell()`里那个真实导轮
   （R45/法兰R60/半宽9）正带着它。少报15 mm，符号都反。
   **plans/07第六节早有一行"0032/0034 法兰轴向尺寸（维持失败关闭）"**——
   本轨证明那条债不只是"轴向尺寸"，是**整个法兰没有实体形制**；
4. **`x ** 0.5`不是`math.sqrt(x)`。** `segment_segment_distance_mm`用的是前者，
   而`**`走libm的`pow`、IEEE不要求它正确舍入——本机实测20万个正数里**277个**差1 ulp。
   **本轨没有改它**（改了会破掉逐位不变那条判据），但凡是拿闭式去撞它的判据都不能写零容差；
5. **CPython 3.12起`sum()`对float走Neumaier补偿求和**，与手写的`a + b + c`不逐位相同——
   本机实测20万组三维点积里**44972组**不同（22.5%）。
   **这一条对本仓是普遍性的**：仓里到处在写逐位判据，而`sum()`与手写循环给的是两个数。
   本轨的内核代码因此把三项相加全部写成显式表达式；
6. **一条守"没有实现"的门会随着实现落地悄悄变成守一条不成立的事实。**
   `tests/test_narrow_phase.py::test_unsupported_pair_stays_broad_phase_honestly`
   原来用**球对无法兰圆柱**当"不支持"的样本，而那一对本轨给了闭式。
   本轨换成胶囊对圆柱并把这件事写进了它的docstring；
7. **"沿轴到顶点"不是"到表面"。** 案例第一版把球对转45°的盒写成了`14 − 10√2`，
   而点(14,0,0)其实在**盒里面**（顶点在14.142），最近面是侧面、距离0.1005 mm。
   这一格现在是案例清单里被点名的一格。

## 十一、要主代理在合入后代为登记的

**本轨不碰共享文件**（0084第三节／0089第三节），下面七件由合入方做：

1. **`cases/README.md`**：索引表加一行、分层表加一行、三个"42"改成"43"。
   分层归入**只走`shapes`/`collision`几何查询**那一类（本案例不走`solve_equilibrium`）。
   `tools/check_case_pages.py`今天对本案例报**4条FAIL，全部是这一件事**（页本身已过）；
2. **`docs/plans/07`第六节**：`tools/check_gap_register.py`当前对0090报红。建议三行：

   | 出处 | 欠什么 | 触发条件 |
   |---|---|---|
   | 0090 | **一般凸对凸的穿透深度**（胶囊对盒、旋转盒对盒、圆柱对圆柱）仍为零 | 第一个真实构型需要它时。**选项不再是EPA/MPR**（0090第二节已排除），是"给这一对写闭式"或"沿探针采样＋Lipschitz夹逼"，两条都要新裁决 |
   | 0090 | **`FiniteCylinder`的法兰没有实体形制**，精确窄相拒带法兰的圆柱 | 第一个需要"带材真的碰到法兰"的判据出现时（S6.6最近）。与0032/0034那条同源，但欠的比"轴向尺寸"多 |
   | 0090 | **`sampled_field`事件进不了接触管线**（`contact_pipeline.py`只认`narrow_phase`）；场那条路只接球型探针；场景文件格式没有场的入口 | 第一个要把场窄相接进接触管线／接进场景文件的消费方出现时。**今天拒是对的**（失败关闭） |

3. **两条worktree环境门**（`tests/governance/test_worktree_env_check.py`）在本树红，
   **与本轨无关**：一条是`rtime-project-check`在`.claude/`下扫0个文件（AGENTS.md已记的空跑），
   另一条是"HEAD落后main"（并行期间main在动）。**合入主仓后应当自然转绿，但要复核一次**；
4. **面清册（`engine_facets.py`）**：`physics_scene_collision_events`的`confidence`
   取值集合多了`sampled_field`，`CollisionEvent`另加了两个缺省为`None`的字段。
   **今天CLI落盘的字节里不会出现新取值**（场景格式没有场的入口），所以形制上的字节没变；
   但取值集合确实变了，**建议合入方判一次要不要把0.1推到0.2**。本轨不改那个共享文件；
5. **`tests/governance/test_domain_isolation.py`**：**不需要改**。
   `collision`已在`modelgen`圈，本轨没有新增任何包内import（距离场走协议，见第三节）；
6. **`src/physics_engine/__init__.py`公开面清单**：**建议复核**。
   `collision`已在清单里，本轨只是在同一个模块内加了公开名（`__all__`已同步）；
7. **`CHANGELOG.md`**：建议条目见下一节。

## 十二、CHANGELOG建议条目

> * **新增（实验档，破坏性）**：`physics_engine.collision`的窄相从"只有球/胶囊族"扩到
>   **解析原语一族＋距离场协议**。`CollisionEvent.confidence`加第三档`sampled_field`
>   （场给的数带分辨率误差，**叫`narrow_phase`就是冒充**），并加`estimated_bias_mm`
>   与`resolution_mm`两个缺省`None`的字段。
>   新公开名：`CollisionQueryError`、`HalfSpace`、`NarrowPhaseResult`、
>   `SignedDistanceSource`、`shape_signed_distance_mm`、`posed_signed_distance_mm`、
>   `world_to_local_mm`、`half_space_separation_mm`、`field_separation_mm`、
>   `narrow_phase_separation_mm`。
> * **精确闭式覆盖**：球型探针对球/胶囊/圆角盒/无法兰圆柱、任意原语对半空间（支撑函数）、
>   轴对齐圆角盒对。**分离支与穿透支同一条式子**；实测对闭式max\|Δ\| ≤ **1.42e-14 mm**。
> * **既有球/胶囊窄相逐位不变**：120对语料里34对相交，`penetration_mm.hex()` **34/34相同**。
> * **距离场走协议不走import**（决策0090第三节）：`collision`在基座圈、`contact.field`在
>   力学域圈，域隔离门不许那条边。`SignedDistanceField`鸭子类型直接满足`SignedDistanceSource`。
> * **偏差是字段不是注释**：`(h²/6)·tr(∇²φ)`。半空间恒0、凸障碍`+h²/(3ρ)`（偏松）、
>   **凹面`−h²/(6ρ)`（偏紧，0085只写了凸那一半）**。实测实际偏差/估计 = **1.000000**。
> * **失败关闭两个口径**：直查面抛，场景查询降级为`broad_phase`＋`None`。
>   带法兰的圆柱、`MeshAsset`、旋转盒对盒、窄带外，四条全在门里。
> * **新增**：`cases/narrow_phase_signed_distance`——五族解析闭式＋场偏差主项两侧各一条闭式，
>   非凸环面上场值误差实测二阶（比4.0459 / 4.0127）。

## 十三、能力位：本轨的建议与它的保留

**本轨只给证据，不动台账**（0089第四节：派活书里的能力位预期是一句期望不是结论）。

**S3.2**（不规则体的网格碰撞窄相）：现状`todo`。**建议`partial`，不建议`done`。**

`partial`那一半的证据：`missing`原文里"**圆柱/盒窄相为零**"这半句**今天不成立了**——
球型探针对圆角盒与无法兰圆柱有精确闭式、轴对齐盒对有精确闭式、
任意原语对半空间有精确闭式，全部逐点对拍过（第9.2节）。
"EPA还是MPR是待裁项"这半句也不成立了——**本页裁了，两条都出局**（第二节）。

**不建议`done`的四条保留，逐条**：

1. **label里的"网格"两个字一个都没兑现。** `MeshAsset`不带几何，
   内核不读网格字节；场那条路今天只能吃**解析函数烘出来的场**
   （真实件要pcu那个独立venv，且0085那条"场没有落盘形制"的GAP还欠着）；
2. **"不规则体"这四个字只解开了非凸的一半。** 场对非凸确实没有额外代价
   （环面上实测二阶），但**探针必须是球型**——一根杆、一个盒去碰真实件，今天当场拒；
3. **一般凸对凸的穿透深度仍为零**（胶囊对盒、旋转盒对盒、圆柱对圆柱）；
4. **`sampled_field`那一档进不了接触管线**，于是"查询有了"与"力算得出来"之间还隔一层。

**`missing`建议改写为**：

> 精确闭式已覆盖：球型探针对球/胶囊/圆角盒/无法兰圆柱、任意原语对半空间、
> 轴对齐圆角盒对（决策0090，`cases/narrow_phase_signed_distance`）。
> **网格那一侧仍为零**——`MeshAsset`不带几何、内核不读网格字节，
> 三条路（EPA/MPR/距离场）都够不着，只能由外部烘好的场经`SignedDistanceSource`协议进来，
> 而那条路今天**只接球型探针**、且`sampled_field`档进不了`contact_pipeline`。
> 一般凸对凸的穿透深度（胶囊对盒、旋转盒对盒、圆柱对圆柱）仍为零。

**S3.3**：一位不动（本轨没碰质量属性）。
**S3.10 / S5.2 / S6.6**：一位不动。**S5.2自接触**要的是"匝碰匝"，
那是**胶囊对胶囊的批量候选**，既有那条路本来就覆盖形对，卡的是候选爆炸怎么控——
**本轨一个字都没碰它**，plans/18第二节把它排在轨戊之后是对的，但**它等的不是本轨**。

## 十四、追加：窄相的输出档扩到"法向＋两个见证点"（0094交回后）

**这一节是本页在轨壬（0094）交回之后追加的**，顺序与0094第1条建议的相反：
本轨先落了窄相、后扩的输出档。**如实记下来，不回头返工**——
而且**没有返工的必要**，理由是一条结构上的分层，见第14.3节。

### 14.1 那个"几乎免费"是真的

0094点名的事实经复核成立：`segment_segment_distance_mm`从2026-08-04起
就在算两条中轴上的最近点`closest1`/`closest2`，**然后在最后一行把它们扔掉**，
只返回一个标量。于是窄相有距离却没有法向与接触点，**接不上力学**
（力要作用在一个点上、摩擦锥要一个法向）。

本轨的做法：新增`segment_segment_witnesses`，**它就是那个函数原来的代码**，
只是不再扔掉那两个点；`segment_segment_distance_mm`现在是它的第三项。
**一次运算都没有多做，一个字节都没有变**（500组随机输入`float.hex()`全等）。

### 14.2 三样输出与它们的约定

`NarrowPhaseResult`与`CollisionEvent`各加三个缺省`None`的字段：

* `normal_ab`——**单位法向，从B指向A**（Bullet的`normalBtoA`同一个约定）；
* `witness_a_mm` / `witness_b_mm`——两个体表面上互相最近的那两个点。

**法向的来路统一是`∇φ`**：有符号距离场的梯度在可微处就是单位外法向，
于是"接触法向"不需要第二套几何。四条路由各自的实现：

| 路由 | 法向从哪来 |
|---|---|
| 球/胶囊族 | 两条中轴最近点之差，归一化 |
| 球型探针对解析原语 | `posed_signed_distance_gradient`（本轨新增的四条解析梯度） |
| 轴对齐盒对 | 分离时是间隙向量、穿透时是**最小重叠那一轴** |
| 距离场 | 场自己的`gradient`，**归一化之后**才用——B样条拟插值的`|∇φ|`只是近似为1，而法向要拿去乘力 |

### 14.3 为什么不用返工：分离量与法向是**两层**

`_segment_pair_result`把分离量交给`_segment_pair_separation_mm`（一个字不改），
法向与见证点另算一层。**这条分层是有意的**：0094第1条要的"先扩输出档"
如果顺手把分离量也重写一遍，就等于用一次扩档换掉了本仓唯一一条逐位判据
（既有球/胶囊窄相34/34指纹）。**先落后扩因此不是欠债，是把那条判据保住了**——
扩档时它一直在那里盯着。

### 14.4 判据：两条**零新增金标**的自洽恒等式

0094给的三条可抄门形状里，本轨抄了两条、核了第三条：

1. **Bullet的自洽恒等式**（抄）：`witness_a == witness_b + separation · normal_ab`。
   零新增oracle——它判的是"法向、接触点、距离三样互相对得上"；
2. **见证点真的落在各自面上**（本轨自己加的第二条）：`|φ_A(witness_a)| ≈ 0`。
   **两条必须并列**：恒等式对整体平移是盲的，"在面上"对沿面内滑动是盲的。
   两条各配一个必红用例，**第二个必红（沿面内滑动）正是证明它们不能互相顶替的那一条**；
3. **IPC的"超集＋逐条证人"**（核过，**本仓已经有了**）：
   `cases/broadphase_superset`对**每一对**都算精确分离量（不只是AABB相交的那些），
   `superset_counterexamples`计数严格为0。这正是0094描述的形状，**不需要新加**；
4. 另加一条0094没提的：**梯度对中心差分**。前两条对一个**整体错的方向**是盲的
   （它们只判自洽），只有拿有限差分去撞才抓得到"梯度写成了另一个形状的梯度"。

### 14.5 实测

| 路由 | 恒等式残差max | 见证点`|φ|`max |
|---|---|---|
| 球/胶囊族（120对语料，全部有定义） | **1.0049e-14 mm** | **2.1316e-14 mm** |
| 球型探针对圆角盒/圆柱（400组，含一般姿态） | **4.0701e-15 mm** | **1.6875e-14 mm** |
| 轴对齐盒对（400组，分离314／穿透86） | **8.1886e-15 mm** | **3.5527e-15 mm** |
| 距离场（球，h=0.25） | **0.0**（三样出自同一次查询） | 见证点半径**9.998188406** vs 10 |

**最后一行那个差值是1.8116e-3 mm，而`h²/(3ρ) = 0.0625/34.5 = 1.8116e-3`**——
见证点偏离真面的量**正好是本页第五节那条偏差主项**。
两个独立算出来的东西撞到一起，是本轨对那条主项最强的一次交叉验证。

梯度对中心差分（四种原语各200点，步长1e-5）：max偏差 **< 2e-6**，模恒为1到末位。

### 14.6 法向没有定义时**报`None`，不编方向**

三种构型今天返回`None`：同心球（两条中轴相交）、圆柱轴上且侧壁最近、
以及`broad_phase`那一档（它整档都是"不知道"）。

**这是"诚实可信度"在法向上的执行面**：距离照给（穿透深度仍然良定义），
方向留空。编一个方向出来会让摩擦锥安静地作用在一个错方向上——
而0094给的两条同行反面教材正是同一形状：MuJoCo**静默**把非凸网格换成凸包、
Bullet修内部边伪法向那套**假设流形**且非流形边被邻居逐个覆盖**且无告警**。
**本仓的网格恰恰非流形**（0074第二节第3条：6条与10条非流形边）。

两处**并列的选择**要写在明处，它们不是`None`：盒体内的最近面并列时取轴序第一个、
盒对盒最小重叠并列时取轴序第一个。**并列意味着两条法向都对**，取一个是选择不是编造。

### 14.7 0094给的两条实证在本页的落点

* **PhysX的`GuSDF.h:329`用`|w| > 0.5`判内外**——这**独立证实了0085第十节第1条**
  （pcu的`triangle_soup_fast_winding_number`文档写反了，阈值是0.5不是符号）。
  两家独立到达同一个阈值，那条更正因此不再只是本仓的一次实测；
* **EPA八种状态里六种是失败**（`btGjkEpa2.cpp:586—595`）、
  **MPR的深度沿"两物体中心之差"射线量**（`btMprPenetration.h:165—169`）故对细长体系统偏大，
  **且两者都只给一个接触点**——最后半句与0082"底面是一组物质点"的多点路线方向相反。
  这三条**加强了本页第二节的裁决，但不改变它**：本页排除它们靠的是前置
  （没有凸体输入），而不是它们算得准不准。
