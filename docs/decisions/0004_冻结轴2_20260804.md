# 0004 冻结轴2：稳定ID与单位量v1.0（2026-08-04）

状态：已决（同0003的授权与冻结标准：蒸馏已验证实践可直接v1.0）。

## 决定

[spec/03 轴2](../spec/03_轴2_稳定ID与单位量_v1.md)以v1.0冻结。

## 为什么它排第二

映射设施（WDS当前主线）的新契约即将出生，它们的字段命名、ID形制、证据段
都由本轴管。轴3—轴5（内容寻址、run package、原子发布与严格复读）管的是
运行与产物，晚于输入声明被消费，可后置。

## 证据锚点

- WDS：design/15（四段身份+vNNN+材料证据段）、案例只增不改（上手指南）、
  单位后缀制（AGENTS命名约定）、`assert_reader_compatible`（ID语义由契约冻结）。
- FTS：`contracts/scene.py:84`起（ComponentKind15种、StableId、Quantity、
  AssertionRecord）、AGENTS硬约束（不得硬编码组件数/两通道/单拓扑）。

## 已知后果

- WDS两条缺口（内核裸float、penalty_context单数）都是**既有登记**的欠账，
  本轴把它们从"库化愿望/design/23方案"升格为**规范偏差**，必须挂收敛计划。
- 规则4"硬编码数量"无法全自动验红，判据里如实降为评审判据——规范不冒充
  自己有做不到的门。
