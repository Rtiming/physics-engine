# 0007 M-E1第一批代码落地（2026-08-04晚）

状态：已决并执行（用户"继续开发吧"授权按plans/00推进）。

## 落了什么

- `physics_engine.facets`：面清册+失败关闭读取端（轴1参考实现，含状态分级；
  清册自动登记`facet_registry`自条目——规则1"清册本身也是一个面"进了代码）；
- `physics_engine.canonical`：声明式规范化JSON读写对（轴3规则2；
  两仓实测参数作为现成`WDS_PROFILE`/`FTS_PROFILE`声明；NaN禁令与键排序
  是规则不是参数）；
- `physics_engine.identity`：四段身份、命名空间ID、单位后缀校验
  （轴2参考实现；无量纲必须显式声明，不许留空装有）；
- `physics_engine.engine_facets`：本仓自己的面清册，首个条目是验收回执面；
- `tools/accept.py`：本仓验收器（轴6落地）——30/120双档、功能/计时/仓库
  稳定三轴正交、超时绝不pass、零执行BLOCKED、裁决全是纯函数；
- 52条测试全绿，其中11条governance元测试直接测裁决函数与回执面兼容性；
  每个模块带"必须红"用例（未知面、未来minor、重复键、NaN字面量、
  裸物理量字段、未声明无量纲、仓库漂移BLOCKED）。

## 三前提检验（0001）

1. 规范已冻结：轴1/2/3/6，全部v1.0；
2. 真实消费方：**本仓验收器当日即消费**`canonical`与`engine_facets`
   （回执按FTS_PROFILE落盘、盖清册登记的面版本）；WDS在批次B以
   "两实现输出逐字节一致"门切换，不预支；
3. 指纹不变义务：本批未触碰任何消费方字节，义务空集。

## 自吃药的闭环

验收器写的回执是一个登记过的面；governance测试用本仓自己的
`assert_reader_compatible`验回执版本、并验未来minor被拒。
回执落在被ignore的`work/`下，写回执不搅动仓库身份指纹——
消费方曾踩过"实测值写回被测文件把回执搞stale"的循环，这里从结构上避开。
