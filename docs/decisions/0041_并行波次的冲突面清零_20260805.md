# 0041 并行波次的冲突面清零（2026-08-05）

状态：已落地。用户要求"**多开几个助手并行**"，并要求执行之后
"**整理验收以及整合优化**"。

本页不裁物理，只做一件事：**在派人之前把并行的冲突面降到零**。
理由来自本仓已有的教训——plans/02第三节的分槽规则是为CHANGELOG立的，
但这一波五条轨道会同时撞**三个**共享文件，光靠append规避不掉。

## 一、三个共享冲突面，逐个处理

| 冲突面 | 为什么会撞 | 处理 |
|---|---|---|
| `benchmarks/engine_budgets.baseline.json` 的源码上限 | **五条轨道各自加代码，各自撞上限、各自抬** —— 五份互斥的抬升记账 | **本页一次抬到位**，轨道禁止改此文件 |
| `tests/governance/test_domain_isolation.py` 的 `ENGINE_RINGS` | 新模块不登记即红（第四条门）；电磁与刚体都要加行 | **本页预登记**，轨道只在自己那行落实 |
| `docs/decisions/` 编号 | 五条轨道并发写 `00NN` 必然撞号 | **本页预分配 0042—0046** |

## 二、预算抬升（记账，第6次）——**这次是预支，如实标注**

前五次抬升都是"真撞破后抬"。**本次不是**：实测 328273 对上限 458752，
余量还有 130479 字节，门没红。

**但五条轨道并行意味着五份增量同时落地**，若不预抬，
最先合并的那条会把上限吃掉，后面四条各自撞红——而它们撞的不是自己的增长，
是**别人的**。那不构成"增长必须是一次被记录的决定"，只是并发噪声。

抬到 **655360（640KiB）**，约实测的 2.0 倍。

**同时如实登记本次抬升的性质不同**：这是**预支不是追认**，
因此附一条自我约束——**并行波次收口时必须复测实测值，
若实际增量远小于预支额度，下一次发版前把上限调回"实测×1.4"**。
理由是0039写过的那条：**绊线一旦长期不响就等于被拆了**。
预支额度如果长期空着，这道门就名存实亡。

## 三、域环预登记

`electromagnetics` 作为**第三个物理域**登记（力学、光学之后）。
按域隔离的第一条规则，它只许到**同域与基座**，不许到力学、不许到光学。
场景②（线圈互感）与场景①（超导带电流）都落在这个域里。

`rigidbody` 登记为**力学域**——它用 `state`、`geometry` 的惯量张量，
与 `energies`/`solve` 同域。

## 四、编号预分配

| 号 | 轨道 |
|---|---|
| 0042 | 电磁域与同轴圆环互感 |
| 0043 | 刚体转动与自由飞行 |
| 0044 | 光学双光束干涉 |
| 0045 | `Scene` 装配容器 |
| 0046 | `PointLoad` 与 Euler 屈曲 |

## 五、收口时我要做的三件事（用户明确要求的"整理验收以及整合优化"）

1. **验收**：五条全部合并后跑一次 `accept.py full`，**并复跑每条轨道申报的必红门**
   —— 轨道自己说"验过红"不算数，合并后的树上再红一次才算数
   （轴6规则5：金标件与被测输入都在执行树内，五条合并后那棵树变了）；
2. **整理**：案例阶梯（0040）打钩、`cases/README.md` 索引、CHANGELOG 收口合并；
3. **优化**：合并后统一做一次性能复测——**五条轨道各自的加速不可加**，
   而 eager import 模块数与冷启动是**全局量**，只有合并后才测得准。
