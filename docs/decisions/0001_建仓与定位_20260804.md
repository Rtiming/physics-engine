# 0001 建仓与定位（2026-08-04）

状态：已裁决（用户2026-08-04晚口述，记录于winding-deviation-sim的design/25第七节）。

## 裁决

两个数字孪生仓（winding-deviation-sim力学、fts-digital-twin光学）走**共同仓库、
深度融合、接口规范统一**。本仓即那个共同仓库。

## 位置判断（本仓为什么在这里）

`/Volumes/MacDate/Projects/physics-engine/`，理由四条：

1. **中立地**：与lab、fts-spectrometer平级的顶层根。放进任何一方的树都会把
   另一方耦合到不相关的工作区（lab是实验室工作区、fts-spectrometer是光谱仪项目）。
2. **外置盘规矩**：Mac内置盘只有256GB，项目一律落`/Volumes/MacDate`。
3. **git同步**：纯代码/文档仓，按rtime-project范式走git，不进Syncthing。
4. **远程惯例**：起步走orangepi裸仓库（与fts-digital-twin同惯例，私有基建），
   开源时另加GitHub Rtiming（与winding-deviation-sim同惯例）。

## 形制

规范先行、代码后置（README阶段阶梯）。代码进仓的前提：对应规范已冻结，
且两个消费方采纳后既有产物指纹逐字节不变。

## 治理边界

- 本仓不反向依赖任何消费方。
- fts-digital-twin采纳前必须先在其仓内以新ADR取代其0001（standalone边界）——
  在FTS自己的会话与治理下完成，本仓与WDS不代写。
- winding-deviation-sim采纳属动src/，走其冻结diff→重签回执→全量门禁流程。
