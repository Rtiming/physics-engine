# 0008 M-E2上半：溯源机械层落地（2026-08-04深夜）

状态：已决并执行（用户"继续23"授权波次3）。

## 落了什么

`physics_engine.provenance`——轴5的**机械半边**参考实现：

- 写侧：O_EXCL耐久写+fsync、目录fsync（Windows按平台返回）、
  **no-replace目录改名**（macOS `renamex_np`/Linux `renameat2`/
  Windows语义等效/其它平台失败关闭不静默降级）；
- 读侧：O_NOFOLLOW四点签名保护读（读前/打开/读后/最终）、目录初末
  签名快照+根身份(dev,ino)比对、`verified_bytes_snapshot`机械严格复读。

7条测试含红例：覆盖O_EXCL拒覆盖、空目录竞争者拒绝、符号链接拒收、
非常规条目拒收、**观察窗口内被碰即拒**（monkeypatch注入mid-verify突变）。

## 落地门执行记录

**消费方真实回放：WDS全部36个已发布运行目录逐例机械复读通过**；
篡改副本的字节偏移在快照中如实反映（语义判拒归装配半边）。
套件内保留env驱动的回放测试（`PE_REPLAY_CASE_RUNS`），无env时skip——
仓内不写死消费方绝对路径（可移植性校验0错误）。

## 明确的边界与剩余

本批只保证"拿到的字节=盘上那一刻的字节，观察窗口内无人动过"。
**语义半边**（manifest绑定、哈希自洽、生命周期状态机、发布装配）
=M-E2下半，未做；届时的落地门才是"复读判定与消费方reader**含语义**
逐例一致"。诚实边界与WDS reader同款：不声称阻止复读结束后的外部写者。
